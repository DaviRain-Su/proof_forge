//! Integration-test crate for Solana ELF runtime differentials.
//!
//! Product tests live under `tests/`. This library unit is intentionally empty
//! so `cargo test` can load the package without a binary target.

#![allow(dead_code)]
