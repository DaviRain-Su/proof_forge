# Goal slice — N-2: ContextRead + callerContext

> **Run inside Goal — not chat-side “almost done”.**
> Primary entry (finish this slice then drain):
>
> ```text
> /goal @.grok/goals/prompt-n-2-finish.md --budget 4000000
> ```
>
> Or master drain starting here:
>
> ```text
> /goal @.grok/goals/prompt-master-queue.md starting at N-2 --budget 8000000
> ```

## ID
`N-2`

## Phase
`normalize` · milestone hint `D2` · shared_cutover `True`

## Objective
Ship **product** `context.caller` → Principal via `Op.ContextRead` key
`proof-forge.context.caller.v1` (+ wire requirement `context.caller`), keep
`context.unixTimeSeconds` → UInt64, through **CheckV1 + Normalize + Reference**.
Target Plans may remain **FAIL-CLOSED** with tests. One **local** commit; never push;
never formal TASK/TST claims.

## Status (live — re-check tree)
- Backlog must stay **pending** until this slice has a **commit whose file list
  includes the N-2 product path** (not a docs-only mark).
- As of handoff (HEAD `f682eb01e` N-1), **uncommitted WIP already exists** for N-2
  (~13 files). Treat that dirty tree as **this Goal’s work**, not “others’ WIP”.
- Do **not** discard the WIP unless tests prove it wrong and you re-implement under
  the same allowlist.

## Dependencies (must be done)
`N-1` (commit `f682eb01e` or later on main)

## Allowed path prefixes
```
ProofForgeV2/Semantic/
ProofForgeV2/Typed/
ProofForgeV2/Targets/
ProofForgeV2/Source/ContextCommitSurfaceV1.lean
Tests/
docs/engineering-backlog.md
supply-chain/
```

## Expected product surface (acceptance)
1. Source place `context.caller` resolves and type-checks as **Principal**.
2. Normalize lowers it to `Op.ContextRead` with schema key
   `proof-forge.context.caller.v1` (see `callerContextKeyV1`).
3. Requirements freeze includes wire id `context.caller` when used
   (`wireContextCallerIdV1` / `callerContextRequirementV1`).
4. Reference admits Principal and steps ContextRead for caller (product helpers OK).
5. `context.unixTimeSeconds` still works; dual flags `usedContextUnixTime` /
   `usedContextCaller` merge both requirement rows when both used.
6. Target Plan path for ContextRead may stay fail-closed; tests must document that.
7. Focused tests green; SBOM refresh if `ProofForgeV2/**` changes; backlog → **done**
   only **after** local commit.

## WIP inventory (do not re-do from zero)
If `git status` shows these paths dirty, **finish / verify / commit** them:

| Area | Files (typical) |
|---|---|
| Wire ids / keys | `RequirementIdsV1.lean`, `Wire/ModelV1.lean`, `Wire/RequirementsGateV1.lean`, `Wire/InvariantClosureV1.lean` |
| Surface | `Source/ContextCommitSurfaceV1.lean` (`isContextCallerPlaceV1` / read places) |
| Normalize | `Semantic/NormalizeV1.lean` — dual ContextRead lowerPlace; flags; merge rows; **`synthPlaceTypeV1` must know context fields** so `context.caller == …` typing works |
| Typed | `TypeCheckV1.lean`, `NameResolutionV1.lean` |
| Resolver | `Targets/RequirementResolverV1.lean` |
| Reference + tests | `ReferenceMachineV1.lean`, `Tests/Semantic/ReferenceV1.lean`, `Tests/Typed/CheckV1.lean` (`testContextCaller*`) |
| Backlog | `docs/engineering-backlog.md` — mark done **only after** commit |

### Known bug class (already patched in WIP — re-verify)
`synthPlaceTypeV1` previously treated bare `context` like a normal place → error
`S1 bare place 'context' is neither param nor state` when comparing/returning
`context.caller`. Fix: admit field chains
`context.unixTimeSeconds` → UInt64 and `context.caller` → Principal in
`synthPlaceTypeV1` (and lowerPlace dual path). Re-run typed shard; if residual,
fix in Goal — do not claim done with red log.

## Focused checks
```bash
# Prefer logging under Goal SCRATCH if available:
#   just test-shard typed 2>&1 | tee "$SCRATCH/slice-N-2-checks.log"
just test-shard typed
# If Semantic/Reference or targets touched and time allows:
just test-shard targets
just docs-check
just sbom-package-files-refresh   # required when ProofForgeV2/** changes
git diff --check
```

Skip full `just ci` only if focused shards + docs-check + sbom are green and
slice is small; prefer `just dev-check` when unsure.

## Commit
- One local commit, message like:
  `feat(N-2): context.caller Principal ContextRead + wire row`
- Never `git add -A` blindly across unrelated dirt; stage only allowlisted paths.
- Never push.
- After commit: backlog N-2 → **done** with date + one-line scope (Plan FAIL-CLOSED OK).

## Engineering-slice args skeleton
```json
{
  "slice_id": "N-2",
  "milestone": "D2",
  "objective": "context.caller Principal ContextRead + CheckV1/Normalize/Reference; Plans FAIL-CLOSED OK",
  "dependencies": ["N-1"],
  "allowed_paths": [
    "ProofForgeV2/Semantic/",
    "ProofForgeV2/Typed/",
    "ProofForgeV2/Targets/",
    "ProofForgeV2/Source/ContextCommitSurfaceV1.lean",
    "Tests/",
    "docs/engineering-backlog.md",
    "supply-chain/"
  ],
  "focused_checks": ["just test-shard typed", "just docs-check"],
  "verification_commands": ["just docs-check", "just sbom-package-files-refresh", "git diff --check"],
  "deletion_zero_patterns": ["planFromAlpha", "alphaResidualOf"],
  "shared_cutover": true,
  "commit_message": "feat(N-2): context.caller Principal ContextRead + wire row",
  "constraints": "Serial shared-core. Own existing N-2 dirty WIP. Never push. Never formal. Backlog done only after commit."
}
```

## Workflow hooks
1. `BASE=$(git rev-parse HEAD)` if starting clean; if WIP present, BASE = last commit (N-1).
2. Optional: `proof-forge-one-slice` after green uncommitted tree.
3. Commit; then if running master drain → **immediately** next pending (`N-3` or first pending by backlog).

## Global forbidden
- No push, force, amend of published commits, secrets, network package install
- No new formal TASK-*/TST-*/EV-*/freeze objects
- No just governance-check / release-check / Stage-0
- No fallback/dual-reader/second ProgramV1 decoder
- No formal D1–D4 completion claims
- Do **not** mark N-2 done in backlog without a commit
- Do **not** “finish N-2 in chat” outside Goal

## After N-2
Master drain default next: **N-3** (Commit disclosure) if still pending; else first
pending from backlog × QUEUE order.
