# Goal slice — SYS-S4-CW: MessageInfo.funds

## ID
`SYS-S4-CW`

## Objective
CosmWasm execute `MessageInfo.funds` single-denom (`stake` C1) → UInt64.
Query/view fail closed. cw-vm mock gate. Not wasmd/formal.

## Dependencies
`SYS-S4-SHARED`
