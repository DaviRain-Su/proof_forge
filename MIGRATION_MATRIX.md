# D1–D4 设计要求与代码完成度矩阵

本文件是 **engineering fact snapshot**，不是 task completion ledger，也不是 formal evidence。

- Original audit baseline date: `2026-07-26`
- Original audit baseline commit: `8b42f7ebeb60652d1789e495f23247e5685b1e22`
- Engineering fact updates through: `2026-07-28`（后续增量事实见本矩阵与 `docs/06-implementation-log.md`；上述 commit 仅为原始审计基线）
- Formal task source: [`docs/04-task-breakdown.md`](docs/04-task-breakdown.md)
- Test requirement source: [`docs/05-test-spec.md`](docs/05-test-spec.md)
- Product migration decision: [`docs/adr/0019-single-programv1-source-authority.md`](docs/adr/0019-single-programv1-source-authority.md)

## 结论

D1–D4 共 27 个 formal task，当前仍为：

| Milestone | Formal 状态 | Engineering 结论 |
|---|---|---|
| D1 | `TASK-D1-01` blocked，D1-02..08 pending | ProgramV1 wire/hash/NodeId 地基很强；CLI 只接通窄 V1 decoder，Lean command/export 仍在旧轨，worker/diagnostic 未完成 |
| D2 | 7/7 pending | 只有 UInt64 Counter/Accumulator 所需的 alpha Typed/Semantic 子集；正式 SemanticProgramV1、provenance、完整 checker 不存在 |
| D3 | 7/7 pending | 有可运行的 alpha registry/materializer/output/CLI；SupportClaim、BuildIdentity、exact resolver 与 OutputSetV1 不存在 |
| D4 | 5/5 pending | EVM Plan/Yul/ABI/solc/Anvil 算法已有真实功能，可复用；仍绑定 alpha D2/D3 contract，不能按 D4 完成 |

因此当前产品不是 `active/` 中的旧 v1，也不是正式 D1–D4 新设计完成态；准确描述是：

```text
ProgramV1 product subset
  → alpha Typed.Program
  → alpha Semantic.Program
  → alpha descriptor/contains resolver
  → target-owned alpha Plan/IR
  → v2alpha1 output
```

## 完成度口径

| 标签 | 可验证含义 |
|---|---|
| **可复用地基** | 新设计需要的 type、codec、primitive 或算法已存在，但没有完整产品接线 |
| **新轨子集** | 当前 CLI 确实使用 ProgramV1/new entry，但只覆盖规范子集 |
| **旧轨实现** | 行为主要存在于 `Source.Program`、quoted payload 或旧 schema 中 |
| **功能型 alpha** | 有真实端到端行为和测试，但 contract 不是正式 D1–D4 contract |
| **缺失** | 没有对应 production implementation；邻近 primitive 不计完成 |
| **替代就绪** | 新实现是唯一产品路径、已有 TST 迁移完成、旧引用归零并通过删除门禁 |

不使用百分比。formal 状态与 engineering 标签是两条独立轴；development test 成功不能把
`blocked`/`pending` 改成 `done`。

## D1：Source frontend

| Task | Formal | 新设计要求 | 实际代码与产品接线 | 现有测试事实 | Engineering | 迁移与旧代码处理 |
|---|---|---|---|---|---|---|
| `TASK-D1-01` | blocked | token/span、完整 ProgramV1 wire/hash、NodeId、同一 immutable source origin | `ProofForgeV2/Source/*V1.lean` 已有完整 ProgramV1 AST、root codec/decoder/validator、`ValidatedSourceV1`、`sourceHashV1`、57-tag traversal、NodeId assign/collision seam；`SpanV1.lean` 可验证原始 parser span。新增 `ProofForgeV2/Source/SpanJoinV1.lean` 已建立 immutable parser snapshot → canonical node preorder 的 exact span join（透明 syntax 结构跳过、共享 token 共享 span、count/tag 不一致 fail closed），Loader 提供 additive `selectProgramV1WithSpans`（identity/canonical bytes/sourceHash 不变）；NodeId inventory 与 SourceOrigin 尚未接入 compiler-core/diagnostic | `SourceAst*` wire suites、full-tag/field-count/marker/unknown-tag goldens及 independent Python oracle覆盖大量 wire事实；`ProgramV1SpanJoin` 固定全 constructor 家族的 path/count 精确对应、命令 span 包含、preorder 单调、注释/布局变体 delta shift、same-length rename 的 identity/position 分离与 synthetic/count-mismatch fail closed；不是 contained frontend integration | **可复用地基**；hash 与 span join 已接线，NodeId/origin 未接入 compiler | span join 已完成；把 NodeId inventory + SourceOrigin 接入 compiler/diagnostic 后保留 V1模块；删除 legacy source canonical/hash只能等 D2 consumer迁移 |
| `TASK-D1-02` | pending | 唯一 `program ... where` parser直接产出 validated ProgramV1 | `Syntax.lean` `elab_rules` 已原子切到 V1：`decodeProgramCommandV1Checked` 产出 `ValidatedSourceV1`，随后 `canonicalValidatedSourceAstBytesV1` 生成 canonical root bytes，最终以 `@[proof_forge_program]` 登记 `ProgramExportPayloadV2{schema="proof-forge.program-export.v2"}`；legacy `decodeProgramCommandChecked` 与 Source AST decoder 保留给旧 Loader API，但不再参与 command 路径。CLI `selectProgramV1` 仍是当前产品调用入口 | `ProgramCommandAcceptance` 验证 command 产出 v2 payload、identity join、非法顶层 kind/run_cmd 拒绝；`CounterV1Evm` 验证 CLI V1 subset | **新轨子集**（command 与 export 已是 V1 唯一路径） | 完成 v2 export 切over；删除 `quoteProgram` 与旧 command decode/quote 路径；保留 legacy decoder 直到旧 Loader API 迁移 |
| `TASK-D1-03` | pending | 13种 Phase-1 declaration、完整 type/default elaboration | legacy decoder覆盖 state/struct/enum/const/event/error/init/entry/view/fn/invariant/requires/proof；V1 Loader 已从 Syntax 直接接线 13 种 declaration alternatives，并保留 source order/raw identity；递归 TypeV1 prefix atom decoder内部可构造 primitive/Named/Bytes/Field/Option/Array/Map，`pfType` surface 已通过通用 same-line prefix-atom rule 补全 `Map K V` 任意嵌套 key/value（Array/Option 容器仍保留既有 fixed-combo surface，未承载 `Array Map`/`Option Map` 等递归子类型，这些形式继续 parser-boundary reject；legacy reader 行为不变），`ProgramV1TypeSurface` 固定所有 EBNF Type 替代形式、Map 嵌套、canonical bytes/sourceHash 类型变更绑定、ordinary/escaped named-type equality 与完整负例矩阵；legacy reader 对 Map/named 等不支持的类型保持 fail closed。全部 declaration negatives 与 D2 typed legality仍未闭合 | `DeclarationAcceptance`仍包装大量 legacy Source AST suites；`ProgramV1Declarations`覆盖 source→Loader V1 的13种 declaration正例、source order、Named边界和 Map param/result，以及少量 legacy fallback/type/extension/proof negatives；`ProgramV1TypeSurface`覆盖完整 type surface；新增 `ProgramV1DeclarationNegatives` 直接覆盖 ProgramV1 declaration negatives（reserved/qualified 声明名、malformed syntax、empty aggregates、duplicates、reserved components、visibility misuse）；ProgramV1 full-tag golden仍是constructed value | **新轨子集 + 旧轨测试残留**（declaration negatives 已新增 `ProgramV1DeclarationNegatives`） | 新增 `ProgramV1DeclarationNegatives` 覆盖 declaration negatives；继续迁移完整 TST-SRC-004 剩余 negatives 与旧 Source AST tests；替代就绪仍需唯一入口、legacy引用归零和删除门禁，不得经 legacy AST投影 |
| `TASK-D1-04` | pending | 完整 statement/expression/place/pattern；local fn与qualified external call/schedule分流 | grammar/legacy decoder有大量 unary/binary、let/assert/revert/emit/if/for等；V1 Loader 现已从 Syntax 直接构造 `.call/.schedule ExternalCallExprV1`、最小 `.if_/.for_` control-flow slice、`.localCall` / `.constructor` / complete `Place ::= Ident PlaceSuffix*` expression slice（任意混合 `.` field 与 `[Expr]` index suffix，构造嵌套 `.place (.field/.index ...)`）、prefix `-`/`~`/`!` `.unary` expression slice、`+`/binary `-`/`*`/`/`/`%` `.binary` arithmetic expression slice、`<<`/`>>` `.binary .shl/.shr` shift expression slice、`==`/`!=` `.binary .eq/.ne` equality expression slice、`<`/`<=`/`>`/`>=` `.binary .lt/.le/.gt/.ge` ordering comparison expression slice、`.literal (.string value)` string literal expression slice（entry return/let-value positions，含 empty/ASCII/escaped quote/backslash/tab/newline/Unicode scalar decoded values、alternate Lean escape spelling canonical identity、string payload/tag hash non-aliasing、redundant grouping identity 与 parser-boundary negatives）、dot-only `.place (.field ...)` field-place slice与任意 PlaceSuffix chaining slice（bare/field/index/mixed-chain rvalue 与 assignment target，含 multi-field/source suffix order、escaped raw component canonical identity、unary/binary operands、canonical ProgramV1 bytes/sourceHash equality、field-vs-whole-escaped-name/depth/order/suffix-kind non-aliasing、malformed dot/index boundaries、reserved component order、target-before-rhs 与 earlier-suffix-before-later-suffix priority，并固定 uppercase/lowercase root case-sensitive identity、constructor/local-call parentheses classification、terminal qualified-base index acceptance，继续拒绝 `(x)[0]`/`f()[0]`）、core statement slice（annotated/omitted-type `.let_`、simple-component、field-place及任意 place-suffix target `.assign`、valued/value-less `.return_`、`.assert_`/assert-else）、statement-level `.match_` slice（`match Expr with` + 一个或多个 `| Pattern => do Block` arms，Pattern 新增 `ConstructorPattern ::= QualifiedId "(" PatternList? ")"`，空参数、嵌套与混合参数 patterns 经 `pfPattern` max-precedence form 与现有 qualified-id helper 解码为 `PatternV1.constructor`，保留 scrutinee/arm/pattern/body source order、raw identity 与 canonical hash binding；wildcard/bind/bool/integer/string literal 行为不变，单组件 call-like pattern 继续 fail closed，nested match statements 递归解码；expression-level `.match_` slice（`match Expr with` + 一个或多个 `| Pattern => Expr` arms，使用独立 `pfExprMatchArm` category，arm value 为完整 precedence-0 expression，支持 nested match expression 与 parenthesized operand，保留 scrutinee/arm/pattern/value source order、raw identity 与 canonical hash binding；statement match 与 expression match 通过不同 arm category 严格分离，`do` block 进入 expression arm 与 bare expression 进入 statement arm 均在 parser boundary 失败；legacy Source reader 保持 fail closed）以及 `.revert`/`.emit` statement slice，保留 raw qualified components、escaped embedded-dot raw components、args/index/source order、unary/binary operator与 operand order、same-tier left-assoc（含 shift cross-operator同层）、MulExpr over AddExpr、AddExpr/MulExpr/UnaryExpr over ShiftExpr、Shift/Add/Mul/Unary over equality、shared comparison-tier ordering/equality chain non-associativity、`&`/`^`/`|` bitwise operator tag/source-order/raw escaped identity、bitwise left-assoc/right-nested grouping、Compare > bit-and > bit-xor > bit-or precedence、bitwise preservation of higher unary/arithmetic/shift behavior、enum variant pipe coexistence、`&&`/`||` logical operator tag/source-order/Bool/raw escaped dotted place identity、logical same-tier left-assoc/right-nested grouping、Compare > `&` > `^` > `|` > `&&` > `||` precedence、logical preservation of higher unary/arithmetic/shift behavior、logical redundant grouping canonical identity/hash non-aliasing/malformed digraph boundary/left-before-right portable-name errors、unary-over-multiplicative、grouping override/canonical redundant grouping identity、nested/dangling-else ownership、statement/source order、raw iterator/binder/target/condition/error/event identity、revert/emit argument count/order/nested expression shape与 loop endpoint order；`decodeNameV1` 已在 ProgramV1 Syntax decoder 层拒绝多 component Lean Name，whole-escaped dotted identifier 仍是一个 raw component，unescaped qualified revert/emit name 在 D2 error/event lookup 尚未迁移前 fail closed；dot-only field-place chains（含 uppercase/lowercase roots）与任意 mixed PlaceSuffix chains 已迁移；并拒绝 one-component/reserved/legacy string、malformed core/if/for/call-like/index/arithmetic/shift/revert/emit/place-suffix、reserved iterator/callee/path/base/binder/target/error/event、`(x)[0]`/`f()[0]` grouped/call base suffix 和非法/越界 exact decimal bound；固定除零/模零与 shift count 0/64只形成 Source AST，不做 D1 numeric/width/sign求值或拒绝；仍缺剩余 pattern forms、D2 error/event lookup、typing/effect、Semantic rollback、event runtime与target behavior等完整迁移。shared grammar 仍保留 string-call shape 用于稳定拒绝，不能写成 string grammar 已删除 | `ProgramV1ExternalStatements`、`ProgramV1ControlFlow`、`ProgramV1MatchStatements`、`ProgramV1MatchExpressions`、`ProgramV1ConstructorPatterns`、`ProgramV1ExpressionForms`、`ProgramV1UnaryExpressions`、`ProgramV1ArithmeticExpressions`、`ProgramV1ShiftExpressions`、`ProgramV1EqualityExpressions`、`ProgramV1OrderingComparisons`、`ProgramV1BitwiseExpressions`、`ProgramV1LogicalExpressions`、`ProgramV1CoreStatements`、`ProgramV1FieldPlaces`、`ProgramV1IndexedPlaces`、`ProgramV1PlaceSuffixes`、`ProgramV1RevertEmitStatements` 与 `ProgramV1StringLiterals` 是聚焦 source→ProgramV1 suites；`Tests/Language/*Statements.lean`、operator suites（含 CheckedSub/CheckedMul/CheckedDiv/CheckedMod/CheckedNeg/ShiftLeft/ShiftRight/BitwiseNot/LogicalNot/Grouping）与 legacy expression suites 仍固定旧 Source AST | **旧轨实现 + 新轨子集** | formal 仍 pending；继续迁移剩余 statement/expression/place forms、未迁移 pattern forms、旧 statement/operator suites 与 legacy decoder/grammar reader；未达到删除门槛，不删除旧代码 |
| `TASK-D1-05` | pending | accepted ADR-0019要求 `proof-forge.program-export.v2` + canonical ProgramV1 root bytes | `ProgramExport.lean` 只登记 `proof-forge.program-export.v2`，attribute 要求 `ProgramExportPayloadV2`，`programPayloadV2` 先 bounded-exhaustively 解码 scalar/ByteArray 再**仅**调用 `decodeCanonicalSourceAstBytesV1`，并验证 `programIdentity == moduleName ++ declaration raw components`；v1 schema、非 `ProgramExportPayloadV2` 类型、malformed 表达式均以 PF-EXPORT-001/004 拒绝；`ProgramPayload.lean` 已删除，无 v1 row/type/reader | `ProgramExports.lean`、`ProgramExportAcceptance.lean`、`ProgramExportAcceptanceEmpty.lean` 已迁移到 v2 schema/export table/empty registry/payload reconstruction/identity join 覆盖；v1 schema 在 `normalizeProgramExports` 中明确拒绝 | **新轨子集**（v2 export/registration/reconstruction 已是唯一路径） | `ProgramPayload.lean` 已删除；旧 v1 payload 消费者归零；legacy Loader/Source decoder 已在 D1-06 删除 |
| `TASK-D1-06` | pending | multi-program loader、raw component identity、exact `--program` selection | `parseProgramsV1`/`selectProgramV1`已成为唯一 source-reading 入口并被CLI调用；`Syntax.lean` legacy Source decoder 家族（`decodeType`/`Param`/`Expr`/`Statement`/`Item`/`Program`、`decodeProgramCommandChecked` 与所有 private unchecked helpers）与 `Loader.lean` legacy APIs（`parsePrograms`/`selectProgram` session/top-level variants 与 `processCommands`/`selectParsedProgram`）已全部删除；CLI/`ParserSession`/`selectProgramV1` 仅返回 `ValidatedSourceV1`；`renderSourceQualified` 仍用于 duplicate-error 文本但不参与 identity | `Tests/Language/Loader.lean` 已迁移到 V1 并覆盖 source-size/run_cmd 拒绝/deep-import 拒绝/duplicate-init/multiple-program selection/namespace-bound；`Tests/Language/ProgramCommandAcceptance.lean` 使用 `parseProgramsV1` 验证 v2 payload/identity/kind/run_cmd 拒绝；`Tests/Language/ProgramSyntax.lean` 移除 per-subdecoder bound 调用，改用 Counter V1 source-text 验证 identity/hash/semantic requirements；`Tests/Language/StateVisibility.lean` 使用 `selectProgramV1` 与 `compileValidatedSourceV1`；`Tests/Language/AggregateDeclarations.lean` 已迁移到 V1 并注册；`Tests/Language/SourceBoundsAcceptance.lean` 已删除，其 PF-BOUND-001 覆盖由 `ProgramSyntax`/`Loader` 与 ProgramV1 suites 承载 | **新轨子集**（legacy source-reading surface 已删除，`parseProgramsV1`/`selectProgramV1` 是唯一入口） | 删除 `Tests/Language/SourceBoundsAcceptance.lean`；`Core/Source.lean`、alpha Typed/Semantic core 与 `Tests/Fixtures/SourcePrograms.lean` 保留到 D2 consumer 迁移；下一步 D2 Typed/Semantic/Resolver contract 迁移 |
| `TASK-D1-07` | pending | stable DiagnosticV1 code/schema/order/redaction和source origin | `ProofForgeV2/Core/DiagnosticV1.lean` 提供 closed `DiagnosticCodeV1` enum、`DiagnosticV1` 载体、`toCanonicalJson`/`renderHuman`、`compare`/`sortAndDedupe`；`ProofForgeV2/Language/Loader.lean` 已新增 additive `ParserSession.parseProgramsV1WithDiagnostics`/`selectProgramV1WithDiagnostics`，parser-boundary 与 duplicate-program 错误映射为 `DiagnosticV1` 并携带 `SourceOrigin`（`nodeId` 为 documented 16-zero-byte sentinel），decoder-internal 错误 origins 为空；现有 `selectProgramV1`/`parseProgramsV1` 与 CLI 输出逐字节不变 | `Tests/Core/DiagnosticV1.lean` 固定 wire 唯一性、human golden、canonical JSON 精确性、origin JCS round-trip、排序/`sortAndDedupe`、absolute path redaction；`Tests/Language/ProgramV1Diagnostics.lean` 固定 parser-boundary/duplicate-program/decoder-internal 映射、human 与 legacy 字节一致、JSON 确定性与 redaction、multi-error `sortAndDedupe` | **新轨子集**（additive diagnostic API 已接线，legacy API 字节不变） | 实现typed diagnostic bundle、稳定priority/origin/redaction和human/JSON renderer；迁移测试后移除String error seam |
| `TASK-D1-08` | pending | no-follow safe-open、contained frontend worker、ResourceProfileV1 supervisor/receipt | `Core/Common.lean`已有ResourceProfileV1和lower-only validation；CLI只有relative path/`realPath` containment与进程内 `readFile`/parser，无worker、timeout/OOM containment或receipt | Source bounds和negative shell tests覆盖部分size/path；不能关闭worker/process escape面 | **可复用地基 + 缺失** | 新建frontend worker protocol/safe-open/supervisor；CLI切换后移除不受控进程内source open入口 |

## D2：Type/effect/semantic core

| Task | Formal | 新设计要求 | 实际代码与产品接线 | 现有测试事实 | Engineering | 迁移与旧代码处理 |
|---|---|---|---|---|---|---|
| `TASK-D2-01` | pending | 完整name/type checker、pure-fn call graph、invariant/proof-reference source binding | 新增 `ProofForgeV2/Typed/ModelV1.lean` 与 `NameResolutionV1.lean` 直接消费 `ValidatedSourceV1`，构建 13 类声明 source-order 序数表与显式作用域名解析；新增 `ProofForgeV2/Typed/TypeCheckV1.lean` 在 name resolution 之后对 literal/place（含 struct field chain 与 Array/Map index）/unary/binary/constructor/localCall 做精确 `TypeV1` 推导与宽度边界检查，并对 statement/body 完成精确类型检查：let（annotation 优先、无 annotation 时按 value 推断）、assign（target type 与 value 一致）、return（value 匹配 result / Unit 上下文拒绝带值 return）、assert（condition 为 Bool）、revert/emit（按声明 param count/type 检查参数）、if（condition Bool，分支 block 在同 scope 与 result 下检查，binder 不外泄）、for（端点为同宽整数，iterator 进入 body scope 且不外泄）、call/schedule（参数按无 expected type 的 expression 检查）、match（pattern 对 scrutinee 精确检查 wildcard/bind/Bool/integer-width/constructor 子模式，string pattern fail closed；expression match 全 arm 结果统一；statement match arm body 在 result context 检查；enum exhaustiveness 要求 `_`/bind 或 declaration-order 全 variant 覆盖；非 enum scrutinee 要求 catch-all；诊断按 source order）；init/entry/view/fn/const/invariant bodies 已按声明 scope/result 接线；整数字面量必须携带预期整数类型并按宽度精确范围检查、无默认宽度；新增 `ProofForgeV2/Typed/CallGraphV1.lean` 直接消费 `NameResolutionV1` 的 scope-shadowing 规则，从 `fn`/`entry`/`view`/`init` body 收集 `.localCall` 边（locals/params 遮蔽顶层 fn 不产生边，非 fn callee 不产生边），构建 caller→callee 有向图，以 Tarjan SCC 检测全部 cycle，self-recursion 作为 1-cycle 报告，并按 cycle 最早成员 declaration ordinal 升序输出确定性诊断；`entry`/`view`/`init` 只作为 call site 被遍历，不作为 call target，因此不可能出现在 cycle 上；`checkProgramStructureV1` 单入口串联 tables→resolution→acyclicity，三个阶段始终执行，诊断按 phase order 拼接，一次通过报告全部结构错误；**共享独立多遍入口** `ProofForgeV2/Typed/CheckV1.lean` 单次 resolve 后拼接 structure→type→effect→bound→disclosure；**已作为 fail-closed 门禁接线进** alpha `Typed.checkV1`（`compileValidatedSourceV1`/CLI `build` 经该边界继承），alpha shape 校验与 Typed IR lowering 仍保留（formal pending） | 新增 `Tests/Typed/NameResolutionV1.lean` 覆盖作用域解析与错误类别诊断；新增 `Tests/Typed/TypeCheckExpressionsV1.lean` 覆盖运算符结果类型、struct field chain 与 Array/Map index、整数字面量边界、mixed-type 二元拒绝与 expected-vs-actual source order、一元类型门、bool/整数 分离；新增 `Tests/Typed/TypeCheckCallsV1.lean` 覆盖 struct/enum constructor 与 local fn call 的 exact/nested/wrong-count/wrong-type/wrong-category、source-order 诊断优先级；新增 `Tests/Typed/TypeCheckStatementsV1.lean` 覆盖 statement/body 正向类型检查、let annotation mismatch、assign target/value mismatch、return/result mismatch、value-return-in-Unit 拒绝、revert/emit unknown/wrong-count/wrong-arg-type、assert non-Bool、if non-Bool condition、for 端点宽度 mismatch 与 iterator leak、branch binder non-leak、call/schedule 参数检查、invariant non-Bool 以及 source-order 错误优先级；新增 `Tests/Typed/TypeCheckMatchV1.lean` 覆盖 wildcard/bind/literal pattern、struct/enum constructor pattern（enum 通过 source，struct 通过 AST-direct construction，因 parser 目前要求 constructor pattern 为 2+ component qualified id）、string pattern 拒绝、integer width boundary、constructor on non-named、arm 结果类型 mismatch、statement match result context、enum exhaustiveness 正向/负向、missing-variant declaration-order 报告、非 enum catch-all 要求、unreachable arm 仍类型检查、source-order 优先级；新增 `Tests/Typed/CallGraphV1.lean` 覆盖 acyclic fn chain、direct self-recursion、two-fn/three-fn cycles、entry/view/init→fn 不形成 cycle、shadowed callee（param/local）不产生边、unreachable fn cycle 仍被检查、cycle 与 resolution error 同时按 phase order 报告；新增 `Tests/Typed/CheckV1.lean` 固定共享多遍入口 Counter 正向、type/effect/bound/disclosure 错误、structure→type→effect→bound→disclosure 相位序、resolution 不重复、duplicate-fn incomplete | **新轨子集** | 继续扩展完整 type/effect checker；迁移全部 declaration 和 fn/proof binding |
| `TASK-D2-02` | pending | effect/call/view checker及pure-fn transitive effects | 新增独立 `ProofForgeV2/Typed/EffectCheckV1.lean`：从 ProgramV1 AST 收集当前可表达 direct effects（`state.read`/`state.write`/`event.emit`/`external.call.sync`/`workflow.schedule`/`failure.revert`），复用 ModelV1/NameResolutionV1/CallGraphV1 scope-shadowing 与 localCall 图，以 6-bit EffectSet 有限单调 fixed-point 做 pure-fn transitive closure（环上亦得完整上界，不静默降级）；duplicate fn key 时 analysisComplete=false/ok=false 并 fail closed 暴露 name-resolution 结构诊断；`fn` 仅允许 `failure.revert`，`view` 仅允许 `state.read`/`failure.revert`，`entry`/`init` 允许当前可表达 effects；稳定 `DiagnosticCodeV1.effectDisallowed` → wire `PF-EFFECT-001`，诊断按 declaration order + effect-kind order；**已由产品 `Typed.checkV1` 经 `CheckV1` 门禁 fail-closed 消费**（effect 相位；PF-EFFECT-001 → `CompileError.effectDisallowed`）；**TypedV1 alpha view write/synchronous-call 重复防御已退役**（`checkStatementV1` 不再按 `mode == .view` 拒绝 write/call；产品 sole path 为 EffectCheckV1；`Entry.mode` 仍从 entry/view 设置供 target 使用；alpha residual 仅 missing/after-return shape）；legacy `Core/Typed.lean`/`Typed.check` 内嵌 view 规则保留至 D2 consumer 删除；不做 context/disclosure/extension/bounds 扩展；formal pending | 新增 `Tests/Typed/EffectCheckV1.lean` 覆盖 pure fn/assert/revert 正向、fn state read/write/emit/call/schedule 负向、view read 正向与 write/emit/call/schedule 负向、entry/init 全 effect 正向、fn/view transitive absorption、param/local shadow 不误报 state.read、shadowed callee 不传播、三层 chain 与 human `PF-EFFECT-001` wire；`Tests/Core/DiagnosticV1` 已登记 `PF-EFFECT-001` rank/wire；`CheckV1ProductGate`/`ValidatedSourceV1Pipeline` 固定产品路径 view write/call 仅 `PF-EFFECT-001`/`effectDisallowed`（非 alpha `cannot write state` / `cannot perform synchronous call`），经 `Typed.checkV1` 与 `compileValidatedSourceV1` 双路径 | **新轨子集**（独立 effect pass + 产品 sole EffectCheckV1 权威；TypedV1 alpha view effect residual 已删；legacy Typed.check residual 仍在；formal pending） | 继续补齐 context/disclosure/extension；legacy `Typed.check` view 规则随 D2 consumer 删除；不伪称 formal D2-02 完成 |
| `TASK-D2-03` | pending | loop/call/recursion bound与termination | 新增独立 `ProofForgeV2/Typed/BoundCheckV1.lean`：复用 CallGraphV1 `buildFnCallEdges`/`Tarjan`/`cyclicSccs` 在 fn 序数上拒绝 pure-fn local-call 递归/环，稳定 `DiagnosticCodeV1.resourceBound` → wire `PF-BOUND-001`（message `unbounded recursion (call cycle): …`，成员 declaration-ordinal 升序；**不改动** CallGraph 既有 `.sourceInvalid` cycle suite）；对 init/entry/view/fn body 源序遍历嵌套 `for ... bounded N`，以 checked `UInt32` 乘积维护 nest product（overflow ⇒ `PF-BOUND-001`；0 界有限；不做 range/`end-start≤N` 证明）；duplicate fn key 时 analysisComplete=false fail closed 并暴露 name-resolution structural 诊断；phase order：cycle 后 loop-product；**已由产品 `Typed.checkV1` 经 `CheckV1` 门禁 fail-closed 消费**（bound 相位；PF-BOUND-001 → `CompileError.resourceBound`）；不做 effect-occurrence Semantic IR、ResourceProfile limits、authority；formal pending | 新增 `Tests/Typed/BoundCheckV1.lean` 覆盖 acyclic chain+entry、self/2-/3-fn cycles 的 `PF-BOUND-001` 与成员序、entry/view/init 非 cycle 成员、single `bounded 0/10/4096` 与 double nest ok、三层 4096 乘积溢出、if/match arm 内 nest 乘积、param/local shadow 不造边、`renderHuman` `PF-BOUND-001:`、duplicate-fn incomplete fail closed、cycle-then-loop phase order；CallGraph suite 保持既有 `PF-SRC-INVALID` cycle 期望 | **新轨子集**（独立 bound pass + 聚焦测试；formal pending；产品 CheckV1 门禁已接线） | 继续补齐 effect-occurrence Semantic 上界、ResourceProfile limits，并考虑将 CallGraph cycle 诊断迁到 `PF-BOUND-001` 单一权威；不把 parser syntax-bound/target resource guard 冒充业务 termination；不伪称 formal D2-03 完成 |
| `TASK-D2-04` | pending | disclosure/authority/custody checker | 新增独立 `ProofForgeV2/Typed/DisclosureCheckV1.lean`（**D2-04a 显式 flow + D2-04b PC-label / implicit disclosure 工程子集**）：VisibilityV1 lattice `public_ < commitment < private_`（join=max secrecy；mayFlow 当 secrecy(src)≤secrecy(sink)）；bottom-up 表达式可见性（literal public、param/state/local/const-as-public place、field 继承、index join+public sink、unary/binary/constructor、localCall 成功结果 public_、**match 表达式结果=join(scrutVis, arm values join)** 使 scrutinee 控制污染结果）；语句 walk 线程 **PC label**（entry PC=`public_`）：`if_` 条件不作为 public value sink 但 `pc'=join(pc,condVis)` 进入分支；`match_` 语句 arm body 在 `pc'=join(pc,scrutVis)` 下；`assert_` condition 为 public sink（assert⇒failure.revert）且**不**抬高后续 PC；`for_` 端点仍为 public sink、body 保持当前 PC、binder=`public_`；嵌套 if/match 单调 join PC、出分支恢复外层 PC（参数传递）；每个既有显式 sink 的有效源=**joinVisibility(valueVis, currentPc)** 再 mayFlow；显式 sinks：assign→target、return/emit/revert/external call/schedule args→public_、for 端点→public_、localCall arg→对应 fn `ParamV1.visibility`（`tables.fn` + NameResolution/TypeCheck shadowing；arity mismatch 仅 walk nested）、const 定义表达式→public_；let 记 RHS 可见性（value flow）、match binder 继承 scrutinee（value flow）；稳定 `DiagnosticCodeV1.visibilityViolation` → wire `PF-VIS-001`（message `disclosure violation: cannot flow '<src>' into '<sink>'`，src 为 joined label）；duplicate fn 时 analysisComplete=false fail closed 并暴露 name-resolution structural 诊断；**已由产品 `Typed.checkV1` 经 `CheckV1` 门禁 fail-closed 消费**（disclosure 最终相位；PF-VIS-001 → `CompileError.visibilityViolation`；structure→type→effect→bound→disclosure）；**不做** authority/custody、`commit` 算子；alpha Semantic visibility→requirement inference 与 alpha IR lowering 保留；formal pending | 新增 `Tests/Typed/DisclosureCheckV1.lean` 覆盖 public Counter 正向、private→private assign 正向、private→public assign/return/view-return/emit/revert/call/schedule/index 负向、localCall public-param 负向（含 discarded call）与 private→private param 正向、const private-state 与 const-return launder 负向、**match stmt/expr private scrutinee+public arms 负向（PC/result taint）** 与 binder leak 负向、public scrutinee 正向、**if private/commitment 条件 public return/assign/emit/revert/call/schedule 负向**、**private if 下 private→private 正向**、assert 不抬高后续 PC 正向、for 端点 private/commitment 负向与 public 正向、commitment lattice、shadowing、default=public、multi-error 源序、`renderHuman` `PF-VIS-001:`、duplicate-fn incomplete fail closed、private let 经 assign 泄漏；`Tests/Core/DiagnosticV1` 已登记 `PF-VIS-001` rank/wire；`Tests/Typed/CheckV1.lean` 固定 disclosure 相位、effect/bound 前于 PF-VIS-001 与 incomplete 不发明 VIS；`Tests/Compiler/CheckV1ProductGate.lean` 固定显式 private return 与 **implicit private-if return** 产品路径 PF-VIS-001；完整 formal `TST-VIS-002`（authority/custody 轴）仍未覆盖 | **新轨子集**（显式 + PC-label implicit disclosure flow pass + CheckV1 相位接线 + 聚焦测试；formal pending；产品 CheckV1 门禁已接线；authority/custody/`commit` 仍缺） | 继续补齐 authority/custody 分离与 formal TST-VIS-002 全覆盖，并评估 alpha Semantic visibility inference 删除；不伪称 formal D2-04 完成 |
| `TASK-D2-05` | pending | canonical ProgramRequirements inference、predicate merge与origin | 新增独立 `ProofForgeV2/Typed/RequirementsInferV1.lean`：直接消费 `ValidatedSourceV1`/`ProgramV1`，按 source-order first-seen `stableUnique` 产出 alpha `ProgramRequirement` 数组（与 `Semantic.deriveRequirements` 同 carrier）；规则覆盖 non-empty state⇒`.persistentState`、state visibility⇒`.privateState`/`.commitmentState`（从不借 privateWitness）、init/entry/view/fn param visibility⇒`.privateWitness`/`.commitmentDisclosure`、type carriers（bool/field bn254_fr；Option/Array/Map 递归）、arithmetic `+ - * / %` 与 unary `-`⇒`.checkedArithmetic`+`.transactionalRollback`、`Stmt.call`⇒`.synchronousCall`+rollback、`Stmt.schedule`⇒`.asynchronousWorkflow` only（alpha 无 schedule 规则；不发明 rollback）、`emit`⇒`.eventEmission`、`assert_`/`revert`⇒rollback；不发明 `.callerContext`/authority/custody/commit；pure AST walk 不依赖 call-graph，duplicate fn 仍 analysisComplete=true；**不接线** CheckV1/`Typed.checkV1`/compile/CLI，**不替换** alpha `deriveRequirements`；仍无 RequirementRef/predicate merge/origin provenance/SemVer/digest | 新增 `Tests/Typed/RequirementsInferV1.lean` 覆盖 Counter-like、private/commitment state、private param、emit/call/schedule、Bool、idempotent ids、与 `compileValidatedSourceV1` 在 Counter/StateVisibility/zero-arg call 子集上的 semantic.requirements 精确 parity、exact `ProgramRequirement.id` wire；semantic/target suites 仍固定 alpha enum 集合 | **新轨子集 + 功能型 alpha**（独立 ProgramV1 infer pass + tests；仍 alpha ProgramRequirement carrier；formal pending） | 继续改为 exact RequirementRef/Predicate canonical merge；业务值进 SemanticProgramV1，origin 按 index 进 provenance；接线后评估删除 alpha deriveRequirements |
| `TASK-D2-06` | pending | closed SemanticProgramV1 serializer/decoder、SemanticProvenanceV1、proof-bundle signature validation | 新增 owning `ProofForgeV2/Semantic/WireV1.lean`（**工程子集 skeleton + type-shape + valueBytes**）：closed model（IDs/Visibility/TypeShape/records/ops/terminators/entities/requirements/SemanticProgramDataV1/SemanticProvenanceV1/SemanticProgramV1 carrier）、schema/magic（`proof-forge.semantic-program.v1`/`pf.semantic.v1`、provenance companion）、nested tag tables 4.1–4.5、decode 资源上限；**tagged 共享 nesting fuel** `withTaggedNesting`/`maxNesting=256`（enter 当 nesting<256，leave 后恢复；非 tagged scalar 不耗 fuel；当前 model 无 recursive TypeShape-on-wire，fuel 仍走共享路径）；**API 契约**：`decodeSemanticProgramDataV1`=transport only；`encodeSemanticProgramDataV1` 输出前 structure gate；`decodeSemanticProgramV1`=transport→structure-gated re-encode→exact bytes（非法 structure 在 re-encode 路径以 structure error 或 `.nonCanonical` 失败，**不是** structure-free identity）；`validateSemanticProgramV1`/`semanticHashV1` 显式 structure+identity；structure 子集=table `id==index`、shallow TypeId/CallableId range、**SPEC §5 type-shape/FieldSpec/Map-key**（named iff struct|enum；nonempty struct/enum；widths {8,16,32,64,128,256}；Bytes/Array ≤4096；sole `bn254FrFieldSpecV1` id+exact modulusBE → `.badType`；intra-type field/variant name → `.duplicate`；Map key Bool|UInt|Int|Principal|Bytes|Struct-of-legal-keys fuel=`types.size` → `.badType`）、**SPEC §5 canonical valueBytes**（Constant/`Op.Literal`/`SwitchCase` 共享 type-driven validate；full-consume + encode(decode)==bytes → else `.nonCanonical`；Bool/UInt/Int/Principal/Unit/Bytes/Array/Map/Option/Field/Struct/Enum；Map keys unsigned-lex unique ascending；Field LE width `<` modulus；value nesting fuel=`maxNesting`；transport 仍 structure-free）、requirement key/domain + predicate name/rank/wire + `enumContains` 非空严格升序；**provenance envelope-only stub**：encode/decode envelope + `semanticProvenanceDigestV1`=SHA-256(envelope)（**非** source/NodeId join 接受），`validateSemanticProvenanceV1` 恒 `.badProvenance`；**不接线** Typed/CheckV1/compile/CLI；**alpha `Core/SemanticIR.lean` 未删除** | 新增 `Tests/Semantic/WireV1.lean`：schema/magic、empty/minimal Type.Bool round-trip、wrong magic/tag/fieldCount/trailing/mutated、hash identity、structure 负例（id≠index `.duplicate`、shallow bad TypeId/CallableId `.badReference`）、**type-shape 正/负（legal widths/lengths/named nonempty struct·enum/exact bn254 FieldSpec/Map Bool·UInt·Bytes·Struct-of-UInt keys；name=some on bool/name=none on struct/empty struct·enum/uint7/bytes·array 4097/bad FieldSpec id/zero modulus/dup field·variant/Map Option·Array·Unit·Enum key、Map Struct-of-Option/Field/Map key structure+encode）**、**valueBytes 正/负（Bool/UInt8·64/Int8/Unit/Bytes0·N/Option none·some/Field 0·p-1/Struct two UInt8/Enum empty·payload/Map empty·sorted/Array u8×2 + Op.Literal/SwitchCase；Bool 02/wrong-len/Option 02/Field≥p/Map unsorted·dup·keyLen/Enum OOB/trailing/truncated → `.nonCanonical` structure+encode；hand-built transport garbage valueBytes still ok）**、requirements domain/order/dup/digest-tie、predicate name/rank order、enumContains empty/unsorted/dup 与正向、nesting at maxNesting/max-1 与 nested FieldSpec fuel（zero-modulus Field shape encode 仍合法）、provenance envelope + digest envelope-only + validate 恒 `.badProvenance`、Digest raw-32、invalid carrier invariants=`#[]`、**CFG shape + reachability（`testCfgShapeAndReachability`：5 正/6 负，structure+encode 双路径）**、**block-param arg arity count portion（`testCfgBlockParamArity`：jump/branch/switch target `args.size == params.size` 4 正/6 负含 OOR 归 range check，helpers `cfgBlockWithParams`/`cfgJumpTargetWithArgs`/`cfgBoolParam`，block params typeId 0 Bool）**、**loopBounds back-edge coverage（`testCfgLoopBounds`：6 正/10 负，structure+encode 双路径，全 `.badCfg`；helper `cfgLoopBound`/`cfgCallable` 增 optional `loopBounds` 默认 `#[]`；self back-edge 0→0、两块 1→0、嵌套+forward tail、maxIter 0 合法、空 loopBounds 无 back-edge、双 back-edge 升序；missing/extra/wrong header·backEdgeFrom/maxIter 4097/dup pair/unsorted/header·backEdgeFrom OOR/单块 return 无 self edge 负例）**；**不是** formal TST-SEM-001 full corpus / cross-implementation / proof-bundle / CFG/TypeKey | **功能型 alpha + 新轨子集 skeleton**；formal TASK-D2-06 / proof-bundle join 仍 pending | 继续补 TypeKey anonymous ranking/interning、provenance source/NodeId join、ProgramV1→Semantic normalizer、product wire 与四 target 迁到 SemanticProgramV1 后删除 alpha IR；**CFG shape + reachability foundation 已加入 structure gate**（per-callable entryBlock==0、block id==index、terminator target range、从 entry 的 total reachability，全部 `.badCfg`；**block-param arg arity（count portion：jump/branch/switch target `args.size == target block params.size`，仅 in-range target，OOR 仍归 range check）已加入 step c.5**、**loopBounds back-edge coverage（exact coverage of every CFG back edge，`(header,backEdgeFrom)` 唯一升序、`maxIterations ≤ 4096`、range 归此处 `.badCfg`）已加入 step e）**、**ValueId SSA definition-table + exactly-once + use-existence 已加入 step f**（`collectValueDefSites`/`checkValueIdDefUniqueness`/`opValueUses`/`terminatorValueUses`/`checkValueIdUsesExist`/`validateCallableValueIdSsa`，全 `.badCfg`；`testCfgValueIdSsa` P1–P6 正/N1–N8 负）、**dominance-of-use 已加入 step g**（`cfgPredecessors`/`computeDominators` 迭代 dataflow dom[b]={b}∪(∩ reachable preds dom[p])、`checkDominanceOfUse`/`validateCallableDominanceOfUse`，每个 use 必须在 dominated-by-def-block，全 `.badCfg`；`testCfgDominanceOfUse` DP1–DP5 正/DN1–DN3 负，negative 仅 dominance 失败、reachability/arity/loopBounds/SSA def-table 全通过），全部 `.badCfg`；**def-site TypeId range 已加入 step h**（`collectValueTypeDefs`/`checkDefSiteTypeIdsInRange`：block params + instruction result ValueDef TypeIds 必须 in `[0, types.size)`，callable param/result TypeIds 已由 step 2 覆盖不重复，失败 `.badReference`）、**terminator typing 已加入 step i**（`boolTypeId`/`checkTerminatorTyping`：branch cond==Bool TypeId、switch case.typeId==typeOf(scrut)、jump/branch/switch target args positional type==target block params[i].typeId（min-length guard total，arity 归 step c.5）、return (some v) type==callable.result.typeId；return none/revert/trap 不检查；全 `.badCfg`；`testCfgBlockParamTypeAndTerminatorTyping` P1–P5 正/N1–N8 负，N1·N2 `.badReference`、N3–N8 `.badCfg`，structure+encode 双路径），**per-op §5.1 type/result contract (step j) 已加入 value-producing ops（literal/constant/stateLoad/construct/fieldGet/indexGet/unary/binary/pureCall）：`checkOpTyping` 复用 step h `defTypes`，`uint32TypeId`/`uint8TypeId`/`optionTypeId` shape-resolving helper 与 `serializableType` predicate（eq/ne operand 合法性：bool/uint/int/principal/bytes/field/struct·enum-of-serializable，非 array/map/option/unit）；construct 解析 Struct/Enum/Array/Option/Unit/empty-Map shape（primitives/Bytes/Principal/Field/uint/int/bool reject）、indexGet 解析 Array/Bytes/Map（UInt32 index、UInt8/Option<value> result）、unary 解析 neg(Int|Field)/not(Bool)/bitNot(UInt|Int)、binary 解析 arithmetic(UInt/Int，Field 仅 add/sub/mul/div 非 mod)/eq-ne(serializable→Bool)/lt-le-gt-ge(UInt/Int→Bool)/and-or(Bool→Bool)/bitAnd-bitOr-bitXor(UInt/Int)/shl-shr(lhs UInt/Int, rhs UInt32)、pureCall 检查 callee.kind==.pureFn 与 arg count/type、result==callee.result.typeId；**void-op result-presence (step j extension) 已加入：StateStore/Assert/Emit/ExternalCall/Schedule 的 `Instruction.result` 必须为 `none`，spurious `result := some _` → `.badCfg`（不查 event/error/effectId range 或 argument typing；`testCfgVoidOpResultPresence` P1–P5 正/N1–N5 负 structure+encode 双路径）**；**value-producing result-presence (step j extension) 已加入：每个 value-producing op（Literal/Constant/StateLoad/Construct/FieldGet/IndexGet/Unary/Binary/PureCall/FieldSet/VariantTag/VariantPayload/IndexSet/CheckedCast/ContextRead/Commit）必须 `Instruction.result := some _`，缺失 → `.badCfg`；typed 9 family 继续要求 result.typeId == op exact result type，7 deferred family 仅 presence-only、exact input/result typing 仍跳过（`testCfgValueOpResultPresence` P1–P16 正/N1–N16 负 structure+encode 双路径 + 5 个 void result-none 回归）**；`testCfgOpTyping` P1–P9 正/N1–N12 负 structure+encode 双路径，全 `.badCfg`**；**Op.FieldSet exact §5.1 contract (step j extension) 已加入：base ValueId type 必须解析为 Struct、fieldIndex 必须在 range、type(value) 必须精确等于 fields[fieldIndex].typeId、result 必须存在且 result.typeId 必须精确等于 type(base)，全 `.badCfg`；FieldSet 从 7 deferred family 中独立，余 6 family（VariantTag/VariantPayload/IndexSet/CheckedCast/ContextRead/Commit）仍 presence-only；`testCfgFieldSetTyping` P1（first field）/P2（later field）正与 N1（non-Struct base）/N2（OOR fieldIndex）/N3（wrong value type）/N4（wrong result type）负 structure+encode 双路径，各 negative 隔离 FieldSet typing；`PresP10FieldSet`/`PresN10FieldSet` 已改为合法 Struct base 隔离 presence**，全部 `.badCfg`/`.badReference`；**Op.VariantTag exact §5.1 contract (step j extension) 已加入：base ValueId type 必须解析为 Enum 或 Option、`uint32TypeId` 必须解析到唯一 UInt32 TypeId、result 必须存在且 result.typeId 必须精确等于该 UInt32 TypeId，全 `.badCfg`；VariantTag 从 6 deferred family 中独立，余 5 family（VariantPayload/IndexSet/CheckedCast/ContextRead/Commit）仍 presence-only；`uint32TypeId` 改为 uniqueness-gated（仅当恰好一个 `.uint 32` 声明时返回 `some`，重复匿名 UInt32 声明返回 `none` 被 VariantTag 拒绝，作为 TypeKey 结构化 interning 仍 pending 下的保守 fail-closed 替代）；`testCfgVariantTagTyping` P1（Enum base）/P2（Option-some base）/P3（Option-none base）正与 N1（primitive base）/N2（no UInt32 closure type）/N3（wrong result type）/N4（duplicate UInt32 closure type）负 structure+encode 双路径，各 negative 隔离 VariantTag typing；`PresP11VariantTag`/`PresN11VariantTag` 已改为合法 Enum base 隔离 presence**；**Op.VariantPayload static exact §5.1 contract (step j extension) 已加入：Enum base 要求 variantIndex/payloadIndex in range 且 result.typeId == selected payload TypeId，Option base 只允许 `(1,0)` 且 result.typeId == element TypeId；runtime Enum tag agreement 本切片不检查，D2-07 interpreter 尚未实现/接线且届时必须 trap；`testCfgVariantPayloadTyping` P1（Enum payload）/P2（Option-some）正与 N1（primitive base）/N2（variant OOR）/N3（payload OOR）/N4（Option-none）/N5（Option payload OOR）/N6（wrong result）/N7（empty variant payload）负 structure+encode 双路径；`PresP12VariantPayload`/`PresN12VariantPayload` 已改为合法 Option-some base 隔离 presence**；**Op.IndexSet exact static §5.1 contract 已加入：Array/Bytes index 必须为 UInt32、Array value=element、Bytes value=UInt8、Map key/value exact，result.typeId 必须等于 type(base)；runtime Array/Bytes bounds 留 D2-07 且尚未实现；`testCfgIndexSetTyping` P1 Array/P2 Bytes/P3 Map 正与 N1 primitive base/N2 Array index/N3 Array value/N4 Bytes index/N5 Bytes value/N6 Map key/N7 Map value/N8 result type/N9 duplicate UInt8 closure 负 structure+encode 双路径；`uint8TypeId` 改为 uniqueness-gated（重复 `.uint 8` fail closed，同时收紧 Bytes IndexGet/IndexSet）；`PresP13IndexSet`/`PresN13IndexSet` 改为合法 Map base 隔离 presence**；**Op.CheckedCast exact static §5.1 contract 已加入：source 与 `toType` 均必须解析为 UInt/Int，result.typeId 必须精确等于 `toType`；运行值 representability 留给尚未实现的 D2-07 checked-revert；`testCfgCheckedCastTyping` P1 UInt→UInt/P2 UInt→Int/P3 Int→UInt/P4 Int→Int 正与 N1 Bool source/N2 Bytes source/N3 Bool destination/N4 Bytes destination/N5 wrong result/N6 missing destination 负 structure+encode 双路径**；**Op.StateStore exact static §5.1 contract 已加入：stateId 必须存在、type(value) 必须精确等于 selected state.typeId、result 必须为 none；`testCfgStateStoreTyping` P1 UInt8/P2 Bool/P3 second-state selection 正与 N1 missing state/N2 wrong value/N3 wrong selected-state type 负 structure+encode 双路径，既有 void presence fixture 同步改为类型合法**；**Op.Assert exact static §5.1/§6 contract 已加入：condition 必须 Bool，`errorId=none` 要求 args empty，`some errorId` 必须存在且 args 按 ErrorDecl fields positional exact，result 必须为 none；`testCfgAssertTyping` P1 standard/P2 declared two-arg/P3 second-error selection 正与 N1 condition/N2 none+args/N3 missing error/N4 arity/N5 arg type/N6 selected error type 负 structure+encode 双路径**；**Term.Revert exact §6 ErrorDecl join 已加入：errorId 必须存在，args 按 selected ErrorDecl fields positional exact；`checkTerminatorTyping` 显式消费 errors table；`testCfgRevertTyping` P1 empty/P2 two-arg/P3 second-error selection 正与 N1 missing error/N2 arity/N3 arg type/N4 selected error type/N5 reversed two-arg positional order 负 structure+encode 双路径**；**Op.Emit exact §5.1 EventDecl join 已加入：eventId 必须存在，args 按 selected EventDecl fields positional exact，result 必须 none；`testCfgEmitTyping` P1 empty/P2 two-arg/P3 second-event selection 正与 N1 missing event/N2 arity/N3 arg type/N4 selected event type/N5 reversed two-arg positional order 负 structure+encode 双路径；既有 void presence Emit fixtures 改为合法 empty EventDecl**；**§6 per-callable EffectId canonical assignment 已加入：Emit/ExternalCall/Schedule 按 BlockId/instruction order 从 0 连续编号、每 callable 重置，gap/duplicate/wrong start/reorder 均 `.badCfg`；`testCfgEffectIdOrder` P1 三 effect family/P2 跨 block/P3 per-callable reset 正与 N1 starts-at-one/N2 duplicate/N3 gap/N4 block-order restart 负 structure+encode 双路径**；**ExternalCall/Schedule callee qualified-shape gate 已加入：callee 必须至少两个 components，`testCfgExternalCalleeShape` P1/P2 双 component、P3 三 component 正与 N1/N2 单 component 负 structure+encode 双路径；argument serializability 因允许 TypeShape 集合仍有规范歧义而 deferred，不复用 Eq/Ne 专用 `serializableType`**；**program root qualifiedName shape gate 已加入：SemanticProgram root 必须至少两个 components，复用 helper 在 encoder table-size 前与 structure step 0 检查，使单 component 对 table-size/table-id 等 mixed-invalid 保持 `.badScalar` 优先；transport decoder 保持 transport-only，carrier re-encode fail closed；`testProgramQualifiedNameShape` 固定双/三 component 正向、单 component structure/encode/carrier 负向及 table-id/table-size precedence**；**Term.Switch canonical case-shape gate 已加入 CFG step b.5：cases 必须 nonempty，零 case（有/无 default）均 `.badCfg`，必须由 normalizer 物化为 Jump；`testCfgSwitchCasesNonempty` 固定单 case 有/无 default 正向与零 case 负向，SSA undefined-switch fixture 已迁为 nonempty case 隔离 use-existence**；**Term.Switch typed case-value uniqueness 已加入 CFG step b.6：每个 Switch 内 `(typeId,valueBytes)` 必须唯一，target/args 不参与 key；实现以 typed raw key private qsort + adjacent compare 避免 quadratic scan且不改 public case order；`testCfgSwitchCaseValueUniqueness` 固定 Bool/UInt8 distinct、跨两个 reachable Switch 同 key 重用正向，以及同/异 target、异 target args duplicate 负向与 malformed duplicate `.nonCanonical` phase precedence**；**callable kind/name presence 已加入 signature step 4.25：initializer 仅 `name=none`，entry/view/pureFn/invariant 仅 `name=some`，在 canonical values 后、CFG 前失败 `.badCfg`；`testCallableKindNamePresence` 固定五 kind 正向、逐 kind wrong Option shape 负向及 canonical-value-before-signature/signature-before-CFG mixed-invalid phase precedence；name grammar/NFC/uniqueness、initializer result 与 invariant join 仍 deferred**；**initializer cardinality 已加入 signature step 4.3：source-order 允许零或一个 initializer，第二个 initializer `.badCfg` 且先于 CFG；`testInitializerCardinality` 固定 0/1、initializer+entry 正向、double initializer 与 later-CFG mixed-invalid 负向；Unit/public result仍 deferred**；TypeKey/provenance join/normalizer/product wire、ExternalCall/Schedule argument serializability、2 deferred family（ContextRead/Commit）的 exact contracts 仍 'Not yet'）；不在本切片接线 Typed/compile/CLI；不伪称 formal D2-06 完成 |
| `TASK-D2-07` | pending | reference step interpreter：Invocation/context/responses/Outcome/effect/rollback | `Core/Semantics.lean`只解释UInt64 state/literal/add/store/return；同步call要求显式response但实际直接拒绝；无effect occurrence或规范Outcome | `Tests/Core/Semantics.lean`覆盖Counter式happy/overflow子集 | **功能型 alpha 子集** | 实现完整reference semantics和rollback differential oracle；新oracle替代后删除旧interpreter |

## D3：Registry、resolver、materializer与OutputSet

| Task | Formal | 新设计要求 | 实际代码与产品接线 | 现有测试事实 | Engineering | 迁移与旧代码处理 |
|---|---|---|---|---|---|---|
| `TASK-D3-01` | pending | TargetId、CodegenProfileId、NetworkProfileId三个独立typed parser | `Core/Diagnostic.lean`有TargetId enum/parse；descriptor和manifest中的codegen profile是裸String；无NetworkProfileId产品路径 | target parse/negative覆盖TargetId；无三ID exact parser suite | **可复用地基** | 引入三个独立identity/parser/renderer并迁移CLI/registry；删除裸String dispatch |
| `TASK-D3-02` | pending | static registry canonical digest、duplicate rejection、exact target/profile lookup | `Targets/Registry.lean`是hardcoded arrays/match；没有完整descriptor/profile payload或duplicate validation | `Tests/Materialization/Targets.lean`检查当前四target行为 | **功能型 alpha** | 建typed static registry和canonical digest；设计-only与implemented lookup按spec fail closed |
| `TASK-D3-03` | pending | ProgramRequirements → exact SupportClaim/claimDigest + BuildIdentity/ProfileSupportIndex | `Targets.resolve/checkSupport`只做 `supportedRequirements.contains`；production Lean中没有SupportClaim、BuildIdentity、ProfileSupportIndex | target negative固定unsupported enum；没有predicate/evidence binding矩阵 | **缺失** | 实现exact claim/predicate implication、aggregate diagnostics和binding resolution；`specified`空binding合法，更高grade无真实binding时零输出拒绝 |
| `TASK-D3-04` | pending | associated Plan/TargetIR protocol含schema、CodegenProfile、validation、EmitContext | `Materialization/Protocol.lean`已有associated `Plan`/`TargetIR`，但method是alpha `makePlan/lower/emit`，缺schema、validateTargetIR、IO EmitContext | target modules证明Plan type未擦除；没有正式boundary compile test | **可复用地基** | 升级接口并让四target直接实现；不得用Unit/string/JSON或兼容adapter保留旧API |
| `TASK-D3-05` | pending | OutputSetV1、support-decisions、manifest schema/digest、exact file closure与atomic publish | alpha OutputSet/OutputManifest和 `CLI/Emit.lean`具备路径检查、staging rename、no-clobber；schema为 `proof-forge-output/v2alpha1`，没有candidate/build/plan/IR/tool/support digest | `Tests/CLI/Emit.lean`与output shell negatives覆盖alpha原子性；artifact validator固定alpha manifest | **功能型 alpha** | 实现完整v1 manifest/decision-set/closure/rollback；迁移consumer后删除alpha manifest/evidence sidecar contract |
| `TASK-D3-06` | pending | `check/build/inspect/list-targets`、profile/evidence/resource flags、stable human/JSON | CLI只有list-targets、describe-target、build、build-counter；缺check/inspect/profile/minimum evidence/resource override/JSON | CLI emit与shell product-negative覆盖当前窄参数面 | **功能型 alpha 子集** | 实现规范command surface；删除ad-hoc `describe-target`/`build-counter`产品入口 |
| `TASK-D3-07` | pending | contained compiler-core/tool/output supervisor、effective profile digest/receipt和whole cleanup | Toolchain有exact locked execution/isolated env，Common有resource profile；compiler-core/output仍进程内，无统一receipt/whole-containment | `ToolchainPolicy`、toolchain negatives覆盖selected closure；不能关闭core/output process containment | **可复用地基 + 缺失** | 实现三stage supervisor/receipt/signal cleanup；保持与TaskQualification ceremony完全分离 |

## D4：EVM

| Task | Formal | 新设计要求 | 实际代码与产品接线 | 现有测试事实 | Engineering | 迁移与旧代码处理 |
|---|---|---|---|---|---|---|
| `TASK-D4-01` | pending | versioned EvmPlan schema、canonical invariants/hash和resolved identity binding | `Targets/Evm.lean`有target-owned Plan、storage/selector/ABI/resource/shape validation；无正式schema/canonical serializer/BuildIdentity binding | `Tests/Materialization/EvmSmoke.lean`有Plan mutation/shape测试 | **功能型 alpha，可大量复用** | 保留算法，增加schema/hash/decision binding；旧alpha Plan public contract随后删除 |
| `TASK-D4-02` | pending | validated SemanticProgramV1 → EvmPlan | `makePlan`从alpha `ResolvedProgram .evm`通用lower UInt64 fragment；Counter和非Counter Accumulator均成功，无fixture/name matcher | `CounterV1Evm`、`EvmSmoke`、Accumulator runtime证明算法非模板 | **功能型 alpha，可大量复用** | 直接迁到new ResolvedProgram/SemanticProgramV1，不经过adapter；保留unsupported fail-closed |
| `TASK-D4-03` | pending | EvmPlan → validated TargetIR/Yul + ABI | 有typed IR、dynamic Keccak selector、Yul/ABI renderer；`lower`先validate Plan，但没有TargetIR schema/hash/trace validation | EVM smoke、artifact validator固定selectors/ABI/Yul | **功能型 alpha，可大量复用** | 加TargetIR schema/validator/hash/trace，接D3 Materializer/EmitContext；移除alpha emit signature |
| `TASK-D4-04` | pending | locked solc bytecode packaging到OutputSetV1 | 真实source已通过digest-pinned solc 0.8.34生成bytecode；selected-tool closure已修复。solc仍由 `CLI/Emit.finalizeEvm` target-special-case，输出alpha manifest | `ToolchainPolicy`、target-smoke、solc-only root positive/full release-root negative通过 | **功能型 alpha** | 将ToolchainIdentity、bytecode validation和artifact roles移入EVM emitter/OutputSetV1；删除CLI inline finalize |
| `TASK-D4-05` | pending | Anvil Counter与reference interpreter differential | `scripts/smoke_evm.sh`已验证Counter和Accumulator init/mutate/read/UInt64 overflow rollback | 当前开发机真实Anvil smoke通过；不是新Semantic/Output contract上的formal TST closure | **功能型 alpha** | 新D2/D3/D4唯一路径完成后重跑reference-vs-Anvil；旧结果只作回归，过期hash/golden删除 |

## 共享迁移依赖

删除 D2/D3 alpha core 不只是 EVM 问题。当前以下 consumers 都直接 import旧 shared types：

| Consumer | 当前 maturity | 迁移要求 | 不得顺带声称 |
|---|---|---|---|
| EVM | runtime-validated alpha | 完整迁入 D4新contract | D4 formal done，除非全部TST/formal条件真实闭合 |
| Solana | plan-only | 机械改接新 Semantic/ResolvedProgram/Materializer/OutputSet，保留target-owned Plan/IR | sBPF ELF或runtime完成 |
| NEAR | Wasm-validated alpha | 机械改接新shared contract，保留NEAR Plan/Wasm算法 | sandbox receipt或D6完成 |
| Noir | source-only | 机械改接新shared contract，继续non-deployable | ACIR/witness/proof/VK/verify或D7完成 |

如果任一 consumer仍import旧类型，相关删除行保持 `blocked by consumer`，不得增加公开兼容层来伪造归零。

## 旧代码退役清单

| 层 | 旧实现候选 | 当前删除状态 | 删除门槛 |
|---|---|---|---|
| D1 frontend | `Syntax.lean` legacy decoder/quote/elab；Loader legacy APIs；`ProgramExport` v1；quoted `ProgramPayload`；string-call语法；legacy-only fixtures | **D1-06 source-reading surface 已删除**：`Syntax.lean` legacy Source decoder 家族与 `Loader.lean` legacy `parsePrograms`/`selectProgram` API 已移除，`parseProgramsV1`/`selectProgramV1` 成为唯一入口；其余项（DiagnosticV1、contained worker、span/NodeId inventory 到 compiler-core 的完整接线）仍 **未满足** | 完整V1 declaration/stmt parser、span/NodeId、v2 export、selection、DiagnosticV1、worker都成为唯一入口 |
| D2 core | `Core/Source.lean`、`Core/Typed.lean`、`Core/TypedV1.lean`、`Core/SemanticIR.lean`、`Core/Semantics.lean`、`Compiler.compile(Source.Program)` | **未满足** | 完整Typed/Semantic/provenance/interpreter完成，四target consumer全部直接迁移，imports归零 |
| D3 framework | alpha TargetDescriptor/contains resolver、旧 Materializer signature、v2alpha1 OutputSet、旧 CLI staging/finalize seams | **未满足** | typed registry/resolver/BuildIdentity/OutputSetV1/supervisor完成，四target直接实现新protocol |
| D4 EVM | alpha EVM descriptor/materialize入口、CLI inline solc finalize、alpha manifest assertions和过期 plan-hash goldens | **未满足** | 新EvmPlan/TargetIR/OutputSetV1上Counter+Accumulator deterministic build及Anvil differential全绿 |

## 每次删除的机械门禁

1. 新实现已经是 CLI、Lean command、Loader/compiler和相关target的唯一产品入口；没有flag、fallback或第二reader。
2. `ProofForgeV2/**`、`Tests/**`、Lake入口和当前scripts对旧symbol/type/schema引用为零；历史文档只能以historical文字保留。
3. 旧正例、负例、边界和diagnostic priority已迁到新carrier，不能靠删测试获得绿色。
4. 所有shared consumers已直接迁移；不保留长期adapter。
5. 聚焦测试、`just dev-check`、`just ci`、`just target-negative`、`git diff --check`通过；EVM删除再要求deterministic target-smoke和Anvil differential。
6. `ProofForgeV2/**`变化已刷新并核对 `supply-chain/lean-package-files.v1.json`。
7. 本矩阵更新replacement、remaining consumers、删除文件和实际验证；formal task状态只按其独立规则变化。

## 下一实施顺序

1. 完整迁移 ProgramV1 declaration decoder和 `TST-SRC-004`现有测试断言。
2. 继续迁移剩余 pattern/expression/place forms、旧 statement/operator suites 和 `TST-SRC-005`。
3. 接通span/NodeId inventory，原子切Lean command/export/Loader到ProgramV1 v2 schema。
4. 完成DiagnosticV1和contained frontend worker，随后删除D1 legacy frontend。
5. 按 D2 → D3 → D4迁移shared core和EVM；shared cutover时机械迁移另外三个target consumers。
