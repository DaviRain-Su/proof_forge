# D1–D4 设计要求与代码完成度矩阵

本文件是 **engineering fact snapshot**，不是 task completion ledger，也不是 formal evidence。

- Original audit baseline date: `2026-07-26`
- Original audit baseline commit: `8b42f7ebeb60652d1789e495f23247e5685b1e22`
- Engineering fact updates through: `2026-07-30`（后续增量事实见本矩阵与 `docs/06-implementation-log.md`；上述 commit 仅为原始审计基线）
- Formal task source: [`docs/04-task-breakdown.md`](docs/04-task-breakdown.md)
- Test requirement source: [`docs/05-test-spec.md`](docs/05-test-spec.md)
- Product migration decision: [`docs/adr/0019-single-programv1-source-authority.md`](docs/adr/0019-single-programv1-source-authority.md)

## 结论

D1–D4 共 27 个 formal task，当前仍为：

| Milestone | Formal 状态 | Engineering 结论 |
|---|---|---|
| D1 | `TASK-D1-01` blocked，D1-02..08 pending | ProgramV1 wire/hash/NodeId/OriginJoin 地基很强，expression/pattern integer literal 已共享 unsigned-decimal/lowercase-`0x`、UInt256-bounded decoder；B8b located Normalize→`compileProgramProductV1` multi-error `DiagnosticBundleV1`/`selectExitCode` 已接通；**B12 前端监督层已按 2026-08-01 产品决策移除**（SafeOpen/DarwinSupervisor/native C/worker exe/B12 gate 删除；CLI 产品源读取恢复为进程内 `Loader.selectProgramV1Product`；原 pinned-worker 监督切片整体 superseded）；command/export 已是 V1；B9/B9R FrontendProtocolV1 wire/bounds 已落地；**B10 standalone one-request worker、B11a native safe-open foundation、B11a2 pure Darwin receipt model、B11b1 worker supervisor 与 B11b2 shared source-open/worker supervisor 已完成工程切片**；B11b2 以 closed safe-open helper + hardened `posix_spawn` 两阶段监督建立 open→request→worker overall monotonic wall、aggregate peak join 与 live `sourceOpenFailed`；private supervised carrier保留 canonical SafeOpen Err 的 closed fault，使 `.tooLarge` 精确映射 16 MiB `PF-SRC-INVALID`且无需 reopen/stat；B12 已令 CLI sole consume该路径并把 source/resource/protocol事件映射到 closed DiagnosticV1，且 native只从 no-follow fd-derived bounded private snapshot执行 worker（suspended spawn + vnode recheck；不再 direct spawn caller path）；contained/host-race formal 资格随该层删除不再适用，formal 仍 pending |
| D2 | 7/7 pending | ValidatedSourceV1/ProgramV1 产品路径已跑 CheckV1 + NormalizeV1 structure-gated SemanticProgramV1（**仅 narrow public-UInt64 工程子集**，含 expected-type UInt64 integer literal、checked add/sub、UInt64 六比较与 bare assert、Bool entry/view 返回值（Normalize 已开 multi-width UInt/Int{8..256}+Field 算术+Principal identity；target Plan ABI 仍 UInt64/Int64-only）、**if/match forward-only 多块 CFG**（稠密 id、无 block param/back edge；双臂 return 无 join；UInt64/Bool 字面量臂 + 恰好一个 wildcard/bind catch-all；catch-all-only match 内联；constructor/string pattern、match expression、重复字面量仍 fail closed）、**public UInt64 event/error 表 + revert/emit**（revert 与 return 同构闭合；S2 catalog 五键含 `effect.event`；assert-else、call/schedule 仍 fail closed）、**fn/localCall（pureCall）**（fn 参数/结果限 public UInt64/Bool；fn body 禁 state read/write 与 event emit；ReferenceV1 显式调用帧栈；EVM Yul functions、Solana `.fn` plan 段、NEAR Wasm funcs、Noir call-site 内联+block-valued select）、**mul/div/mod + unary**（`*`/`/`/`%` 与 `~`/`!` 直接 lowering；一元 `-` desugar 为 `0 - x`，wire `Op.Unary.neg` 仍 Int/Field-only；EVM mul div 回环 overflow guard、Solana checked_mul/div/rem_u64、NEAR div_u 回环 guard、Noir 原生 checked u64 + 显式零除断言）、**不可变 let + bounded for**（`let` 为 SSA env 绑定、reassign fail closed；`for` 降为单 block-param header + latch 回边 + canonical loopBounds + 预计数 block-param id 区间；Reference `noteBackEdge` 第 N+1 次 body 后回边 revert；EVM Yul for+counter revert、Solana loop_u64+bound 段 0x1003、NEAR block/loop/br_if+body 后 unreachable、Noir 谓词嵌套展开+回边不可满足；Noir emit/fn 内循环 fail closed）、**shift/bitwise/logical 二元**（`<<`/`>>` lhs UInt64 + rhs UInt32（包络唯一 UInt32 上下文；字面量 ≥64 CheckV1 静态拒绝、计算 count 运行时 invalidShift；shl 越界 arithmeticOverflow）、`&`/`^`/`|` 同宽、`&&`/`||` 严格 Bool 无短路；EVM count+overflow 双 guard、Solana 五族+invalidShift 0x1004、NEAR count guard+round-trip、Noir 常数折叠 guard+2^k mul/div）、**call/schedule**（S2 catalog 七键含 `effect.synchronous-call`/`effect.asynchronous-workflow`；Normalize void op+共享 EffectId+verbatim qualified callee；**resolver 首个非均匀 capability 矩阵**：EVM/Solana 拒双键（无 address-bearing 类型）、NEAR 仅拒 sync、Noir 全持；NEAR schedule→原生 promise（account-id 门禁+u64-LE args+显式零 deposit/gas；fire-and-forget 与 Reference 无 response 语义一致）、Noir status/arg slot（reverted claim 不可满足）；Reference：call 按序消费 responses（returned/reverted/invalidExternalResponse）、schedule 无 response）；Semantic declaration/field/parameter/invariant name NFC/common identifier structure gate 已接线）；产品编译已只 mint private-ctor `CompiledSemanticV1`，canonical source/semantic Digest绑定 resolver与artifact identity，alpha Typed/Semantic只留显式测试兼容面，产品 umbrella/compiler/targets/materialization/CLI 递归 import closure已禁止 legacy Source/Typed/SemanticIR/Semantics与 AlphaCompatibility；EVM/Solana/NEAR/Noir 四个 `planFromCapability` 的 public-UInt64 add/sub + 比较/assert + Bool result + if/match region + emit/revert + fn/localCall + mul/div/mod/unary + let/for + shift/bitwise/logical + call/schedule Plan body均已直接消费 retained `SemanticProgramV1`（EVM Yul functions+mul overflow guard、Solana `.fn` plan 段+checked_mul/div/rem_u64、NEAR Wasm funcs+div_u 回环 guard、Noir 内联+事件槽/select ops+原生 checked u64；四 lane 统一 dense fn 表+前置签名索引+purity fail closed），各 target module 内 alpha Plan route归零；**D2-07 工程子集**已新增 `InvariantABI`（LogicalState/default/initial/conformance）与 admitted `ReferenceV1` primitive/effect 参考机（`admitReferenceProgramSliceV1` + `stepReferenceSliceV1`，直接消费 validated SemanticProgramV1，不经 alpha；**非** formal `step` / TST-SEM-002/003；guarded counter comparison/assert、if/match、emit/revert、fn/localCall、mul/div/mod/unary、let/for、shift/bitwise/logical 与 call/schedule（returned/reverted/missing/trailing response 与 fire-and-forget schedule）trace 已固定）；**D2-07 reference machine 本身无** product CLI/materializer 接线；`ProgramRequirementsV1` 已成为唯一产品 authority（target-neutral contribution engine → sole freeze），dual-carrier与 alpha产品consumer已删除；完整 Normalize/checker 表面、formal SupportClaim/OutputSetV1、完整 reference corpus 与 formal D2 **仍 pending（未完成）** |
| D3 | 7/7 pending | **D3/S4–Formal Spine V3 工程 registry kernel**：opaque TargetId/CodegenProfileId/NetworkProfileId + sole `TargetRegistryV1` 10-target membership/default/profile seed + closed six-axis enums + `resolveBuildSelectionV1` 已接线；旧 `StaticBuildSelectionIndexV1` 已删除；**工程** root codec/digest 已由 `Targets/RegistryRootV1` 交付（`pf.registry-root.engineering.v1` + length-framed root bytes → `engineeringRegistryRootDigestV1`；encode-only；**不是** formal `registryDigest`/JCS formal root）；`BuildIdentityV1` 仅 private five-field layout、无 mint；**D3/S5 工程**：private-ctor `CompiledSemanticV1` single-semantic carrier + engineering exact requirement resolver（`RequirementResolverV1` 四行静态 S2 support index + private `ResolvedEngineeringBuildV1` capability via `resolveEngineeringRequirementsV1`；`materialize`/`emit` 仅 capability；已删 `checkSupport`）；**D3/S6 工程**：capability-only Plan cutover（`EngineeringBuildV1` leaf sole mint；`DescriptorDataV1`；四 target 仅 capability-gated `planFromCapability`/`irFromCapability`/`buildFromCapability` + inspection；已删 public Common resolve/validateResolved/makePlan、`TargetDescriptor.supportedRequirements` 字段/membership acceptance、public Residual planFromAlpha/lowerPlan/filesFromIR 与 dead public ResolvedProgram；Registry 直接 capability dispatch；`s6-plan-cutover-deletion-gate` + residual type-chain reflection）；**D3/S7a 工程**：private-ctor `MaterializedArtifactsV1` sole mint；`materializeResult` 返回 pure base-file carrier；已删 public `OutputSet`/`OutputManifest`/`makeOutput`/`manifestJson`；`s7-output-envelope-deletion-gate`；**D3/S7b 工程**：`LockedToolchainV1` + private-ctor `FinalizedArtifactsV1` + Registry `finalizeMaterializedArtifactsV1` + 四 target FinalizeV1 adapters；已删 `CLI/Toolchain` 与 CLI finalize；CLI publisher-only v2alpha1；`s7b-finalize-authority-deletion-gate`；**D3/S7c 工程**：sole `validateEngineeringDiskClosureV1` + CLI evidence→manifest-last + unified Python no-follow exact closure；`s7c-disk-closure-gate`；formal pending；**M3b 工程** SupportClaim/BuildIdentity carriers 已接线（非 formal）；仍非 formal SupportClaim/formal BuildIdentity/formal registry root codec/OutputSetV1/ToolchainIdentity；四 target S1 Plan body均已直连 retained SemanticProgramV1；完整 Normalize/formal lowering仍未完成 |
| D4 | 5/5 pending | EVM Plan/Yul/ABI/solc/Anvil 算法已有真实功能；S1 `planFromCapability` 已直接消费 retained `SemanticProgramV1` 并归零 EVM 内 `alphaResidualOf`/`makePlanFromAlpha`，且 compiler/resolver/artifact identity已切到单语义 carrier；但 transitional v2alpha1 output、formal Plan/IR/Build/Output identity与 Reference↔Anvil closure仍缺，不能按 D4 完成 |

### D1 Linux frontend supervisor 增量（2026-07-30）

> **superseded（2026-08-01）**：本节全部工程切片已随前端监督层产品决策移除（`Frontend/SafeOpen*`/`DarwinSupervisor*`/native C/worker exe/B12 gate 删除；CLI 源读取为进程内 `Loader.selectProgramV1Product`）。仅保留为历史记录。

上述 D1 行中的 Darwin-only 描述由本增量补充：ordinary-development product path 现同时支持
Linux。Linux 使用 root-owned sticky 临时根内的 private worker snapshot、ambient-fd close、独立
process group、CLOEXEC launch-status pipe，以及 fail-closed `/proc` process-group/RSS sampling；
receipt 明确标记 `linux-development-observed` 与 `linuxProcRssAggregate`。Darwin 继续使用 suspended
spawn/vnode recheck。两条路径共享 absolute monotonic budget、closed event/receipt 与 cleanup 合同，
且真实 Linux CLI positive 已覆盖；这仍不构成 signed executable identity、cgroup/`setsid`
containment、formal host/race matrix 或 TASK-D1-08 完成。

因此当前产品不是 `active/` 中的旧 v1，也不是正式 D1–D4 新设计完成态；准确 engineering 路径是：

```text
ValidatedSourceV1 / ProgramV1
  → NormalizeV1 (CheckV1 ok∧analysisComplete + narrow public-UInt64 add/sub/mul/div/mod + unary + 六比较 + bare assert + Bool entry/view 返回值 + if/match 多块 CFG + event/error 表与 revert/emit + fn/localCall + 不可变 let + bounded for（回边 + loopBounds）+ shift/bitwise/logical 二元 + call/schedule lowering；Normalize 已开 multi-width/Field/Principal（target Plan ABI 仍 UInt64/Int64-only，Noir 另开 Field）)
  → structure-valid SemanticProgramV1 (retained)
  → CompiledSemanticV1 (private ctor; sole product compile success type;
     artifact name + canonical ProgramV1 source Digest + semanticHashV1 Digest)
  → frozen TargetRegistryV1 membership/default/profile resolve
  → resolveEngineeringRequirementsV1 → ResolvedEngineeringBuildV1 (private capability)
  → retained SemanticProgramV1 → target-private S1 lowering → EVM/Solana/NEAR/Noir Plan
  → target-owned IR/files
  → MaterializedArtifactsV1 (private ctor; capability-bound pure base files)
  → finalizeMaterializedArtifactsV1 → FinalizedArtifactsV1 (private ctor; deployable + extras + note)
  → validateEngineeringDiskClosureV1 (S7c exact disk closure; evidence→manifest-last)
  → CLI private legacy-engineering v2alpha1 on-disk publish (publisher-only)
```

保留边界（engineering fact，非 formal 完成）：

- sole Normalize 当前仍是 narrow public-UInt64 工程子集（multi-block init/entry/view/pureFn + literal/load/checked add-sub-mul-div-mod/unary（`~`/`!`；一元 `-` desugar 为 `0 - x`）/UInt64 六比较/bare assert/store/return/Bool entry-view result + if/match region + public UInt64 event/error 表与 revert/emit + fn/localCall + 不可变 let + bounded for（单 block-param header + latch 回边 + loopBounds）+ shift/bitwise/logical 二元（shift count 限 UInt32、逻辑严格无短路）+ call/schedule（args 限 UInt64、v1 无返回值、schedule 无 response）lowering；Bool 限 body 临时值与 entry/view 返回值；fn body 禁 state/effect；let 不可变、loop-carried local 仅归纳变量；其余 CheckV1-ok 形状继续在 Normalize 门禁 fail closed）
- formal D2 / D3 仍 pending
- 四 target 的 retained `SemanticProgramV1` Plan lowering/cutover及 checked-sub 首次扩面与**六比较+bare assert 扩面**（Bool 限 body 临时值、state/param/result 仍 UInt64-only；四 target 均经 worker lane + 主代理审计集成，聚合 `testGuardedCounterSemanticPlans` 固定 assert(ge)→checkedSub→return 跨 target 一致性）**已完成**，但完整 Normalize lowering、**formal** SupportClaim、**formal** registry root codec/`registryDigest`、**formal** BuildIdentity 与 OutputSetV1 **未完成**；工程 `TargetRegistryV1` sole membership/default/profile + selection cutover 与 private formal-layout `BuildIdentityV1` five-field shape **已完成**；**工程 registry root codec/digest（M3a：`RegistryRootV1`/`engineeringRegistryRootDigestV1`）已完成**；**工程 SupportClaim + BuildIdentity（M3b：`EngineeringSupportClaimV1`/`EngineeringBuildIdentityV1`，domain `pf.support-claim.engineering.v1` / `pf.build-identity.engineering.v1`；resolver 绑定 claim、materialize 绑定 identity；**仍非** formal claim/identity 与 on-disk OutputSet）**已完成**；工程 single-semantic carrier + exact requirement resolver capability cutover **已完成**；工程 S6 Plan cutover + S7a MaterializedArtifactsV1 + S7b FinalizedArtifactsV1 + **S7c exact disk closure/manifest-last** **已完成**（仍非 formal D3 / OutputSetV1 / ToolchainIdentity）

## 完成度口径

| 标签 | 可验证含义 |
|---|---|
| **可复用地基** | 新设计需要的 type、codec、primitive 或算法已存在，但没有完整产品接线 |
| **新轨子集** | 当前 CLI 确实使用 ProgramV1/new entry，但只覆盖规范子集 |
| **旧轨实现** | 行为主要存在于 `Source.Program`、quoted payload 或旧 schema 中 |
| **功能型 alpha** | 有真实端到端行为和测试，但 contract 不是正式 D1–D4 contract |
| **缺失** | 没有对应 production implementation；邻近 primitive 不计完成 |
| **替代就绪** | 新实现是唯一产品路径、已有 TST 迁移完成、旧引用归零并通过删除门禁 |

不使用百分比。formal 状态与 engineering 标签是两条独立轴；development test 成功不能把
`blocked`/`pending` 改成 `done`。

## D1：Source frontend

| Task | Formal | 新设计要求 | 实际代码与产品接线 | 现有测试事实 | Engineering | 迁移与旧代码处理 |
|---|---|---|---|---|---|---|
| `TASK-D1-01` | blocked | token/span、完整 ProgramV1 wire/hash、NodeId、同一 immutable source origin | `ProofForgeV2/Source/*V1.lean` 已有完整 ProgramV1 AST、root codec/decoder/validator、`ValidatedSourceV1`、`sourceHashV1`、57-tag traversal、NodeId assign/collision seam；`SpanV1.lean` 可验证原始 parser span。新增 `ProofForgeV2/Source/SpanJoinV1.lean` 已建立 immutable parser snapshot → canonical node preorder 的 exact span join（透明 syntax 结构跳过、共享 token 共享 span、count/tag/path 不一致 fail closed）；Type 子树仅 zip sole decoder `decodeTypeV1WithAnchors` anchors 与 `NodeTraversalV1.canonicalTypeVisitsV1`（Map key-before-value），无独立 type 分类/递归 walk；Loader 提供 additive `selectProgramV1WithSpans`（identity/canonical bytes/sourceHash 不变）；**B5** `ProofForgeV2/Source/OriginJoinV1.lean` 为 sole Source-owned exact NodeId×span join（opaque `OriginInventoryV1`、length-framed path keys、NodeId-ordered origins、path lookup；Semantic `buildSourceNodeInventoryV1` thin projection only；wire `SourceNodeInventoryV1` 仍在 Semantic.WireV1）；Normalize source-bound rebuild 权威未迁；**B7a** Loader additive `selectProgramV1WithOrigins`（WithSpans→parse path→`joinOriginsV1`）、`NodeTraversalV1.childPathV1`/`directChildPathV1`/`indexChildPathV1` sole path helpers、Source `DiagnosticLocateV1` path-draft materializer 已接线；Typed producer 真实 path 归因（B7b engineering，含 B7b3d CheckV1 composition）已完成；compiler-core/CLI product cutover（**B8b**）已完成（B8a foundation + product Loader/Normalize/Compiler/CLI bundle chain；formal 仍 pending） | `SourceAst*` wire suites、full-tag/field-count/marker/unknown-tag goldens及 independent Python oracle覆盖大量 wire事实；`ProgramV1SpanJoin` 固定全 constructor 家族的 path/count 精确对应、命令 span 包含、preorder 单调、注释/布局变体 delta shift、same-length rename 的 identity/position 分离、synthetic/count-mismatch fail closed、recursive Array Map/Option Map type-node span join、Map key-before-value span 序与 same-node-count Array↔Option/Map key-value swap tamper 拒绝；**B5** `ProgramV1OriginJoin` 固定 Source join ownership/sourceHash/NodeId 序/path lookup/length-framed non-collision/determinism 与 fail-closed invalid path·dup·missing·extra·invalid origin；CheckV1 provenance 机械 retarget Source parity；**B7a** `ProgramV1DiagnosticLocate` 固定 WithOrigins sourceHash/lookup parity、root/item primary+related NodeId/span、related sort/dedupe、all-or-nothing missing path、empty-origin gate、field preservation、childPathV1 深度界；**B7b1/B7b2/B7b3a/B7b3b/B7b3c/B7b3d** `Tests.Typed.DiagnosticLocationsV1` 固定 NR/CallGraph + TypeCheck + EffectCheck + BoundCheck + DisclosureCheck + CheckV1 composition path drafts→WithOrigins exact NodeId/stableContext/related set（public erase；属 D1-07 产品诊断切片，非 B7a locate 基础设施）；**B3** source-driven golden `source-full-tag-v1` + `ProgramV1SourceFullTagGolden` + independent Python oracle 闭合 source→command/Loader identity/canonical bytes/sourceHash/NodeId preorder/spans（constructed `full-tag-v1` 仍为 wire/mutation 负例 base，不变）；`SourceNodeTraversalV1` 另固定 public Type visits；不是 contained frontend integration |**可复用地基**；hash、span join、OriginJoin、WithOrigins/locate 基础设施已接线；NR/CG/TypeCheck/Effect/Bound/Disclosure drafts via B7b1/B7b2/B7b3a/B7b3b/B7b3c（path→locate）；B8a foundation + B8b product cutover done；formal 仍 open（B7b engineering 含 B7b3d 已完成）|span join + OriginJoin + B7a locate 已完成（含 recursive Type + B1R decoder-anchor zip）；B7b1 NR/CallGraph + B7b2 TypeCheck + B7b3a EffectCheck + B7b3b BoundCheck + B7b3c DisclosureCheck + **B7b3d CheckV1 draft composition/located API** path drafts 已接线（public erase；B7b engineering complete；B8a+B8b engineering done；formal pending）；删除 legacy source canonical/hash只能等 D2 consumer迁移|
| `TASK-D1-02` | pending | 唯一 `program ... where` parser直接产出 validated ProgramV1 | `Syntax.lean` `elab_rules` 已原子切到 V1：`decodeProgramCommandV1Checked` 产出 `ValidatedSourceV1`，随后 `canonicalValidatedSourceAstBytesV1` 生成 canonical root bytes，最终以 `@[proof_forge_program]` 登记 `ProgramExportPayloadV2{schema="proof-forge.program-export.v2"}`；legacy `decodeProgramCommandChecked` 与 Source AST source-reading decoder 已删除。B12 后 CLI 产品入口为 `superviseFrontendSourceV1` 的 success-only `productInput`（worker 内部共享 Loader snapshot，随后经 `compileProgramProductV1`）；`selectProgramV1Product`/`selectProgramV1` 仅非 CLI 库/测试 API | `ProgramCommandAcceptance` 验证 command 产出 v2 payload、identity join、非法顶层 kind/run_cmd 拒绝；`CounterV1Evm` 验证 CLI V1 subset | **新轨子集**（command 与 export 已是 V1 唯一路径） | 完成 v2 export 切over；删除 `quoteProgram`、旧 command decode/quote 与 legacy Source source-reading decoder；无 compatibility fallback |
| `TASK-D1-03` | pending | 13种 Phase-1 declaration、完整 type/default elaboration | legacy decoder覆盖 state/struct/enum/const/event/error/init/entry/view/fn/invariant/requires/proof；V1 Loader 已从 Syntax 直接接线 13 种 declaration alternatives，并保留 source order/raw identity；递归 TypeV1 prefix atom decoder 经唯一 `collectTypeAtomSyntaxV1`→`decodeTypeV1At` 构造 primitive/Named/Bytes/Field/Option/Array/Map 并 emit canonical-preorder syntax anchors（`decodeTypeV1WithAnchors`）；`pfType` surface **仅** sole `portableType` + same-line `prefixType`（`Option`/`Array`/`Map`；`default+1`），**全部 fixed-depth `default+2` combo parsers 已删除**（`pfType_parser==2`）；递归表面含浅层 Array/Option/Field/Bytes 与 `Array Map`/`Option Map`/更深 Option·Array 嵌套与 Map key/value 任意嵌套；`pfAggregateMember` **仅** sole `aggregateField` 嵌入 `categoryParser pfType`（`pfAggregateMember_parser==1`；specialized fixed-depth field alternatives 已删除），struct field 与 state/const/param/result 共享同一 Type 表面与 sole decoder 路径（无第二 type decoder）。`ProgramV1TypeSurface` 固定所有 EBNF Type 替代形式、shallow portableType|prefixType 回归、recursive Map/Array/Option 嵌套（含 struct-field Map/Array Map/Option Map 正例与 malformed 负例）、canonical bytes/sourceHash 类型变更与 recursive non-aliasing、ordinary/escaped named-type equality 与 malformed Map/length/constructor 负例矩阵。全部 declaration negatives 与 D2 typed legality仍未闭合 | legacy `DeclarationAcceptance` wrapper 与其 Source-AST-only suites 已删除；`ProgramV1Declarations`覆盖 source→Loader V1 的13种 declaration正例、source order、Named边界和 Map param/result，以及 type/extension/proof parser-boundary negatives；`ProgramV1TypeSurface` 覆盖完整 recursive type surface（含 shallow 回归与 Array Map/Option Map / struct-field Map 正/负例）；`ProgramV1SpanJoin` 另固定 recursive type-node span join、Map key-before-value 与 same-count type tamper 拒绝；新增 `ProgramV1DeclarationNegatives` 直接覆盖 ProgramV1 declaration negatives（reserved/qualified 声明名、malformed syntax、empty aggregates、duplicates、reserved components、visibility misuse）；constructed `full-tag-v1` 仍为 wire/mutation 负例 base；**B3** `source-full-tag-v1` 为 source-driven full-tag authority（13 种 declaration surface + command/export 与 Loader 双路径 identity/bytes/sourceHash/NodeId/spans） | **新轨子集**（legacy declaration wrapper 已删除；declaration negatives 已迁入 `ProgramV1DeclarationNegatives`；recursive Type surface 与 B1R fixed-depth 删除门禁已闭合） | 递归 Type surface 与 fixed-depth parser 删除已闭合；继续迁移完整 TST-SRC-004 剩余 negatives 与旧 Source AST tests；替代就绪仍需唯一入口、legacy引用归零和删除门禁，不得经 legacy AST投影 |
| `TASK-D1-04` | pending | 完整 statement/expression/place/pattern；local fn与qualified external call/schedule分流 | grammar/legacy decoder有大量 unary/binary、let/assert/revert/emit/if/for等；V1 Loader 现已从 Syntax 直接构造 `.call/.schedule ExternalCallExprV1`、最小 `.if_/.for_` control-flow slice、`.localCall` / `.constructor` / complete `Place ::= Ident PlaceSuffix*` expression slice（任意混合 `.` field 与 `[Expr]` index suffix，构造嵌套 `.place (.field/.index ...)`）、prefix `-`/`~`/`!` `.unary` expression slice、`+`/binary `-`/`*`/`/`/`%` `.binary` arithmetic expression slice、`<<`/`>>` `.binary .shl/.shr` shift expression slice、`==`/`!=` `.binary .eq/.ne` equality expression slice、`<`/`<=`/`>`/`>=` `.binary .lt/.le/.gt/.ge` ordering comparison expression slice、`.literal (.string value)` string literal expression slice（entry return/let-value positions，含 empty/ASCII/escaped quote/backslash/tab/newline/Unicode scalar decoded values、alternate Lean escape spelling canonical identity、string payload/tag hash non-aliasing、redundant grouping identity 与 parser-boundary negatives）、dot-only `.place (.field ...)` field-place slice与任意 PlaceSuffix chaining slice（bare/field/index/mixed-chain rvalue 与 assignment target，含 multi-field/source suffix order、escaped raw component canonical identity、unary/binary operands、canonical ProgramV1 bytes/sourceHash equality、field-vs-whole-escaped-name/depth/order/suffix-kind non-aliasing、malformed dot/index boundaries、reserved component order、target-before-rhs 与 earlier-suffix-before-later-suffix priority，并固定 uppercase/lowercase root case-sensitive identity、constructor/local-call parentheses classification、terminal qualified-base index acceptance，继续拒绝 `(x)[0]`/`f()[0]`）、core statement slice（annotated/omitted-type `.let_`、simple-component、field-place及任意 place-suffix target `.assign`、valued/value-less `.return_`、`.assert_`/assert-else）、statement-level `.match_` slice（`match Expr with` + 一个或多个 `| Pattern => do Block` arms，Pattern 新增 `ConstructorPattern ::= QualifiedId "(" PatternList? ")"`，空参数、嵌套与混合参数 patterns 经 `pfPattern` max-precedence form 与现有 qualified-id helper 解码为 `PatternV1.constructor`，保留 scrutinee/arm/pattern/body source order、raw identity 与 canonical hash binding；wildcard/bind/bool/integer/string literal 行为不变，单组件 call-like pattern 继续 fail closed，nested match statements 递归解码；expression-level `.match_` slice（`match Expr with` + 一个或多个 `| Pattern => Expr` arms，使用独立 `pfExprMatchArm` category，arm value 为完整 precedence-0 expression，支持 nested match expression 与 parenthesized operand，保留 scrutinee/arm/pattern/value source order、raw identity 与 canonical hash binding；statement match 与 expression match 通过不同 arm category 严格分离，`do` block 进入 expression arm 与 bare expression 进入 statement arm 均在 parser boundary 失败；legacy Source reader 保持 fail closed）以及 `.revert`/`.emit` statement slice，保留 raw qualified components、escaped embedded-dot raw components、args/index/source order、unary/binary operator与 operand order、same-tier left-assoc（含 shift cross-operator同层）、MulExpr over AddExpr、AddExpr/MulExpr/UnaryExpr over ShiftExpr、Shift/Add/Mul/Unary over equality、shared comparison-tier ordering/equality chain non-associativity、`&`/`^`/`|` bitwise operator tag/source-order/raw escaped identity、bitwise left-assoc/right-nested grouping、Compare > bit-and > bit-xor > bit-or precedence、bitwise preservation of higher unary/arithmetic/shift behavior、enum variant pipe coexistence、`&&`/`||` logical operator tag/source-order/Bool/raw escaped dotted place identity、logical same-tier left-assoc/right-nested grouping、Compare > `&` > `^` > `|` > `&&` > `||` precedence、logical preservation of higher unary/arithmetic/shift behavior、logical redundant grouping canonical identity/hash non-aliasing/malformed digraph boundary/left-before-right portable-name errors、unary-over-multiplicative、grouping override/canonical redundant grouping identity、nested/dangling-else ownership、statement/source order、raw iterator/binder/target/condition/error/event identity、revert/emit argument count/order/nested expression shape与 loop endpoint order；`decodeNameV1` 已在 ProgramV1 Syntax decoder 层拒绝多 component Lean Name，whole-escaped dotted identifier 仍是一个 raw component，unescaped qualified revert/emit name 在 D2 error/event lookup 尚未迁移前 fail closed；dot-only field-place chains（含 uppercase/lowercase roots）与任意 mixed PlaceSuffix chains 已迁移；并拒绝 one-component/reserved callee、malformed core/if/for/call-like/index/arithmetic/shift/revert/emit/place-suffix、reserved iterator/callee/path/base/binder/target/error/event、`(x)[0]`/`f()[0]` grouped/call base suffix 以及非法 spelling/越界的 shared IntegerLiteral gate（expression/pattern 共用 sole decoder；unsigned decimal 或 lowercase-prefix `0x`，hex digit 大小写不影响 identity，范围 `0..2^256-1`）；**B4**：已删除 obsolete `syntax "call " str : pfStmt` 与 `pfStmt| call $_callee:str` decoder-rejection 臂，sole `call` 表面为 structured `call QualifiedId(args)`（`schedule` 同构），legacy `call "…"` 在 Lean parser boundary 以 `failed to parse file` 拒绝（`ProgramV1ExternalStatements`/`ProgramV1ControlFlow`/`ProgramV1Declarations` 同源负例；无 decoder-only 识别或 compatibility fallback）；固定除零/模零与 shift count 0/64只形成 Source AST，不做 D1 numeric/width/sign求值或拒绝；仍缺剩余 pattern forms、D2 error/event lookup、typing/effect、Semantic rollback、event runtime与target behavior等完整迁移 | `ProgramV1ExternalStatements`、`ProgramV1ControlFlow`、`ProgramV1MatchStatements`、`ProgramV1MatchExpressions`、`ProgramV1ConstructorPatterns`、`ProgramV1ExpressionForms`（二者共同固定 decimal/hex/hex-digit-case canonical identity、UInt256 max、decimal/hex overflow、`0X`/binary/octal/underscore rejection与 expression left-before-right priority）、`ProgramV1UnaryExpressions`、`ProgramV1ArithmeticExpressions`、`ProgramV1ShiftExpressions`、`ProgramV1EqualityExpressions`、`ProgramV1OrderingComparisons`、`ProgramV1BitwiseExpressions`、`ProgramV1LogicalExpressions`、`ProgramV1CoreStatements`、`ProgramV1FieldPlaces`、`ProgramV1IndexedPlaces`、`ProgramV1PlaceSuffixes`、`ProgramV1RevertEmitStatements` 与 `ProgramV1StringLiterals` 是聚焦 source→ProgramV1 suites；旧 `*Statements`/Checked*/operator/legacy expression suites 已按 coverage mapping 删除；对应行为由上述 direct ProgramV1 suites 承载 | **新轨大子集 + isolated alpha Core/Source compatibility residual** | formal 仍 pending；精确盘点并闭合剩余 pattern/forms 与 D2 event/error lookup 边界；legacy source reader与旧 suites 已删除，产品递归 import closure已禁止 alpha `Core/Source`；物理删除等待显式 compatibility tests迁完 |
| `TASK-D1-05` | pending | accepted ADR-0019要求 `proof-forge.program-export.v2` + canonical ProgramV1 root bytes | `ProgramExport.lean` 只登记 `proof-forge.program-export.v2`，attribute 要求 `ProgramExportPayloadV2`，`programPayloadV2` 先 bounded-exhaustively 解码 scalar/ByteArray 再**仅**调用 `decodeCanonicalSourceAstBytesV1`，并验证 `programIdentity == moduleName ++ declaration raw components`；v1 schema、非 `ProgramExportPayloadV2` 类型、malformed 表达式均以 PF-EXPORT-001/004 拒绝；`ProgramPayload.lean` 已删除，无 v1 row/type/reader | `ProgramExports.lean`、`ProgramExportAcceptance.lean`、`ProgramExportAcceptanceEmpty.lean` 已迁移到 v2 schema/export table/empty registry/payload reconstruction/identity join 覆盖；v1 schema 在 `normalizeProgramExports` 中明确拒绝 | **新轨子集**（v2 export/registration/reconstruction 已是唯一路径） | `ProgramPayload.lean` 已删除；旧 v1 payload 消费者归零；legacy Loader/Source decoder 已在 D1-06 删除 |
| `TASK-D1-06` | pending | multi-program loader、raw component identity、exact `--program` selection | B12 后产品 CLI 仅调用 `superviseFrontendSourceV1`，由 B10 worker 的 shared private parse/select/SpanJoin snapshot 返回 request-bound response，composer sole reconstruct 为 `ValidatedSourceV1 × OriginInventoryV1`；CLI 不 import/call Loader。`selectProgramV1Product` 与其他非产品库 API 为 `parseProgramsV1`/`selectProgramV1`/`WithSpans`/`WithOrigins`；`*WithDiagnostics` 已删除；`Syntax.lean` legacy Source decoder 家族（`decodeType`/`Param`/`Expr`/`Statement`/`Item`/`Program`、`decodeProgramCommandChecked` 与所有 private unchecked helpers）与 `Loader.lean` legacy APIs（`parsePrograms`/`selectProgram` session/top-level variants 与 `processCommands`/`selectParsedProgram`）已全部删除；`renderSourceQualified` 仍用于 duplicate-error 文本但不参与 identity | `Tests/Language/Loader.lean` 已迁移到 V1 并覆盖 source-size/run_cmd 拒绝/deep-import 拒绝/duplicate-init/multiple-program selection/namespace-bound；`Tests/Language/ProgramCommandAcceptance.lean` 使用 `parseProgramsV1` 验证 v2 payload/identity/kind/run_cmd 拒绝；`Tests/Language/ProgramSyntax.lean` 移除 per-subdecoder bound 调用，改用 Counter V1 source-text 验证 identity/hash/semantic requirements；`Tests/Language/StateVisibility.lean` 使用 `selectProgramV1` 与 `compileValidatedSourceV1`；`Tests/Language/AggregateDeclarations.lean` 已迁移到 V1 并注册；`Tests/Language/SourceBoundsAcceptance.lean` 已删除，其 PF-BOUND-001 覆盖由 `ProgramSyntax`/`Loader` 与 ProgramV1 suites 承载；**B2** 新增 `Tests/Language/ProgramV1Bounds.lean` resident unit（preflight depth/nodes + identifier/Root-identity 256/257；不含 300-term/20k-state/16 MiB）与 `scripts/program_v1_source_bounds`/`just source-bounds` 子进程 CLI gate（`--module Root`；300-term/20k-state/16 MiB±1/combined；精确 PF-BOUND-001/PF-SRC-INVALID、零 partial output），已注册 ordinary `just ci`，不依赖 quarantined `dsl-negative`；programIdentity 组装处 structured `resourceBound`/`PF-BOUND-001` | **新轨子集**（legacy source-reading surface 已删除；sole CLI source 入口为 B12 supervisor/product carrier；`selectProgramV1Product`/`selectProgramV1*` 为 worker共享实现或非 CLI 库 API） | 已删除 `Tests/Language/SourceBoundsAcceptance.lean`（不得复活）；B2 以 `ProgramV1Bounds`+`source-bounds` 为 ProgramV1 权威 source-bound 门禁；`Core/Source.lean`、alpha Typed/Semantic core 与 `Tests/Fixtures/SourcePrograms.lean` 保留到 D2 consumer 迁移；下一步 D2 Typed/Semantic/Resolver contract 迁移 |
| `TASK-D1-07` | pending | stable DiagnosticV1 code/schema/order/redaction和source origin | **B6** `ProofForgeV2/Core/DiagnosticV1.lean` 原子替换为 SPEC-DIAG-001/ADR-0022 结构化 v1：59-code closed `DiagnosticCodeV1`（exact wire+rank+default severity/phase；无 alpha `PF-SEM-*`）、`DiagnosticOriginV1` nullable nodeId、primary/related、severity/phase、program/target/RequirementKeyV1/ExtensionKeyV1/expected/actual/context/stableContext/suggestion、sole PF-JCS encode/decode、message-independent order/dedupe、结构 redaction、`normalizeDiagnosticBundleV1` 100-cap+`PF-DIAG-LIMIT`（B8b 经 `mkFailureBundleV1` 产品接线）；`renderHuman`=`CODE: message`。**B7a**：删除 `errorSentinelNodeId`；Loader parser/duplicate primary 使用显式 `nodeId=none`；`NodeTraversalV1.childPathV1` sole helpers；Source `DiagnosticLocateV1` location-draft materializer；Loader `selectProgramV1WithOrigins`。Typed name-resolution/call-graph 已经 **B7b1** `DiagnosticDraftV1` path drafts（`resolveProgramDraftsV1`/`analyzeFnCallGraphV1`；public APIs erase；locate 经 sole `DiagnosticDraftV1`→B7a）；**B7b2** TypeCheckV1 path drafts（`typeCheckProgramDraftsV1` additive；public type-check APIs exact erase；sole childPath helpers + ModelV1 field/param/result/variant/error/event paths；primary=offending AST、related=decl contracts；resolution-not-ok short-circuit 保留）；**B7b3a** EffectCheckV1 path drafts（sole `checkEffectsDraftsV1`/`checkProgramEffectsDraftsV1`；public effect APIs exact erase；primary=FnDecl/ViewDecl；related=finite causal direct+LocalCall evidence；fixed-point closed-effect 权威不变）；**B7b3b** BoundCheckV1 path drafts（sole `checkBoundsDraftsV1`/`checkProgramBoundsDraftsV1`；public bound APIs + collectors exact erase；cycle: primary min-ordinal FnDecl + remaining SCC decls + in-SCC LocalCall；loop overflow: primary Stmt.For + enclosing callable + ancestor Fors；PF-BOUND-001 字段/相位/product 行为不变）；**B7b3c** DisclosureCheckV1 path drafts（sole `checkDisclosureDraftsV1`/`checkProgramDisclosureDraftsV1`；public disclosure APIs exact erase；VisibilityEvidence；primary=sink input expr；related=value∪PC causes∪sink site；PF-VIS-001 行为不变；**B7b3cR** `withControlRootEvidence` 为 if/match PC 与 Expr.Match result 附加 condition/scrutinee Expr root，join 仍 label-authoritative）；**B7b3d** CheckV1 sole draft composition + additive located API 已完成（`TypedCheckDraftResultV1`/`checkProgramTypedDraftResultV1` 单 authority；legacy exact erase；located sourceHash 门禁 + all-or-nothing `locateArray`；无 sort/dedupe/cap）；**B7b engineering complete**；**B8a** inert `DiagnosticBundleV1`/`DiagnosticResultV1` 地基已落地（private constructor、sole total `mkFailureBundleV1` 复用 `normalizeDiagnosticBundleV1`+validate/encode、fail-closed 固定 `PF-INTERNAL`、read-only diagnostics、human/PF-JCS array render、exit priority selection；B8a foundation）；**B8b** sole atomic product multi-error cutover 已完成（`selectProgramV1Product`/`normalizeProgramLocatedV1`/`compileProgramProductV1`/CLI full-bundle render+selectExitCode；删除 `*WithDiagnostics` 与 Pipeline first-error product helpers；formal 仍 pending；Pipeline/TypedV1 三 special CompileError + 其余 fail-closed `invalidProgram`；无 dual reader | `Tests/Core/DiagnosticV1.lean` 固定 catalog uniqueness/ranks/defaults、nullable NodeId、related 归一、全字段 JSON round-trip、unknown/trailing/noncanonical 拒绝、message-independent order/dedupe、结构 redaction、100/101/200/idempotent cap；`Tests/Language/ProgramV1Diagnostics.lean` 固定 parser/duplicate/redaction pre-node `nodeId=none` 与 human 字节；**B7a** `Tests/Language/ProgramV1DiagnosticLocate.lean` 固定 WithOrigins/locate 契约 ；**B7b1/B7b2/B7b3a/B7b3b/B7b3c** `Tests.Typed.DiagnosticLocationsV1` 固定 NR/CallGraph + TypeCheck + EffectCheck + BoundCheck + DisclosureCheck path drafts→exact NodeId/stableContext/related set 与 erase parity（places/patterns/exprs/match/stmts/const/callable/invariant；resolution short-circuit；Effect direct/transitive/cycle/shadow/incomplete causal related；Bound self/2/3-fn cycle + loop overflow fn/entry/view/init/if/match/sibling multi + cycle-then-loop + dup-fn incomplete；Disclosure assign/return/emit/revert/call/schedule/index/for/assert/const/localCall/match/if-PC/binder/join/shadow/multi/incomplete；B7b3cR control-root path/NodeId（if Param+State、match stmt/expr、binder、nested equal-private both roots、nested lower-secrecy exclude、PC restore count）；**B7b3d** CheckV1 composition locate exact primary/related NodeId phase order + `Tests/Typed/CheckV1` draft erase/located hash-gate；**B8a** `Tests/Core/DiagnosticBundleV1.lean` 固定 multi-error normalize/sort/dedupe/100-cap、nonempty+≥1 real error≠PF-DIAG-LIMIT、≤1 final limit、canonical encode、idempotent constructor、fail-closed fixed PF-INTERNAL（empty/limit-only/warning-note-only/invalid/non-encodable、无 input leak）、human+PF-JCS array render、exit priority 70>7>6>5>4>3（diagLimit/non-error neutral）、`DiagnosticResultV1` ok/error 不碰 alpha CompileResult）；**B8b** `Tests/Compiler/DiagnosticPipelineV1` + `Tests/CLI/DiagnosticsV1` 固定 product multi-error/full-bundle/selectExitCode/usage exit 2|**新轨子集**（B6 carrier + B7a locate/sentinel 退役完成；B7b1 NR/CallGraph + B7b2 TypeCheck + B7b3a EffectCheck + B7b3b BoundCheck + B7b3c DisclosureCheck path drafts done (public erase)；**B7b engineering complete**（含 B7b3d）；**B8a**+**B8b** product diagnostic cutover engineering done；formal 仍 pending）|B7b1–B7b3d + **B8a** inert DiagnosticBundle foundation 已完成；**B8b** compiler/CLI multi-error product cutover 工程门槛已关闭；formal TaskQualification 仍独立|
| `TASK-D1-08` | pending（**2026-08-01 superseded**：前端监督层已按产品决策移除，本任务目标整体删除，含 B11a/B11a2/B11b1/B11b2/B12 全部切片；原描述保留为历史） | no-follow safe-open、contained frontend worker、ResourceProfileV1 supervisor/receipt | `Core/Common.lean`已有ResourceProfileV1和lower-only validation。**B12** 已把 CLI `build`/`build-counter` 原子切到 pinned sibling safe-open/frontend workers；success 只消费 composer 内 sole reconstruct 的 product pair，canonical SafeOpen Err 只额外保留 closed fault（`.tooLarge`→精确 16 MiB `PF-SRC-INVALID`），source open/resource/protocol事件映射 closed DiagnosticV1，Main 内 `realPath`/`readFile`/Loader/caller reconstruct/embedded Counter fallback 已删除；native worker执行以 no-follow source fd + 512 MiB metadata gate冻结到私有 snapshot，Darwin suspended spawn/vnode检查后才恢复，snapshot mutation/cleanup fail closed。**B9** 新增 inert `ProofForgeV2/Frontend/ProtocolV1.lean`：closed one-frame `Frontend.Req.v1` / `Frontend.Ok.v1` / `Frontend.Err.v1` binary wire（exact SemVer、validated `ProjectRelativePath`、raw UTF-8 selectors 无 NFC、raw `sourceBytes`；success=`requestDigest`+canonical ValidatedSourceV1 bytes+preorder spans-only；failure=`requestDigest`+canonical `DiagnosticBundleV1` PF-JCS；`maxProtocolBytes=64MiB`/`maxSourceBytes=16MiB`/`maxNodeSpanCount=100000`；full-consume+re-encode identity；reconstruct 仅 `decodeCanonicalSourceAstBytesV1`+`assignNodeIdsV1` zip+`joinOriginsV1`）；opaque smart-constructor/accessor API 供 B10；**B9R**：selector 仅受 `maxProtocolBytes` 分配/帧 guard（`maxSelectorBytes`=同值兼容别名，**无** 4096 语义上限；Lean `1..256×1..240` 推迟 Loader）；failure 在 `parsePfJcs` 前 O(n)/O(1) top-level PF-JCS 条目预扫描（0 或 >101 拒绝）；**B10** 新增 `ProofForgeV2/Frontend/WorkerV1.lean` + `WorkerMainV1.lean`/`proof-forge-frontend-worker-v1`：one request/process、stdin bounded read to EOF、exact 1.0.0→UTF-8→Loader、valid source diagnostics=`Frontend.Err.v1`+exit0、abnormal usage/protocol/internal=64/65/70+stable stderr token；Loader `selectProgramV1FrontendPayload` 与 product API 共享 private single parse/select/SpanJoin snapshot；**B11a** 新增 `Frontend/SafeOpenV1.lean` + package-owned C FFI：trusted absolute root 下 component-by-component `openat` no-follow/nonblocking/cloexec、directory-only parents、regular single-link leaf、16 MiB initial-`fstat` pre-read gate、exact initial-size read + one-byte probe、fd/path device/inode/mode/link-count/size/mtime/ctime recheck、closed redacted fault labels与 opaque snapshot；**B11a2** 新增 `Frontend/DarwinSupervisorReceiptV1.lean` pure private-ctor receipt model（sole `darwin-development-observed` assurance、hard/effective profile identity+digest、optional request digest、bounded observations、closed event/result/cleanup、11-field canonical PF-JCS+4 KiB cap+exact re-encode+receipt digest；无 path/PID/signal/exit/stderr/tail/secret carrier）；**B11b1** 新增 `Frontend/DarwinWorkerSupervisorV1.lean` + `DarwinSupervisorV1.lean` 与 package-owned Darwin C primitive：监督已编码 canonical frame；native monotonic budget 从 allocation/pipe/spawn 前开始，`posix_spawn` 独立 process group + CLOEXEC/nonblocking pipes，selective `LEAN_SYSROOT`/`LEAN_PATH` + fixed env，轮询 aggregate process count/`phys_footprint`、stdout/stderr caps 与 deadline，按 process→memory→output→deadline 归因，bounded group kill/cleanup；private closed native frame 在 Lean exact decode 后绑定 request digest/path/spans（success 另 reconstruct），malformed/cross-request/incomplete-cleanup fail closed，并实际 mint B11a2 receipt。**B11b2** 新增 closed `SafeOpen.Req/Ok/Err.v1` protocol 与 one-request `proof-forge-frontend-safe-open-worker-v1`（不 import Loader），`superviseFrontendSourceV1` 以显式 pinned opener/frontend executable 逐阶段复用同一 `posix_spawn` primitive；private native `CLOCK_MONOTONIC` budget capability 在 open request 前 sole mint，两个 child stage 消费 exact absolute start；overall wall 覆盖 open→decode/request construction→frontend worker，native 在每次 allocation/pipe/spawn 前检查耗尽，最终 receipt 合并两阶段 peak；SafeOpen Ok/Err 绑定 canonical request digest，仅 request-bound canonical fault + complete cleanup mint live `sourceOpenFailed`，open transport limit/exit/signal 保留 closed attribution。**B12 CLI/Loader 产品切换已完成**；仍无 controller-backed process containment 或完整 formal host/race matrix | Source bounds和negative shell tests覆盖部分size/path；**B9/B9R** `Tests/Frontend/ProtocolV1.lean` 固定 request/success/failure round-trip、fixed full-frame request golden hex、hard-maxima constants + 16MiB source length-bomb + span-count bomb、**legal plain QN selector >4096 round-trip**、**selector declared-length maxProtocolBytes+1 bomb**、**102-entry tiny PF-JCS pre-scan bomb**、**nested/string commas not miscounted**、**normalized 100+PF-DIAG-LIMIT=101 round-trip**、**101 non-limit raw noncanonical**、success AST root re-encode identity mutation、tag/field/trailing/truncation、digest replay（bind/reconstruct）、span count/range（mk/bind）、noncanonical diagnostics、foreign path（mk + decode+bind）、reconstruct→sourceHash/OriginJoin；**B10** `Tests/Frontend/WorkerV1.lean` 固定 direct Loader/product parity、version/UTF-8/parser priority、selection/determinism，并以真实 subprocess 固定 success/Err、malformed/truncated/declared-oversize/argv 的 stdout/exit/stderr；**B11a** `Tests/Frontend/SafeOpenV1.lean` 固定 fault wire、regular/empty/determinism、relative root、root/leaf/intermediate symlink、hardlink、permission denied、directory/FIFO、missing 与 full-suite exact 16 MiB/+1；**B11a2** `Tests/Frontend/DarwinSupervisorReceiptV1.lean` 固定 exact PF-JCS/digest KAT、request replay、lower-only profile、event/result/request join、limit projection、canonical closed fields与 privacy rejection；**B11b1** `Tests/Frontend/DarwinWorkerSupervisorV1.lean` 以真实 worker/子进程固定 Ok/Err、deadline/process/memory/stdout/stderr limits、exact cap acceptance、nonzero exit/signal、malformed与 cross-request response、missing worker closed fault、lower-only profile 和 non-Darwin compile/skip；**B11b2** `Tests/Frontend/SafeOpenWorkerV1.lean` 与 `DarwinSourceSupervisorV1.lean` 固定 closed protocol/process exits、regular Ok/parser Err、open fault 无 frontend spawn、FIFO/socket/symlink/hardlink、16MiB±1、test-only opener deadline、absolute budget pre-spawn expiry、non-rearm shared wall、SafeOpen Ok/Err cross-request replay、two-phase peak join、open process/memory/output/exit/signal 与 malformed/unknown fault；仍不能关闭 `setsid()` escape、controller-backed containment、完整 formal host/race matrix 或 CLI 产品面 | **可复用地基 + 协议 foundation（B9+B9R）+ standalone worker（B10）+ native safe-open foundation（B11a）+ pure Darwin receipt model（B11a2）+ actual Darwin development-only worker supervisor（B11b1）+ shared source-open/frontend composition（B11b2）+ CLI sole supervised source authority（B12）；均非 contained，formal TASK-D1-08 仍 pending** | B12 已移除不受控进程内 source open/Loader与 fallback，并以 `b12-cli-source-authority-deletion-gate` 固定；formal host/race/contained qualification 仍 pending |

**2026-07-30 D1 工程复核纠正**：按 `docs/specs/language.md` 的规范 EBNF 逐项映射后，
D1-03 的 13 种 declaration、递归 Type，以及 D1-04 的全部 statement/expression/place/pattern
alternatives 已由 direct ProgramV1 decoder 与 ordinary/fast suites 覆盖；上表单元格中的“剩余
pattern/forms”是迁移过程遗留措辞，不再表示 D1 source grammar 缺项。D2 的 name/type/effect、
event/error lookup、rollback/runtime 与 full Normalize 仍独立 pending。CLI/frontend 边界现由 static
`LanguageParserDescriptorV1` 解析 omitted/explicit `1.0.0` 为同一 descriptor，并在 source open 前以
`PF-LANGUAGE-VERSION-UNKNOWN` 拒绝 range/`latest`/malformed/unknown exact；formal task 状态不变。

## D2：Type/effect/semantic core

| Task | Formal | 新设计要求 | 实际代码与产品接线 | 现有测试事实 | Engineering | 迁移与旧代码处理 |
|---|---|---|---|---|---|---|
| `TASK-D2-01` | pending | 完整name/type checker、pure-fn call graph、invariant/proof-reference source binding | 新增 `ProofForgeV2/Typed/ModelV1.lean` 与 `NameResolutionV1.lean` 直接消费 `ValidatedSourceV1`，构建 13 类声明 source-order 序数表与显式作用域名解析；新增 `ProofForgeV2/Typed/TypeCheckV1.lean` 在 name resolution 之后对 literal/place（含 struct field chain 与 Array/Map index）/unary/binary/constructor/localCall 做精确 `TypeV1` 推导与宽度边界检查，并对 statement/body 完成精确类型检查：let（annotation 优先、无 annotation 时按 value 推断）、assign（target type 与 value 一致）、return（value 匹配 result / Unit 上下文拒绝带值 return）、assert（condition 为 Bool）、revert/emit（按声明 param count/type 检查参数）、if（condition Bool，分支 block 在同 scope 与 result 下检查，binder 不外泄）、for（端点为同宽整数，iterator 进入 body scope 且不外泄）、call/schedule（参数按无 expected type 的 expression 检查）、match（pattern 对 scrutinee 精确检查 wildcard/bind/Bool/integer-width/constructor 子模式，string pattern fail closed；expression match 全 arm 结果统一；statement match arm body 在 result context 检查；enum exhaustiveness 要求 `_`/bind 或 declaration-order 全 variant 覆盖；非 enum scrutinee 要求 catch-all；诊断按 source order）；init/entry/view/fn/const/invariant bodies 已按声明 scope/result 接线；整数字面量必须携带预期整数类型并按宽度精确范围检查、无默认宽度；新增 `ProofForgeV2/Typed/CallGraphV1.lean` 直接消费 `NameResolutionV1` 的 scope-shadowing 规则，从 `fn`/`entry`/`view`/`init` body 收集 `.localCall` 边（locals/params 遮蔽顶层 fn 不产生边，非 fn callee 不产生边），构建 caller→callee 有向图，以 Tarjan SCC 检测全部 cycle，self-recursion 作为 1-cycle 报告，并按 cycle 最早成员 declaration ordinal 升序输出确定性诊断；`entry`/`view`/`init` 只作为 call site 被遍历，不作为 call target，因此不可能出现在 cycle 上；`checkProgramStructureV1` 单入口串联 tables→resolution→acyclicity，三个阶段始终执行，诊断按 phase order 拼接，一次通过报告全部结构错误；**共享独立多遍入口** `ProofForgeV2/Typed/CheckV1.lean` 单次 resolve 后拼接 structure→type→effect→bound→disclosure；**已作为 fail-closed 产品门禁接线进** located `NormalizeV1`（`compileProgramProductV1`/CLI `build` 经该边界继承）；legacy alpha checker/Typed IR lowering仅在隔离 compatibility/tests保留（formal pending） | 新增 `Tests/Typed/NameResolutionV1.lean` 覆盖作用域解析与错误类别诊断；新增 `Tests/Typed/TypeCheckExpressionsV1.lean` 覆盖运算符结果类型、struct field chain 与 Array/Map index、整数字面量边界、mixed-type 二元拒绝与 expected-vs-actual source order、一元类型门、bool/整数 分离；新增 `Tests/Typed/TypeCheckCallsV1.lean` 覆盖 struct/enum constructor 与 local fn call 的 exact/nested/wrong-count/wrong-type/wrong-category、source-order 诊断优先级；新增 `Tests/Typed/TypeCheckStatementsV1.lean` 覆盖 statement/body 正向类型检查、let annotation mismatch、assign target/value mismatch、return/result mismatch、value-return-in-Unit 拒绝、revert/emit unknown/wrong-count/wrong-arg-type、assert non-Bool、if non-Bool condition、for 端点宽度 mismatch 与 iterator leak、branch binder non-leak、call/schedule 参数检查、invariant non-Bool 以及 source-order 错误优先级；新增 `Tests/Typed/TypeCheckMatchV1.lean` 覆盖 wildcard/bind/literal pattern、struct/enum constructor pattern（enum 通过 source，struct 通过 AST-direct construction，因 parser 目前要求 constructor pattern 为 2+ component qualified id）、string pattern 拒绝、integer width boundary、constructor on non-named、arm 结果类型 mismatch、statement match result context、enum exhaustiveness 正向/负向、missing-variant declaration-order 报告、非 enum catch-all 要求、unreachable arm 仍类型检查、source-order 优先级；新增 `Tests/Typed/CallGraphV1.lean` 覆盖 acyclic fn chain、direct self-recursion、two-fn/three-fn cycles、entry/view/init→fn 不形成 cycle、shadowed callee（param/local）不产生边、unreachable fn cycle 仍被检查、cycle 与 resolution error 同时按 phase order 报告；新增 `Tests/Typed/CheckV1.lean` 固定共享多遍入口 Counter 正向、type/effect/bound/disclosure 错误、structure→type→effect→bound→disclosure 相位序、resolution 不重复、duplicate-fn incomplete | **新轨子集** | 继续扩展完整 type/effect checker；迁移全部 declaration 和 fn/proof binding |
| `TASK-D2-02` | pending | effect/call/view checker及pure-fn transitive effects | 新增独立 `ProofForgeV2/Typed/EffectCheckV1.lean`：从 ProgramV1 AST 收集当前可表达 direct effects（`state.read`/`state.write`/`event.emit`/`external.call.sync`/`workflow.schedule`/`failure.revert`），复用 ModelV1/NameResolutionV1/CallGraphV1 scope-shadowing 与 localCall 图，以 6-bit EffectSet 有限单调 fixed-point 做 pure-fn transitive closure（环上亦得完整上界，不静默降级）；duplicate fn key 时 analysisComplete=false/ok=false 并 fail closed 暴露 name-resolution 结构诊断；`fn` 仅允许 `failure.revert`，`view` 仅允许 `state.read`/`failure.revert`，`entry`/`init` 允许当前可表达 effects；稳定 `DiagnosticCodeV1.effectDisallowed` → wire `PF-EFFECT-001`，诊断按 declaration order + effect-kind order；**已由产品 located `NormalizeV1` 经 `CheckV1` 门禁 fail-closed 消费**（effect 相位；PF-EFFECT-001保持结构化 DiagnosticV1）；**TypedV1 alpha view write/synchronous-call 重复防御已退役**（`checkStatementV1` 不再按 `mode == .view` 拒绝 write/call；产品 sole path 为 EffectCheckV1；`Entry.mode` 仍从 entry/view 设置供 target 使用；alpha residual 仅 missing/after-return shape）；legacy `Core/Typed.lean`/`Typed.check` 内嵌 view 规则保留至 D2 consumer 删除；不做 context/disclosure/extension/bounds 扩展；**B7b3a** path drafts：sole draft authority + public erase；causal related 含 direct sites 与 effect-propagating LocalCall；formal pending | 新增 `Tests/Typed/EffectCheckV1.lean` 覆盖 pure fn/assert/revert 正向、fn state read/write/emit/call/schedule 负向、view read 正向与 write/emit/call/schedule 负向、entry/init 全 effect 正向、fn/view transitive absorption、param/local shadow 不误报 state.read、shadowed callee 不传播、三层 chain 与 human `PF-EFFECT-001` wire；`Tests/Typed/DiagnosticLocationsV1` B7b3a 固定 Effect origin/NodeId/erase parity；`Tests/Core/DiagnosticV1` 已登记 `PF-EFFECT-001` rank/wire；`CheckV1ProductGate`/`ValidatedSourceV1Pipeline` 固定产品路径 view write/call 仅 `PF-EFFECT-001`/`effectDisallowed`（非 alpha `cannot write state` / `cannot perform synchronous call`），经 legacy checker 与非产品 `compileValidatedSourceV1` compatibility 双路径 | **新轨子集**（独立 effect pass + 产品 sole EffectCheckV1 权威；TypedV1 alpha view effect residual 已删；legacy Typed.check residual 仍在；formal pending） | 继续补齐 context/disclosure/extension；legacy `Typed.check` view 规则随 D2 consumer 删除；不伪称 formal D2-02 完成 |
| `TASK-D2-03` | pending | loop/call/recursion bound与termination | 新增独立 `ProofForgeV2/Typed/BoundCheckV1.lean`：复用 CallGraphV1 `buildFnCallEdges`/`Tarjan`/`cyclicSccs` 在 fn 序数上拒绝 pure-fn local-call 递归/环，稳定 `DiagnosticCodeV1.resourceBound` → wire `PF-BOUND-001`（message `unbounded recursion (call cycle): …`，成员 declaration-ordinal 升序；**不改动** CallGraph 既有 `.sourceInvalid` cycle suite）；对 init/entry/view/fn body 源序遍历嵌套 `for ... bounded N`，以 checked `UInt32` 乘积维护 nest product（overflow ⇒ `PF-BOUND-001`；0 界有限；不做 range/`end-start≤N` 证明）；duplicate fn key 时 analysisComplete=false fail closed 并暴露 name-resolution structural 诊断；phase order：cycle 后 loop-product；**已由产品 located `NormalizeV1` 经 `CheckV1` 门禁 fail-closed 消费**（bound 相位；PF-BOUND-001保持结构化 DiagnosticV1）；**B7b3b** path drafts：sole draft authority + public/collector exact erase；cycle 复用 sole `collectFnCallEdgesV1`；loop walk path-threaded primary=Stmt.For related=callable+ancestor Fors；不做 effect-occurrence Semantic IR、ResourceProfile limits、authority；formal pending | 新增 `Tests/Typed/BoundCheckV1.lean` 覆盖 acyclic chain+entry、self/2-/3-fn cycles 的 `PF-BOUND-001` 与成员序、entry/view/init 非 cycle 成员、single `bounded 0/10/4096` 与 double nest ok、三层 4096 乘积溢出、if/match arm 内 nest 乘积、param/local shadow 不造边、`renderHuman` `PF-BOUND-001:`、duplicate-fn incomplete fail closed、cycle-then-loop phase order、draft erase multi-diag parity；`Tests/Typed/DiagnosticLocationsV1` B7b3b 固定 Bound origin/NodeId/erase parity；CallGraph suite 保持既有 `PF-SRC-INVALID` cycle 期望 | **新轨子集**（独立 bound pass + 聚焦测试；formal pending；产品 CheckV1 门禁已接线） | 继续补齐 effect-occurrence Semantic 上界、ResourceProfile limits，并考虑将 CallGraph cycle 诊断迁到 `PF-BOUND-001` 单一权威；不把 parser syntax-bound/target resource guard 冒充业务 termination；不伪称 formal D2-03 完成 |
| `TASK-D2-04` | pending | disclosure/authority/custody checker | 新增独立 `ProofForgeV2/Typed/DisclosureCheckV1.lean`（**D2-04a 显式 flow + D2-04b PC-label / implicit disclosure 工程子集**）：VisibilityV1 lattice `public_ < commitment < private_`（join=max secrecy；mayFlow 当 secrecy(src)≤secrecy(sink)）；bottom-up 表达式可见性（literal public、param/state/local/const-as-public place、field 继承、index join+public sink、unary/binary/constructor、localCall 成功结果 public_、**match 表达式结果=join(scrutVis, arm values join)** 使 scrutinee 控制污染结果）；语句 walk 线程 **PC label**（entry PC=`public_`）：`if_` 条件不作为 public value sink 但 `pc'=join(pc,condVis)` 进入分支；`match_` 语句 arm body 在 `pc'=join(pc,scrutVis)` 下；`assert_` condition 为 public sink（assert⇒failure.revert）且**不**抬高后续 PC；`for_` 端点仍为 public sink、body 保持当前 PC、binder=`public_`；嵌套 if/match 单调 join PC、出分支恢复外层 PC（参数传递）；每个既有显式 sink 的有效源=**joinVisibility(valueVis, currentPc)** 再 mayFlow；显式 sinks：assign→target、return/emit/revert/external call/schedule args→public_、for 端点→public_、localCall arg→对应 fn `ParamV1.visibility`（`tables.fn` + NameResolution/TypeCheck shadowing；arity mismatch 仅 walk nested）、const 定义表达式→public_；let 记 RHS 可见性（value flow）、match binder 继承 scrutinee（value flow）；稳定 `DiagnosticCodeV1.visibilityViolation` → wire `PF-VIS-001`（message `disclosure violation: cannot flow '<src>' into '<sink>'`，src 为 joined label）；duplicate fn 时 analysisComplete=false fail closed 并暴露 name-resolution structural 诊断；**已由产品 located `NormalizeV1` 经 `CheckV1` 门禁 fail-closed 消费**（disclosure 最终相位；PF-VIS-001保持结构化 DiagnosticV1；structure→type→effect→bound→disclosure）；**B7b3c** path drafts：sole draft authority + public erase；VisibilityEvidence；primary=sink input；related=causes∪PC∪sink site；**B7b3cR** PC/result control Expr root via `withControlRootEvidence`；**不做** authority/custody、`commit` 算子；alpha Semantic visibility→requirement inference 与 alpha IR lowering仅在显式 compatibility/tests保留，产品 import closure已禁止；formal pending | 新增 `Tests/Typed/DisclosureCheckV1.lean` 覆盖 public Counter 正向、private→private assign 正向、private→public assign/return/view-return/emit/revert/call/schedule/index 负向、localCall public-param 负向（含 discarded call）与 private→private param 正向、const private-state 与 const-return launder 负向、**match stmt/expr private scrutinee+public arms 负向（PC/result taint）** 与 binder leak 负向、public scrutinee 正向、**if private/commitment 条件 public return/assign/emit/revert/call/schedule 负向**、**private if 下 private→private 正向**、assert 不抬高后续 PC 正向、for 端点 private/commitment 负向与 public 正向、commitment lattice、shadowing、default=public、multi-error 源序、`renderHuman` `PF-VIS-001:`、duplicate-fn incomplete fail closed、private let 经 assign 泄漏；`Tests/Core/DiagnosticV1` 已登记 `PF-VIS-001` rank/wire；`Tests/Typed/CheckV1.lean` 固定 disclosure 相位、effect/bound 前于 PF-VIS-001 与 incomplete 不发明 VIS；`Tests/Typed/DiagnosticLocationsV1` B7b3c/B7b3cR 固定 Disclosure origin/NodeId/erase parity 与 if/match control Expr root related paths；`Tests/Compiler/CheckV1ProductGate.lean` 固定显式 private return 与 **implicit private-if return** 产品路径 PF-VIS-001；完整 formal `TST-VIS-002`（authority/custody 轴）仍未覆盖 | **新轨子集**（显式 + PC-label implicit disclosure flow pass + CheckV1 相位接线 + 聚焦测试；formal pending；产品 CheckV1 门禁已接线；authority/custody/`commit` 仍缺） | 继续补齐 authority/custody 分离与 formal TST-VIS-002 全覆盖，并评估 alpha Semantic visibility inference 删除；不伪称 formal D2-04 完成 |
| `TASK-D2-05` | pending | canonical ProgramRequirements inference、predicate merge与origin | 新增独立 `ProofForgeV2/Typed/RequirementsInferV1.lean`：直接消费 `ValidatedSourceV1`/`ProgramV1`，按 source-order first-seen `stableUnique` 产出 private-ctor `RequirementContributionV1` canonical id序列（不 import alpha Semantic）；规则覆盖 non-empty state⇒`.persistentState`、state visibility⇒`.privateState`/`.commitmentState`（从不借 privateWitness）、init/entry/view/fn param visibility⇒`.privateWitness`/`.commitmentDisclosure`、type carriers（bool/field bn254_fr；Option/Array/Map 递归）、arithmetic `+ - * / %` 与 unary `-`⇒`.checkedArithmetic`+`.transactionalRollback`、`Stmt.call`⇒`.synchronousCall`+rollback、`Stmt.schedule`⇒`.asynchronousWorkflow` only（alpha 无 schedule 规则；不发明 rollback）、`emit`⇒`.eventEmission`、`assert_`/`revert`⇒rollback；不发明 `.callerContext`/authority/custody/commit；pure AST walk 不依赖 call-graph，duplicate fn 仍 analysisComplete=true；由 `Semantic.RequirementsV1.freezeProgramRequirementsV1` sole consume并完成产品 requirements freeze；不直接接线 CheckV1/CLI，也不形成第二个 ProgramRequirements mint；alpha derive不再有产品 consumer；仍无 RequirementRef/predicate merge/origin provenance/SemVer/digest | 新增 `Tests/Typed/RequirementsInferV1.lean` 覆盖 Counter-like、private/commitment state、private param、emit/call/schedule、Bool、idempotent ids、与 `compileValidatedSourceV1` 在 S1 Counter/public-state 上的 semantic.requirements 精确 parity（private/commitment/call 仅 direct infer，因 S3 Normalize 门禁不再 full-compile）、exact `ProgramRequirement.id` wire；semantic/target suites 仍固定 alpha enum 集合 | **新轨子集 + 功能型 alpha**（独立 ProgramV1 infer pass + tests；仍 alpha ProgramRequirement carrier；formal pending） | S2 semantic path 已新增 `ProofForgeV2/Semantic/RequirementsV1.lean`：internalize Infer 规则为 pure ProgramV1 walk → closed `RequirementRequestV1` （三键 catalog、SemVer 1.0.0、engineering digest、empty predicates、SPEC wire 序），由 NormalizeV1 在 encode/hash 前 freeze；**不**返回 alpha ProgramRequirement、**不**接线 CheckV1/compile/CLI；alpha `RequirementsInferV1` 套件独立保留；formal RequirementRef/predicate merge/provenance CAP registry 仍 pending；接线后评估删除 alpha deriveRequirements |
| `TASK-D2-06` | pending | closed SemanticProgramV1 serializer/decoder、SemanticProvenanceV1、proof-bundle signature validation | 新增 owning `ProofForgeV2/Semantic/WireV1.lean`（**工程子集 skeleton + type-shape + valueBytes**）：closed model（IDs/Visibility/TypeShape/records/ops/terminators/entities/requirements/SemanticProgramDataV1/SemanticProvenanceV1/SemanticProgramV1 carrier）、schema/magic（`proof-forge.semantic-program.v1`/`pf.semantic.v1`、provenance companion）、nested tag tables 4.1–4.5、decode 资源上限；**tagged 共享 nesting fuel** `withTaggedNesting`/`maxNesting=256`（enter 当 nesting<256，leave 后恢复；非 tagged scalar 不耗 fuel；当前 model 无 recursive TypeShape-on-wire，fuel 仍走共享路径）；**API 契约**：`decodeSemanticProgramDataV1`=transport only；`encodeSemanticProgramDataV1` 输出前 structure gate；`decodeSemanticProgramV1`=transport→structure-gated re-encode→exact bytes（非法 structure 在 re-encode 路径以 structure error 或 `.nonCanonical` 失败，**不是** structure-free identity）；`validateSemanticProgramV1`/`semanticHashV1` 显式 structure+identity；structure 子集=table `id==index`、shallow TypeId/CallableId range、**SPEC §5 type-shape/FieldSpec/Map-key**（named iff struct|enum；nonempty struct/enum；widths {8,16,32,64,128,256}；Bytes/Array ≤4096；sole `bn254FrFieldSpecV1` id+exact modulusBE → `.badType`；intra-type field/variant name → `.duplicate`；Map key Bool|UInt|Int|Principal|Bytes|Struct-of-legal-keys fuel=`types.size` → `.badType`）、**SPEC §5 canonical valueBytes**（Constant/`Op.Literal`/`SwitchCase` 共享 type-driven validate；full-consume + encode(decode)==bytes → else `.nonCanonical`；Bool/UInt/Int/Principal/Unit/Bytes/Array/Map/Option/Field/Struct/Enum；Map keys unsigned-lex unique ascending；Field LE width `<` modulus；value nesting fuel=`maxNesting`；transport 仍 structure-free）、requirement key/domain + predicate name/rank/wire + `enumContains` 非空严格升序；**provenance**：encode/decode 为 envelope transport；Wire `*JoinV1` 与 Provenance `buildSourceNodeInventoryV1`/`buildSemanticProvenanceV1` 为 caller-trusted low-level helpers（`validateSourceNodeInventoryExactV1` 仅 hash+NodeId set）；权威仅 `NormalizeV1.validateSemanticProvenanceV1`/`semanticProvenanceDigestV1`/`normalizeProgramWithProvenanceV1`（source+path+spans 内部 rebuild inventory，不接受 caller inventory；carrier re-normalize + exact originMap；coordinated path/span mutation fail closed）；formal CAP registry 与 full-surface attribution 仍 pending；**S3/B8b product gate**：`Compiler.compileProgramProductV1` 唯一产品路径经 `NormalizeV1.normalizeProgramLocatedV1` 返回 structure-gated retained `SemanticProgramV1`，随后 sole mint private `CompiledSemanticV1`；`compileValidatedSourceV1` 仅非产品兼容；resolver、四 target Plan/IR与artifact identity均只读 retained semantic/canonical Digests，无 alpha parity/fallback/dual reader/adapter；Wire suite仍独立；**alpha `Core/SemanticIR.lean` 仅为隔离 compatibility/tests保留**；formal TASK-D2-06 / SemanticProgramV1 materializer cutover / D3 仍 pending | 新增 `Tests/Semantic/WireV1.lean`：schema/magic、empty/minimal Type.Bool round-trip、wrong magic/tag/fieldCount/trailing/mutated、hash identity、structure 负例（id≠index `.duplicate`、shallow bad TypeId/CallableId `.badReference`）、**type-shape 正/负（legal widths/lengths/named nonempty struct·enum/exact bn254 FieldSpec/Map Bool·UInt·Bytes·Struct-of-UInt keys；name=some on bool/name=none on struct/empty struct·enum/uint7/bytes·array 4097/bad FieldSpec id/zero modulus/dup field·variant/Map Option·Array·Unit·Enum key、Map Struct-of-Option/Field/Map key structure+encode）**、**valueBytes 正/负（Bool/UInt8·64/Int8/Unit/Bytes0·N/Option none·some/Field 0·p-1/Struct two UInt8/Enum empty·payload/Map empty·sorted/Array u8×2 + Op.Literal/SwitchCase；Bool 02/wrong-len/Option 02/Field≥p/Map unsorted·dup·keyLen/Enum OOB/trailing/truncated → `.nonCanonical` structure+encode；hand-built transport garbage valueBytes still ok）**、requirements domain/order/dup/digest-tie、predicate name/rank order、enumContains empty/unsorted/dup 与正向、nesting at maxNesting/max-1 与 nested FieldSpec fuel（zero-modulus Field shape encode 仍合法）、provenance envelope transport + join-gated digest + incomplete/foreign validate negatives（完整正向在 Normalize suite）、Digest raw-32、invalid carrier invariants=`#[]`、**CFG shape + reachability（`testCfgShapeAndReachability`：5 正/6 负，structure+encode 双路径）**、**block-param arg arity count portion（`testCfgBlockParamArity`：jump/branch/switch target `args.size == params.size` 4 正/6 负含 OOR 归 range check，helpers `cfgBlockWithParams`/`cfgJumpTargetWithArgs`/`cfgBoolParam`，block params typeId 0 Bool）**、**loopBounds back-edge coverage（`testCfgLoopBounds`：6 正/10 负，structure+encode 双路径，全 `.badCfg`；helper `cfgLoopBound`/`cfgCallable` 增 optional `loopBounds` 默认 `#[]`；self back-edge 0→0、两块 1→0、嵌套+forward tail、maxIter 0 合法、空 loopBounds 无 back-edge、双 back-edge 升序；missing/extra/wrong header·backEdgeFrom/maxIter 4097/dup pair/unsorted/header·backEdgeFrom OOR/单块 return 无 self edge 负例）**；**不是** formal TST-SEM-001 full corpus / cross-implementation / proof-bundle / CFG/TypeKey | **功能型 alpha + 新轨子集 skeleton**；**narrow public-UInt64 normalizer 工程子集** + **S2 requirements freeze + provenance join**：owning `ProofForgeV2/Semantic/NormalizeV1.lean`：ValidatedSourceV1 + CheckV1 ok gate → single-block public UInt64/Unit ProgramV1 子集（含 expected-type UInt64 literal与 checked add/sub + exact provenance origin）→ SemanticProgramDataV1 → sole `encodeSemanticProgramDataV1` carrier；S2 freezes exact ProgramRequirementsV1 + optional ProvenanceV1 companion；suite 在 `Tests/Typed/CheckV1.lean` namespace `Tests.Semantic.NormalizeV1`（`CheckV1.run` 调用，ordinary CI 覆盖）；**S3/B8b**：product `compileProgramProductV1` 已经由 located Normalize structure gate并返回 sole `CompiledSemanticV1`；`compileValidatedSourceV1` 仅非产品兼容；四 target Plan body、resolver与artifact identity均只读 retained semantic/canonical Digests；formal TASK-D2-06 / proof-bundle join /完整 SemanticProgramV1 lowering仍 pending | 继续补 TypeKey anonymous ranking/interning、contained-frontend inventory fidelity / entity-specific origins、full ProgramV1→Semantic normalizer beyond the current public-UInt64 envelope、formal SemanticProgramV1 materializer identity/output contract，并在 explicit compatibility tests可迁后物理删除 alpha IR；**CFG shape + reachability foundation 已加入 structure gate**（per-callable entryBlock==0、block id==index、terminator target range、从 entry 的 total reachability，全部 `.badCfg`；**block-param arg arity（count portion：jump/branch/switch target `args.size == target block params.size`，仅 in-range target，OOR 仍归 range check）已加入 step c.5**、**loopBounds back-edge coverage（exact coverage of every CFG back edge，`(header,backEdgeFrom)` 唯一升序、`maxIterations ≤ 4096`、range 归此处 `.badCfg`）已加入 step e）**、**SPEC §6 per-callable canonical ValueId assignment + use-existence 已加入 step f**（`collectValueDefSites` 按 callable params→全部 block params（BlockId order）→全部 instruction results（BlockId/instruction order）三个全局 pass；`checkValueIdCanonicalAssignment` 线性要求 exact `0..n-1`，wrong-start/gap/duplicate/reorder 均 `.badCfg`；`collectValueTypeDefs` 同序；`testCfgValueIdCanonicalAssignment` 固定 global-category/multi-block/per-callable reset、旧 interleaving/reorder/gap/sparse、phase boundary、carrier/transport 边界，`testCfgValueIdSsa` 保留 duplicate/use-existence 回归）、**dominance-of-use 已加入 step g**（`cfgPredecessors`/`computeDominators` 迭代 dataflow dom[b]={b}∪(∩ reachable preds dom[p])、`checkDominanceOfUse`/`validateCallableDominanceOfUse`，每个 use 必须在 dominated-by-def-block，全 `.badCfg`；`testCfgDominanceOfUse` DP1–DP5 正/DN1–DN3 负，negative 仅 dominance 失败、reachability/arity/loopBounds/SSA def-table 全通过），全部 `.badCfg`；**def-site TypeId range 已加入 step h**（`collectValueTypeDefs`/`checkDefSiteTypeIdsInRange`：block params + instruction result ValueDef TypeIds 必须 in `[0, types.size)`，callable param/result TypeIds 已由 step 2 覆盖不重复，失败 `.badReference`）、**terminator typing 已加入 step i**（`boolTypeId`/`checkTerminatorTyping`：branch cond==Bool TypeId、switch case.typeId==typeOf(scrut)、jump/branch/switch target args positional type==target block params[i].typeId（min-length guard total，arity 归 step c.5）、return (some v) type==callable.result.typeId；return none/revert/trap 不检查；全 `.badCfg`；`testCfgBlockParamTypeAndTerminatorTyping` P1–P5 正/N1–N8 负，N1·N2 `.badReference`、N3–N8 `.badCfg`，structure+encode 双路径），**per-op §5.1 type/result contract (step j) 已加入 value-producing ops（literal/constant/stateLoad/construct/fieldGet/indexGet/unary/binary/pureCall）：`checkOpTyping` 复用 step h `defTypes`，`uint32TypeId`/`uint8TypeId`/`optionTypeId` shape-resolving helper 与 `serializableType` predicate（eq/ne operand 合法性：bool/uint/int/principal/bytes/field/struct·enum-of-serializable，非 array/map/option/unit）；construct 解析 Struct/Enum/Array/Option/Unit/empty-Map shape（primitives/Bytes/Principal/Field/uint/int/bool reject）、indexGet 解析 Array/Bytes/Map（UInt32 index、UInt8/Option<value> result）、unary 解析 neg(Int|Field)/not(Bool)/bitNot(UInt|Int)、binary 解析 arithmetic(UInt/Int，Field 仅 add/sub/mul/div 非 mod)/eq-ne(serializable→Bool)/lt-le-gt-ge(UInt/Int→Bool)/and-or(Bool→Bool)/bitAnd-bitOr-bitXor(UInt/Int)/shl-shr(lhs UInt/Int, rhs UInt32)、pureCall 检查 callee.kind==.pureFn 与 arg count/type、result==callee.result.typeId；**void-op result-presence (step j extension) 已加入：StateStore/Assert/Emit/ExternalCall/Schedule 的 `Instruction.result` 必须为 `none`，spurious `result := some _` → `.badCfg`（不查 event/error/effectId range 或 argument typing；`testCfgVoidOpResultPresence` P1–P5 正/N1–N5 负 structure+encode 双路径）**；**value-producing result-presence (step j extension) 已加入：每个 value-producing op（Literal/Constant/StateLoad/Construct/FieldGet/IndexGet/Unary/Binary/PureCall/FieldSet/VariantTag/VariantPayload/IndexSet/CheckedCast/ContextRead/Commit）必须 `Instruction.result := some _`，缺失 → `.badCfg`；typed 9 family 继续要求 result.typeId == op exact result type，7 deferred family 仅 presence-only、exact input/result typing 仍跳过（`testCfgValueOpResultPresence` P1–P16 正/N1–N16 负 structure+encode 双路径 + 5 个 void result-none 回归）**；`testCfgOpTyping` P1–P9 正/N1–N12 负 structure+encode 双路径，全 `.badCfg`**；**Op.FieldSet exact §5.1 contract (step j extension) 已加入：base ValueId type 必须解析为 Struct、fieldIndex 必须在 range、type(value) 必须精确等于 fields[fieldIndex].typeId、result 必须存在且 result.typeId 必须精确等于 type(base)，全 `.badCfg`；FieldSet 从 7 deferred family 中独立，余 6 family（VariantTag/VariantPayload/IndexSet/CheckedCast/ContextRead/Commit）仍 presence-only；`testCfgFieldSetTyping` P1（first field）/P2（later field）正与 N1（non-Struct base）/N2（OOR fieldIndex）/N3（wrong value type）/N4（wrong result type）负 structure+encode 双路径，各 negative 隔离 FieldSet typing；`PresP10FieldSet`/`PresN10FieldSet` 已改为合法 Struct base 隔离 presence**，全部 `.badCfg`/`.badReference`；**Op.VariantTag exact §5.1 contract (step j extension) 已加入：base ValueId type 必须解析为 Enum 或 Option、`uint32TypeId` 必须解析到唯一 UInt32 TypeId、result 必须存在且 result.typeId 必须精确等于该 UInt32 TypeId，全 `.badCfg`；VariantTag 从 6 deferred family 中独立，余 5 family（VariantPayload/IndexSet/CheckedCast/ContextRead/Commit）仍 presence-only；`uint32TypeId` 改为 uniqueness-gated（仅当恰好一个 `.uint 32` 声明时返回 `some`，重复匿名 UInt32 声明返回 `none` 被 VariantTag 拒绝，作为 TypeKey 结构化 interning 仍 pending 下的保守 fail-closed 替代）；`testCfgVariantTagTyping` P1（Enum base）/P2（Option-some base）/P3（Option-none base）正与 N1（primitive base）/N2（no UInt32 closure type）/N3（wrong result type）/N4（duplicate UInt32 closure type）负 structure+encode 双路径，各 negative 隔离 VariantTag typing；`PresP11VariantTag`/`PresN11VariantTag` 已改为合法 Enum base 隔离 presence**；**Op.VariantPayload static exact §5.1 contract (step j extension) 已加入：Enum base 要求 variantIndex/payloadIndex in range 且 result.typeId == selected payload TypeId，Option base 只允许 `(1,0)` 且 result.typeId == element TypeId；runtime Enum tag agreement 本切片不检查，D2-07 interpreter 尚未实现/接线且届时必须 trap；`testCfgVariantPayloadTyping` P1（Enum payload）/P2（Option-some）正与 N1（primitive base）/N2（variant OOR）/N3（payload OOR）/N4（Option-none）/N5（Option payload OOR）/N6（wrong result）/N7（empty variant payload）负 structure+encode 双路径；`PresP12VariantPayload`/`PresN12VariantPayload` 已改为合法 Option-some base 隔离 presence**；**Op.IndexSet exact static §5.1 contract 已加入：Array/Bytes index 必须为 UInt32、Array value=element、Bytes value=UInt8、Map key/value exact，result.typeId 必须等于 type(base)；runtime Array/Bytes bounds 留 D2-07 且尚未实现；`testCfgIndexSetTyping` P1 Array/P2 Bytes/P3 Map 正与 N1 primitive base/N2 Array index/N3 Array value/N4 Bytes index/N5 Bytes value/N6 Map key/N7 Map value/N8 result type/N9 duplicate UInt8 closure 负 structure+encode 双路径；`uint8TypeId` 改为 uniqueness-gated（重复 `.uint 8` fail closed，同时收紧 Bytes IndexGet/IndexSet）；`PresP13IndexSet`/`PresN13IndexSet` 改为合法 Map base 隔离 presence**；**Op.CheckedCast exact static §5.1 contract 已加入：source 与 `toType` 均必须解析为 UInt/Int，result.typeId 必须精确等于 `toType`；运行值 representability 留给尚未实现的 D2-07 checked-revert；`testCfgCheckedCastTyping` P1 UInt→UInt/P2 UInt→Int/P3 Int→UInt/P4 Int→Int 正与 N1 Bool source/N2 Bytes source/N3 Bool destination/N4 Bytes destination/N5 wrong result/N6 missing destination 负 structure+encode 双路径**；**Op.StateStore exact static §5.1 contract 已加入：stateId 必须存在、type(value) 必须精确等于 selected state.typeId、result 必须为 none；`testCfgStateStoreTyping` P1 UInt8/P2 Bool/P3 second-state selection 正与 N1 missing state/N2 wrong value/N3 wrong selected-state type 负 structure+encode 双路径，既有 void presence fixture 同步改为类型合法**；**Op.Assert exact static §5.1/§6 contract 已加入：condition 必须 Bool，`errorId=none` 要求 args empty，`some errorId` 必须存在且 args 按 ErrorDecl fields positional exact，result 必须为 none；`testCfgAssertTyping` P1 standard/P2 declared two-arg/P3 second-error selection 正与 N1 condition/N2 none+args/N3 missing error/N4 arity/N5 arg type/N6 selected error type 负 structure+encode 双路径**；**Term.Revert exact §6 ErrorDecl join 已加入：errorId 必须存在，args 按 selected ErrorDecl fields positional exact；`checkTerminatorTyping` 显式消费 errors table；`testCfgRevertTyping` P1 empty/P2 two-arg/P3 second-error selection 正与 N1 missing error/N2 arity/N3 arg type/N4 selected error type/N5 reversed two-arg positional order 负 structure+encode 双路径**；**Op.Emit exact §5.1 EventDecl join 已加入：eventId 必须存在，args 按 selected EventDecl fields positional exact，result 必须 none；`testCfgEmitTyping` P1 empty/P2 two-arg/P3 second-event selection 正与 N1 missing event/N2 arity/N3 arg type/N4 selected event type/N5 reversed two-arg positional order 负 structure+encode 双路径；既有 void presence Emit fixtures 改为合法 empty EventDecl**；**§6 per-callable EffectId canonical assignment 已加入：Emit/ExternalCall/Schedule 按 BlockId/instruction order 从 0 连续编号、每 callable 重置，gap/duplicate/wrong start/reorder 均 `.badCfg`；`testCfgEffectIdOrder` P1 三 effect family/P2 跨 block/P3 per-callable reset 正与 N1 starts-at-one/N2 duplicate/N3 gap/N4 block-order restart 负 structure+encode 双路径**；**ExternalCall/Schedule callee qualified-shape gate 已加入：callee 必须至少两个 components，`testCfgExternalCalleeShape` P1/P2 双 component、P3 三 component 正与 N1/N2 单 component 负 structure+encode 双路径；argument serializability 因允许 TypeShape 集合仍有规范歧义而 deferred，不复用 Eq/Ne 专用 `serializableType`**；**program root qualifiedName shape gate 已加入：SemanticProgram root 必须至少两个 components，复用 helper 在 encoder table-size 前与 structure step 0 检查，使单 component 对 table-size/table-id 等 mixed-invalid 保持 `.badScalar` 优先；transport decoder 保持 transport-only，carrier re-encode fail closed；`testProgramQualifiedNameShape` 固定双/三 component 正向、单 component structure/encode/carrier 负向及 table-id/table-size precedence**；**Term.Switch canonical case-shape gate 已加入 CFG step b.5：cases 必须 nonempty，零 case（有/无 default）均 `.badCfg`，必须由 normalizer 物化为 Jump；`testCfgSwitchCasesNonempty` 固定单 case 有/无 default 正向与零 case 负向，SSA undefined-switch fixture 已迁为 nonempty case 隔离 use-existence**；**Term.Switch typed case-value uniqueness 已加入 CFG step b.6：每个 Switch 内 `(typeId,valueBytes)` 必须唯一，target/args 不参与 key；实现以 typed raw key private qsort + adjacent compare 避免 quadratic scan且不改 public case order；`testCfgSwitchCaseValueUniqueness` 固定 Bool/UInt8 distinct、跨两个 reachable Switch 同 key 重用正向，以及同/异 target、异 target args duplicate 负向与 malformed duplicate `.nonCanonical` phase precedence**；**callable kind/name presence 已加入 signature step 4.25：initializer 仅 `name=none`，entry/view/pureFn/invariant 仅 `name=some`，在 canonical values 后、CFG 前失败 `.badCfg`；`testCallableKindNamePresence` 固定五 kind 正向、逐 kind wrong Option shape 负向及 canonical-value-before-signature/signature-before-CFG mixed-invalid phase precedence；name grammar 由后述 shared structure gate 统一检查**；**named callable exact-string uniqueness 已加入 signature step 4.25：所有 `some name` 在 unified callable table 内必须唯一，initializer `none` 不参与；private UTF-8 qsort + adjacent exact equality 不改 source order，重复 `.badCfg`；`testCallableNameUniqueness` 固定五 kind distinct、case-sensitive 正向、same-kind/cross-kind duplicate 负向及 shallow-ref→canonical-value→uniqueness→CFG precedence；identifier grammar 由后述 shared structure gate 统一检查**；**per-callable parameter-name exact-string uniqueness 已加入 signature step 4.25：每个 callable 独立 private sort，重复 parameter name `.badCfg`，不同 callables 可复用同名；`testCallableParameterNameUniqueness` 固定 distinct/per-callable reset/case-sensitive 正向、pureFn/entry duplicate 负向及 shallow-ref→canonical-value→uniqueness→CFG precedence；parameter grammar 由后述 shared structure gate 统一检查**；**named TypeDecl exact-string uniqueness 已加入 type-shape step 3：收集 Struct/Enum 的 `some name` 并复用 shared private UTF-8 qsort + adjacent exact equality checker，重复 `.duplicate` 且不改 types source order；`testNamedTypeNameUniqueness` 固定 anonymous-only、Struct/Enum distinct/case-sensitive、exact wire source-order round-trip正向、cross-shape duplicate 负向及 shallow-ref→type-shape→named-type-name→canonical Constant/Op.Literal/SwitchCase valueBytes→callable-signature→CFG precedence；identifier grammar 由后述 shared structure gate 统一检查；anonymous canonical rank/order、usage closure与完整 TypeKey closure仍 deferred**；**Constant-name exact-string uniqueness 已加入 name step 4.2：constants table 内将 names 复制到 private array，以 UTF-8 qsort + adjacent exact equality 去重，重复 `.duplicate` 且不改 source order；`testConstantNameUniqueness` 固定 empty/distinct/case-sensitive 正向、duplicate 负向及 shallow-ref→canonical Constant/callable valueBytes→Constant-name→callable-signature→CFG precedence；Constant identifier grammar 由后述 shared structure gate 统一检查**；**logicalState StateDecl-name exact-string uniqueness 已加入 name step 4.2：与 Constant-name 同属不暴露组内 rank 的 same-error `.duplicate` phase；logicalState table 内 names 复用 shared private UTF-8 qsort + adjacent exact equality checker，重复 `.duplicate` 且不改 source order；`testLogicalStateNameUniqueness` 固定 empty/distinct/case-sensitive 正向、duplicate 负向及 shallow-ref→canonical Constant/callable valueBytes→logicalState-name→callable-signature→CFG precedence；StateDecl identifier grammar 由后述 shared structure gate 统一检查**；**events EventDecl-name exact-string uniqueness 已加入 name step 4.2：复用 shared private UTF-8 qsort + adjacent exact equality checker，并与 Constant/StateDecl/per-declaration interface-field names 同属不暴露组内 rank 的 same-error `.duplicate` phase；`testEventNameUniqueness` 固定 empty/distinct/case-sensitive、exact wire source-order round-trip正向、duplicate 负向及 shallow-ref→canonical Constant/Op.Literal/SwitchCase valueBytes→duplicate-name phase→callable-signature→CFG precedence；EventDecl identifier grammar 由后述 shared structure gate 统一检查**；**errors ErrorDecl-name exact-string uniqueness 已加入 name step 4.2：复用 shared private UTF-8 qsort + adjacent exact equality checker，并与 Constant/StateDecl/EventDecl/per-declaration interface-field names 同属不暴露组内 rank 的 same-error `.duplicate` phase；`testErrorNameUniqueness` 固定 empty/distinct/case-sensitive、exact wire source-order round-trip正向、duplicate 负向及 shallow-ref→canonical Constant/Op.Literal/SwitchCase valueBytes→duplicate-name phase→callable-signature→CFG precedence；ErrorDecl identifier grammar 由后述 shared structure gate 统一检查**；**event/error interface-field exact-string uniqueness 已加入 name step 4.25：每个 EventDecl/ErrorDecl 将 field names 复制到 private array，以 UTF-8 qsort + adjacent exact equality 去重，重复 field name `.duplicate`，跨 declaration 与跨 event/error kind 可复用同名且不改 source order；`testInterfaceFieldNameUniqueness` 固定 distinct/per-declaration reset/case-sensitive 正向、event/error duplicate 负向及 shallow-ref→canonical-value→field-uniqueness→callable-signature→CFG precedence；interface identifier grammar 由后述 shared structure gate 统一检查**；**initializer signature step 4.3 已闭合 cardinality + result shape：source-order 允许零或一个 initializer，第二个 initializer `.badCfg`；每个 initializer result TypeId 必须解析为 Unit 且 visibility 必须为 public，否则 `.badCfg`；两项均在 CFG 前检查；`testInitializerCardinality` 固定 0/1、initializer+entry、double initializer 与 later-CFG mixed-invalid，`testInitializerResultShape` 固定无 initializer/Unit-public 正向、Bool/private/commitment 负向及 shallow-ref→canonical-value→signature→CFG phase precedence**；**invariant callable result shape 已加入同一 signature phase：每个 `.invariant` result TypeId 必须解析为 Bool 且 visibility 必须为 public，否则 `.badCfg`，非 invariant callable 不受该 gate 影响；`testInvariantResultShape` 固定 Bool-public 与其他 kind 正向、Unit/private/commitment 负向，以及 shallow-ref→canonical-value→signature→CFG phase precedence；closure/op/fuel 与 `invariantSteps` 仍 deferred**；**invariant zero-parameter signature 已加入同一 phase：仅 `.invariant` callable 要求 `params.isEmpty`，非 invariant callable 不受影响，违规 `.badCfg`；`testInvariantParameterShape` 固定 initializer/entry/view/pureFn 单参数与 invariant 零参数正向、invariant 非空参数负向，以及 shallow param-ref→canonical-value→signature→CFG mixed-invalid precedence；closure/op/fuel 与 `invariantSteps` 仍 deferred**；**InvariantDecl exact join 已加入 signature phase：`.invariant` callables 按过滤后的 callable source order 与 `invariants` rows 一一对应，row `callableId`、referenced kind 与 name 必须 exact；missing/extra/wrong-kind/name/reordered/duplicate binding 均 `.badCfg`；`testInvariantDeclarationJoin` 固定 empty/one/mixed-callable source-order 正向及 range→canonical-value→join→CFG phase precedence；closure/fuel 与 `invariantSteps` 仍 deferred**；**invariant root `loopBounds=[]` gate 已加入 signature phase：`.invariant` 禁止任何 static loop bound，其他四种 callable 仍按 generic exact bounded-loop CFG contract；`testInvariantLoopBoundsShape` 固定 initializer/entry/view/pureFn 合法 self-back-edge 正向、invariant empty 正向、invariant exact bounded back-edge 负向及 canonical-value→signature→CFG precedence；完整 closure DAG/op/fuel 与 exact `invariantSteps` 仍 deferred**；**non-closure-by-kind `invariantSteps` absence 已加入 signature phase：initializer/entry/view 不可能成为 invariant root 或 `Op.PureCall` callee，必须携带 `none`，任意 `some` 均 `.badCfg`；`testNonClosureCallableInvariantSteps` 固定三 kind `none` 正向、实际 pureFn→invariant closure exact steps 3/6 的 `some` scope 正向、`some 1`/`some 0`/`some UInt64.max` 负向，以及 canonical-value→fuel-metadata→CFG def-site 的可区分 precedence；pureFn closure membership、invariant root exact step computation、DAG/op closure 与 10M ceiling 仍 deferred**；**rootless pureFn `invariantSteps` absence 已加入同一 signature phase：若 callables 中没有 `.invariant` root，则每个 `.pureFn` 都可证明不属于任何 invariant closure，必须携带 `none`，任意 `some` 均 `.badCfg`；`testNonClosureCallableInvariantSteps` 新增 rootless pureFn `none` 正向、`some` 负向及 canonical-value→rootless-fuel→CFG def-site 可区分 precedence，并保留实际 pureFn→invariant closure steps 3/6 的 `some` 正向防止 scope broadening；有 root 时未被调用 pureFn 的 membership、exact computation、DAG/op closure 与 10M ceiling 仍 deferred**；**invariant root `invariantSteps` presence 已加入 signature phase：每个 `.invariant` callable 必须携带 `some`，`none` 以 `.badCfg` fail closed；本切片只验证 presence，不验证数值。`testInvariantRootStepsPresence` 固定单 literal root 的 exact `some 3` 正向、`none` 负向及 canonical-value→root-fuel→CFG def-site 可区分 precedence；通用 `cfgCallableKindName` 对 simple invariant root 物化 exact `some 2` 以迁移既有 signature suites；pureFn membership、exact step computation、DAG/op closure 与 checked accumulation 仍 deferred**；**`invariantSteps` intrinsic 10M ceiling 已加入 post-CFG/pre-requirements phase：新增 public `maxInvariantStepsV1=10000000`，对所有 present metadata（invariant root 与 pureFn closure member）要求 `≤` ceiling，超界 `.badCfg`；`testInvariantStepsIntrinsicCeiling` 固定 exact simple root 正向、ceiling equality 穿透至 later `.badRequirement`、root/pureFn 10000001 负向，以及 canonical-value→CFG→ceiling→requirements 可区分 precedence；exact computation、membership、DAG/op closure 与 checked accumulation 仍 deferred**；**invariant root direct forbidden-op subset 已加入 post-CFG closure phase：generic `checkOpTyping` 先运行，随后 `validateInvariantRootDirectOpsV1` 仅扫描 `.invariant` body 并拒绝 direct `Op.StateStore`/`Op.ContextRead`/`Op.Commit`/`Op.Emit`/`Op.ExternalCall`/`Op.Schedule` `.badCfg`；StateLoad 与对应 entry op 保持正向。`testInvariantRootStateStoreProhibited`/`testInvariantRootContextReadProhibited`/`testInvariantRootCommitProhibited`/`testInvariantRootEmitProhibited`/`testInvariantRootExternalCallProhibited`/`testInvariantRootScheduleProhibited` 固定双路径 scope、CFG/op typing→closure restriction→intrinsic fuel→requirements precedence；non-wire `validateCfgInvariantPhasesV1` 由真实 structure gate 消费并保留 phase observation，使共享 `.badCfg` 的相邻 phase 顺序可测试；**exact transitive pureFn closure membership metadata gate 已加入同一 post-CFG phase**：从 invariant roots 沿 `Op.PureCall` 做 bounded worklist reachability，每 callable 最多入队一次；pureFn iff closure member 时要求 `invariantSteps=some`，否则要求 `none`；generic CFG/op typing→membership→intrinsic fuel→requirements precedence，以及多跳、双 root disjoint closure，由 `testInvariantPureFnClosureMembership` 双路径固定；**reachable invariant-closure `Op.PureCall` call-graph DAG gate 已加入**：复用 exact membership，以 bounded Kahn indegree traversal 拒绝 reachable self/2-node cycle，duplicate static edges 独立计数，unreachable pureFn cycle 不受本 gate 影响；`testInvariantPureFnClosureDag` 固定 acyclic/duplicate-static-edge/unreachable-cycle 正向、self/two-cycle 负向及 CFG→DAG→fuel precedence；**invariant-closure callable CFG no-back-edge gate 已加入**：复用 exact membership 与 canonical `cfgBackEdges`，在 generic loopBounds exact coverage 后拒绝任何 reachable closure-member back edge，unreachable pureFn bounded loop 保留 generic 正向；`testInvariantClosureCfgBackEdges` 固定 acyclic/unreachable-loop 正向、direct/transitive reachable loop 负向及 CFG→closure-CFG→fuel precedence；**invariant-closure pureFn forbidden-op allowlist 已闭合**：exact reachable closure pureFn 内 `.stateLoad`/`.stateStore`/`.contextRead`/`.commit`/`.emit`/`.externalCall`/`.schedule` 均拒绝，invariant root direct StateLoad 与 unreachable pureFn 对应 ops 保持相应正向，generic op typing 先于 closure restriction；`testInvariantClosurePureFnStateLoadProhibited`/`testInvariantClosurePureFnStateStoreProhibited`/`testInvariantClosurePureFnContextReadProhibited`/`testInvariantClosurePureFnCommitProhibited`/`testInvariantClosurePureFnEmitProhibited`/`testInvariantClosurePureFnExternalCallProhibited`/`testInvariantClosurePureFnScheduleProhibited` 固定 direct/transitive reachable 负向、unreachable 正向及 CFG→closure→fuel→requirements precedence；**exact checked `computedInvariantSteps` 已加入**：在 closure DAG/CFG/op restrictions 后以 reverse Kahn leaf→caller 线性传播，精确计算 `1 + Σ(block.instructions.size + 1) + Σ(each static PureCall occurrence calleeSteps)`，duplicate call occurrence 独立计数，所有加法在 schema 10M ceiling 下 checked fail-closed，carried metadata 必须逐 callable exact 相等；`testInvariantStepsExactComputation` 固定 leaf/direct/duplicate/multi-block 正向、callee/root/duplicate undercount 负向、computed>10M 与 CFG→closure→fuel→requirements phase；interpreter 仍 deferred**；named TypeDecl contiguous-prefix rank 已加入 structure step 3（全部 `name=some` Struct/Enum 必须占 `types[0..namedCount)`，interleaving → `.nonCanonical`；transport reader仍 structure-free，carrier/encoder fail closed）；primitive anonymous TypeKey leaf exact-shape uniqueness 随后接线（Bool/同宽 UInt/同宽 Int/Principal/Unit/同长度 Bytes/exact FieldSpec duplicate → `.nonCanonical`）；recursive anonymous Array/Map/Option structural-class uniqueness 与 no-named-anchor anonymous-cycle rejection 已再后接线（iterative DFS + fixed-size signature/class-ID HashMap interning，lookup/insert only，无 nested-key expansion；10k acyclic Option chain双 shipped-path覆盖）；named-body Option-cycle legality 已接线（移除 Option nodes/incident edges后以 explicit-stack DFS要求 induced TypeId graph acyclic，结合 earlier no-named-anchor anonymous-cycle rejection闭合完整 cycle condition；`TypeKeyValidationPhaseV1` 顺序 namedPrefix→primitive→recursive→namedBodyCycle；5k named chain与1k-variant Enum固定 O(V+E)资源边界）；ContextRead same-key result-TypeId consistency 已接线（全program exact SchemaId lookup/insert，一key一result TypeId，mismatch `.badCfg`，generic CFG后/invariant closure前；raw transport保持structure-free，2k occurrence资源覆盖）；**SPEC §6 declaration/field/parameter/invariant name gate 已加入 structure step 4.4**：`Core.Common.validateIdentifierComponent` 统一 Unicode 17 NFC、UTF-8 1..240、非 exact `_`、`Lean.isIdFirst`/`Lean.isIdRest`；覆盖 named type/field/variant、constant/state/event/error及field、callable/param、InvariantDecl，initializer `none` 保留；uniqueness/join→name `.badScalar`→CFG/requirements 相位固定；transport 仅 NFC，structure/encode/carrier 三路与 mixed-invalid tests 已固定；**非** formal TASK-D2-06/TST-SEM-001；**SPEC §6 aggregate callable entry/view presence gate 已加入 signature step 4.25 后、initializer/invariant signature 前：`callables` 必须至少一个 `.entry`/`.view`，否则 `.badCfg`；O(callables)/O(1) source-order scan，`decodeSemanticProgramDataV1` transport 仍 permissive；`testCallableEntryViewPresence` 固定 entry only/view only/initializer+entry/pureFn+invariant+later view 正向、zero callables/initializer only/pureFn only/invariant only/initializer+pureFn+invariant 负向（structure+encode 双路径 `.badCfg`）、kind-name/duplicate-name/duplicate-param before aggregate 与 absence-before-dup-initializer/bad-invariant-sig/generic-CFG/bad-requirements 稳定相位、hand-built zero-callable envelope transport accept + structure/encode/carrier reject；既有 valid/later-phase structure-gated fixture 已迁移（`programWithTypes`/`programWithState`/`programWithEvents`/`programWithErrors` 追加 minimal `entry_gate` callable，相关正向结构测试改用 `minimalValidProgram`）；`emptyProgram` 仍用于 raw transport fixture 与故意在新 aggregate gate 前由更早 structure phase 拒绝的负例**；anonymous canonical ranking/reachability/usage closure、provenance join、full normalizer beyond S1 Counter-like NormalizeV1、ExternalCall/Schedule argument serializability、ContextRead requirement-to-result binding/key capability support与Commit exact disclosure contract 仍 'Not yet'；**S3/B8b engineering：`compileProgramProductV1` 已经 `normalizeProgramLocatedV1` structure gate并返回 sole `CompiledSemanticV1`，`compileValidatedSourceV1` 仅非产品兼容；resolver、四 target Plan/IR与artifact identity只读 retained semantic/canonical Digests；Normalize 拒绝 fail closed，无 alpha-only fallback**；formal TASK-D2-06/D3 仍 pending；不伪称 formal D2-06 完成 |
| `TASK-D2-07` | pending | reference step interpreter：Invocation/context/responses/Outcome/effect/rollback | **工程子集（非 formal `step`）**：新增 owning `ProofForgeV2/Semantic/InvariantABI.lean`（`LogicalStateV1`、type-driven `defaultValueV1`、`initialLogicalStateV1`、`stateConformsBoolV1`/`StateConformsV1`、length-prefixed slot encode/decode 经 `validateValueBytesV1`；**不**实现 `evalInvariantV1`/`InvariantTheoremV1`）与 `ProofForgeV2/Semantic/ReferenceV1.lean` admitted primitive/effect 参考机：closed runtime carriers（`ReferenceValueV1`/`InvocationV1`/`ExternalResponsesV1`/`OrderedEffectV1`/`OutcomeV1`/`StandardRevertCodeV1`/`SemanticFaultV1`）、`admitReferenceProgramSliceV1`（structure-validated carrier + admitted Bool/UInt8·32·64/Unit/Bytes 与 Literal/Constant/StateLoad·Store/Unary/Binary/CheckedCast/Assert/Emit/ExternalCall/Schedule + Jump/Branch/Switch/Return/Revert/Trap；拒绝 Int/Field/Principal/aggregates/index/Construct/Field*/Variant*/PureCall/ContextRead/Commit 及 view/pureFn 非法 effect，错误与 Outcome 分离）、`stepReferenceSliceV1`（dense env、state overlay、per-EffectId occurrence、external response cursor/terminal exhaustion、ordered provisional effects、fuel、loop-bound counters；仅 `.returned` 提交 overlay/effects，revert/trap 保持 pre 字节一致）；**直接消费** validated `SemanticProgramV1`，**不**走 alpha `Core/Semantics`/`SemanticIR`；**不**导出正式 `step`；umbrella `ProofForgeV2.lean` 在 WireV1 后 import 两模块；**残余 alpha** `Core/Semantics.lean` 仍保留；**无** product CLI/materializer 接线。缺口：PureCall/context/commit、Int/Field/Principal/aggregates/index、general loop/resource profile、`evalInvariant`/`InvariantTheorem`、target differential/full formal corpus | 新增 `Tests/Semantic/InvariantABI.lean`（全 TypeShape 默认值、init flag、conforms、decode/encode roundtrip/negatives、determinism）与 `Tests/Semantic/ReferenceV1.lean`（S1 Normalize Counter init/entry/view/overflow；ordered Emit→ExternalCall→Schedule + response cursor/exhaustion/reordered/extra；declared/standard revert+trailing；emit-then-revert discard；invalidInvocation 矩阵与 init lifecycle；admission unsupported 与 Outcome 分离；Unit return shape；Switch miss/Trap；bounded-loop same EffectId occurrences 与 boundExceeded；UInt underflow/div0/invalidShift）；`Tests.lean`/`Tests/Fast.lean`/lakefile 已注册；**不是** formal TST-SEM-002/003 passed | **新轨子集 + 功能型 alpha residual**（admitted ReferenceV1 + InvariantABI foundation + 聚焦 suites；formal TASK-D2-07/TST-SEM-002/003 仍 pending；alpha Semantics仅留显式 compatibility/tests且未物理删除） | 扩展 PureCall/context/commit/aggregates/index 与 evalInvariant；formal corpus 与 target differential 闭合后替代 alpha `Core/Semantics`；**不**伪称 formal D2-07 完成 |

> 2026-07-31 状态更正：上方 `TASK-D2-06` 累计长表中的 “ContextRead
> requirement-to-result binding/key capability support 仍 Not yet” 已由本次 proposed/pre-acceptance
> static-only slice supersede；Wire 现已关闭单行 exact key/anonymous UInt64/exact requirement
> catalog。Reference ContextRead runtime与Invocation exact-context gate现也已由后续工程切片实现；
> Commit现已闭合operand/result TypeId exact equality、exact disclosure requirement row与Reference identity runtime；
> 全部target support与formal evaluator/theorem也仍未完成。

> 2026-07-31 模块边界更正：`InvariantFoundationV1`现承载原`InvariantABI` state carrier/codec/
> StateConforms declarations（FQName仍在public `InvariantABI` namespace），`ReferenceMachineV1`承载原
> `ReferenceV1` admission/runtime（FQName仍在public `ReferenceV1` namespace）。两个原路径保留为
> public façade；lower machine不import upper InvariantABI，从而为下一切片
> `InvariantABI → ReferenceMachineV1` formal evaluator依赖保持无环。本次仅机械抽取，行为未变。

> 后续lower-runner切片现从engineering `evalInvariantReferenceSliceV1`抽取
> `runInvariantCallableV1(data, callableId, state)`：它直接消费structure-validated decoded data与selected
> invariant callable的carried exact fuel，不构造/消费`AdmittedReferenceSliceV1`，因此无关的
> engineering-unsupported type/callable不会污染所选invariant执行。formal ABI仍未命名/开放；完整
> Int/Field/cast与dynamic canonical边界仍须先闭合。

> 第二个formal前置切片现开放Unit `Construct 0 []`并在线程化machine value写入、primitive operand、
> Assert/Branch/Switch、block-param、PureCall参数/结果及root return边界重验canonical TypeId/value bytes。
> lower runner已覆盖engineering admission仍拒绝的Principal canonical Eq与invariant内Unit Construct；
> Principal/Field general admission仍关闭。

> fixed-width integer前置切片现闭合Wire全部`8/16/32/64/128/256`宽度的UInt/Int：checked
> add/sub/mul/neg、UInt Euclidean div/mod、Int toward-zero div与dividend-sign remainder、signed order、
> fixed-width bit operations、checked left shift、UInt zero-fill/Int sign-extending right shift，以及
> UInt/Int四类CheckedCast的数学范围检查。`minInt / -1`与`neg minInt`精确overflow，`minInt % -1=0`；
> runtime failure保留standard revert分类，malformed canonical operand仍trap。engineering admission同步
> 接纳全部整数宽度，selected lower runner另以无关Field declaration固定不受全程序admission污染。
> Field arithmetic与正式`evalInvariantV1`/theorem仍pending。

> 后续Field前置切片现对sole v1 `bn254_fr`开放canonical modular add/sub/mul/neg/div；division以
> 固定256轮square-and-multiply计算`x^(p-2)`且zero divisor精确`divisionByZero` revert。Field仍不接受
> mod/order/bit/shift，Wire static contract保持权威。engineering admission将Field作为固定32-byte leaf
> 计入资源界限；带无关Principal declaration的selected invariant固定lower runner不依赖general admission。
> numeric前置至此闭合，正式ABI前还需最终closure/defensive gap复核。

> formal前closure/fuel复核已修复两个边界：Wire CFG现在要求每个callable实际包含entry block 0，
> 不再让`blocks=[]`经reachability/fuel检查vacuously通过；lower invariant machine按冻结公式实际扣除
> root frame entry与每次PureCall callee frame entry，并继续与instruction/terminator共享唯一fuel。
> tests固定empty entry/view/invariant CFG拒绝、单empty-instruction block正向，以及exact 3/6 fuel成功、
> 防御性降为2/5后trap。

> public `InvariantABI` façade现直接拥有SPEC exact `evalInvariantV1`与`InvariantTheoremV1`声明。
> evaluator按carrier validation→ordinal→initialized canonical state→selected callable执行，复用
> `runInvariantCallableV1`且不调用engineering admission/general step/外部输入；invalid program/ordinal/
> state/closure与machine fault均total映射trap，checked failure映射reverted。suite固定two-ordinal
> true/false、PureCall、revert/trap、malformed program/state、无关Principal与definitional theorem shape。
> formal TASK-D2-07/TST-SEM-002/003仍需独立canonical corpus/evidence，不能仅凭API实现标done。

> outcome corpus增量现补齐此前未命中的`SemanticFaultV1.resourceExhausted`与
> `.internalInvariant` explicit Trap映射，并固定两者遇trailing external response时由
> `.invalidExternalResponse`优先；`expectTrapped`同时断言pre-state byte exact不变。至此所有10个
> standard revert code与6个fault constructor均有engineering Outcome-level命中，但完整
> TST-SEM-002/003 corpus、target structural differential与retained evidence仍pending。

> kernel theorem前置的首个透明化增量为TypeKey空/单例路径加入数学等价fast path：singleton primitive
> 不qsort、无anonymous container不建HashMap、primitive/Option-only graph不建named-body DFS；
> Struct/Enum cycle gate与phase precedence不变。该优化已审查通过，但closed literal validation仍受
> transport ByteArray extract/copySlice的kernel reduction成本阻塞；未使用native_decide/axiom，也未
> 建立未经refinement证明的第二validator。

### D2-07 invariant reference evaluator 增量（2026-07-31）

首个 proposed/pre-acceptance ContextRead static-only slice 另冻结 wire-owned 单行 catalog：
`proof-forge.context.unix-time-seconds.v1` 必须返回程序唯一匿名 UInt64，并携 exact
`context.unix-time-seconds@1.0.0` empty-predicate/domain-separated digest row。其含义是 invocation
开始时 immutable Unix epoch seconds snapshot；Reference runtime 与所有 target support catalog
原先均关闭；后续Reference工程切片已开放runtime，但所有target support catalog仍关闭，
caller/authorizers/randomness deferred，formal `TASK-D2-07` 状态不变。

Reference invocation gate现从selected initializer/entry/view root沿static `Op.PureCall`边做bounded
closure traversal，收集并UTF-8排序exact ContextRead key/type集合；supplied context必须strict ascending、
exact set、TypeId一致且value bytes canonical，否则在lifecycle/response cursor前返回
`.trapped(.invalidInvocation, pre)`。validated snapshot仅存于Machine、由nested PureCall frame共享；
runtime lookup impossible missing/type mismatch返回`internalInvariant`。invariant closure仍由Wire拒绝
ContextRead/Commit；targets和formal evaluator/theorem未开放。

Commit的独立Wire typing切片现要求operand ValueId可解析且
`Instruction.result.typeId == type(value)`；Option等具有canonical encoding的aggregate同样允许，
不复用Eq/Ne的窄`serializableType` allowlist。Wire-owned `disclosure.commitment@1.0.0` empty-predicate
row现以`domainSeparatedSha256("pf.commit-requirement.v1", UTF-8(id))`冻结并在使用Commit时exact绑定；
该row不加入S2或target support catalog。Reference runtime在initializer/entry/view及ordinary PureCall中
执行exact TypeId/valueBytes identity，不hash、不重编码、不产生effect或state mutation；不得把该工程
runtime写成target support或完整declassification证明。invariant root/reachable closure禁止保持。

`ReferenceV1`现新增明确非formal的`evalInvariantReferenceSliceV1`：按validated InvariantDecl ordinal
选择zero-arg public Bool invariant，以`StateConformsV1`门禁logical state，使用carried exact
`invariantSteps`作为现有general-CFG reference machine的fuel上限，并映射canonical Bool/revert/trap。
普通Invocation仍拒绝invariant root，evaluation不发布state/effects。正式`evalInvariantV1`与
`InvariantTheoremV1`仍由`InvariantABI`唯一拥有且尚未实现；formal TASK-D2-07/TST-SEM-002/003
仍pending。

### D2-07 Map index runtime 增量（2026-07-31）

工程（非formal）ReferenceV1现接纳Wire-legal Map、empty Construct及Map IndexGet/immutable
IndexSet；get产生exact Option<value>，set保留旧SSA。WireV1 public lookup/upsert seam复用sole
cumulative decoder与unsigned-lex comparator，负责canonical framing、key legality、strict unique
order及count/byte/work/nesting cap。admission以`maxMapEntriesV1`理论最大entry count作cap-safe
保守width/work计算且不按count循环，可能拒绝实际可容纳的小Map。ContextRead/Commit Reference
runtime已由后续切片开放；formal evaluator仍pending。

### D2-07 Array/Bytes index runtime 增量（2026-07-31）

工程（非formal）ReferenceV1 slice现接纳fixed-length Array、Array Construct及Array/Bytes
IndexGet/IndexSet；immutable set产生新canonical bytes并保留旧SSA值。UInt32 LE index、base、
element/UInt8均按exact TypeId/shape与canonical bytes防御性复核；越界使用既有标准
`indexOutOfBounds` revert。WireV1新增窄Array split/encode seam，复用sole value decoder并预留outer
fuel、exact count、append前16MiB cap及full-consume/re-encode；canonical value decoder以64MiB
program-wide cumulative work budget跨constants/literals/switch values及所有recursive siblings线程化，
每个node计entry与canonical output work。显式栈资源分析使用相同recurrence和checked乘加，不按
UInt32 length展开，重复与零宽元素均完整计费；递归Array图仍拒绝。Map Construct/Index及
ContextRead/Commit Reference runtime已由后续切片开放；formal evalInvariant/TASK-D2-07/
TST-SEM-002/003仍pending。

### D2-07 Struct runtime 增量（2026-07-31）

`ReferenceV1`现进一步执行Struct-only aggregate family：admission允许递归字段均落在既有admitted
type集合内的Struct，以及Struct形态的`Construct`/`FieldGet`/`FieldSet`；Wire-valid Unit Construct
仍由per-instruction admission明确拒绝，Enum/Option/Array/Map及Variant/Index保持关闭。`WireV1`
新增窄canonical Struct split/encode seam，复用sole recursive value decoder、正确扣除outer nesting、
在append前执行16MiB cap，并对field count/type/canonical bytes/trailing bytes fail closed；Reference
不复制aggregate codec。admission以explicit-stack postorder、cap-saturating width/depth计算拒绝compact
Struct DAG的默认值allocation amplification，并以64MiB construction-work cap拒绝宽值的深层包装。runtime defensively重查shape、constructor/index、operand/result TypeId与
canonical bytes；logical-state defaults另按slot prefix累计受64MiB byte/work caps约束。FieldSet生成新bytes，不改变旧SSA值。聚焦suite覆盖nesting/width/construction-work/state-aggregate资源边界、多字段first/later get、immutable
set、nested Struct、pureFn与invariant root，并覆盖malformed/noncanonical/trailing/wrong-shape/count
及non-Struct Construct admission。后续独立工程切片已在同一machine加入acyclic Option/Enum canonical
Construct、VariantTag/VariantPayload、runtime tag一致性trap及cap-saturating资源分析；Wire-legal
recursive Struct/Option/Enum graph在有限maximum-resource Reference subset仍显式unsupported；Unit
Construct仍关闭，Map index、ContextRead/Commit与Option/Enum canonical Construct/VariantTag/
VariantPayload、fixed-length Array Construct及Array/Bytes IndexGet/IndexSet已由后续独立切片接纳。正式`evalInvariantV1`/
`InvariantTheoremV1`及formal TASK-D2-07/TST-SEM-002/003仍pending。

### D2-07 proof-validation byte refinement 增量（2026-07-31）

`WireV1`现有第一层kernel-checkable transport seam：透明`List UInt8` spine与production `ByteArray`
分别拥有remaining/read-byte primitive，两个refinement theorem覆盖任意offset及exact越界错误；production
Cursor `takeByte`直接委托该shared primitive，未新增第二semantic decoder。聚焦Lean examples固定proof
复用与production cursor成功/EOF。后续exact-slice theorem已通过标准库extract correctness证明
production `ByteArray.extract`输出映回logical list后等于transparent `drop/take`，Cursor `takeBytes`
直接委托该primitive，runtime性能路径不改；short input与越界zero-count边界均固定。后续scalar
切片已用shared byte primitive闭合u16/u32
transparent/production readers与universal refinement，并让production decoders直接委托。magic/tagged
framing原仍pending；后续magic/version切片已复用exact take并以ByteArray/List BEq bridge闭合prefix
success、`.truncated`优先与`.badMagic` mismatch，production `consumeMagic`直接委托。tagged framing与
完整program refinement原仍pending；后续tagged-header切片已闭合u32 length/范围、raw ASCII、exact
expected tag及u16 field count，`decodeTag`/`expectTag`直接委托shared production primitives，两个
universal theorem保持全部错误相位。root payload/完整program refinement仍pending，因此closed
invariant theorem与formal TASK/TST状态不变。后续payload framing切片已在sole `Wire/CodecV1`闭合
u32-length-prefixed bytes的prefix/limit/exact payload refinement，并让`decodeByteArray`/`decodeString`
直接委托。后续array-header切片再闭合u32 count与`maxCount` gate，production `decodeArray`仅将
header委托给shared primitive并保留唯一既有element loop；universal theorem固定truncation优先、
count等于上限及超限边界。后续iteration切片将该唯一production loop抽为结构递归
`decodeArrayElementsV1`，保持顺序、fail-fast与cursor线程，并以header-success composition theorem
提供稳定kernel unfolding seam；没有proof-side generic decoder。后续QualifiedName切片组合既有
sized-byte、sole String UTF-8/NFC、array iteration与`parseQualifiedName` authority，两个theorem保持
全部错误相位且不复制validator。进入root types前的后续切片又为sole `withTaggedNesting`建立
limit-first/body-error/input-offset/parent-depth restoration unfolding theorem，并为sum `decodeTag`建立
raw framing→既有UTF-8/ASCII分支composition。后续TypeShape前置切片将sole production sum body命名为
`decodeTypeShapeBodyV1`并补u16 field-count success/mismatch theorem，全部12分支保持原样；九张root
table与完整carrier refinement仍pending。后续nullary切片已组合Bool/Principal/Unit真实tag+zero-field
success及body→nesting wrapper success，保留body cursor input/offset和parent depth；其余shape与TypeDecl
原仍pending。后续TypeDecl切片已补expected-tag header与canonical Option.none composition，机械抽取sole
production body，并按header→id→name→shape组合success及parent nesting restoration。后续root-types
切片为sole array iterator补单步success与header+完整run组合 theorem，并将其专用于
`decodeArray maxTableElements decodeTypeDeclV1`。后续root scaffold切片将anonymous九字段closure机械抽为
sole `decodeSemanticProgramDataBodyV1`与单一tagged wrapper，正式transport decoder直接消费，并建立
exact field-order与nesting success composition。后续zero-table切片证明真实bounded count=0时立即返回
post-header cursor且绝不调用element decoder，可统一复用到各空root table。后续requirements切片机械抽取
sole production body，并组合tag→items array→parent nesting；canonical empty requirements现可复用zero array
theorem。后续logicalState切片机械抽取Visibility/StateDecl sole bodies，组合public visibility与
StateDecl tag→id→name→typeId→visibility、nested depth及root bounded array；private/commitment proof仍按需。
后续InvariantDecl切片同样组合tag→id→name→callableId、nesting与root bounded array。
后续Callable scaffold切片机械抽取sole九字段body，固定id→kind→name→params→result→entryBlock→blocks→
loopBounds→invariantSteps、nesting与root bounded array；各nested callable字段proof仍pending。
后续CallableKind切片补通用production `Option.some` composition，并机械抽取sole kind body；canonical
entry/pureFn/invariant三分支现可由真实tag→zero field-count及nesting组合，initializer/view runtime分支
保持但proof按需pending。后续CallableResult切片又组合exact tag→typeId→visibility及parent nesting，
可覆盖canonical public results而不提前声称TypeId结构合法。后续u64切片闭合transparent/production
reader universal refinement、decoder composition及Option.some invariantSteps。Block、Instruction/Op与
Terminator原仍待闭合；后续Block scaffold切片已固定header→id→params→instructions→terminator、
两个bounded arrays及parent nesting。后续Terminator切片机械抽取sole sum body，并闭合canonical
Return的tag→field-count→Option ValueId与nesting；其余五分支runtime保持、proof按需pending。后续
ValueDef/Instruction切片又闭合各自sole record body、字段顺序及nesting。后续SemanticOp切片机械抽取
sole sum body并闭合canonical Literal/PureCall及nesting；其余op runtime保持、proof按需pending。向上
组合先从sole production iterator导出singleton array success，随后将ValueDef、Literal/PureCall与
Instruction body theorem提升为public-decoder corollary；后续Return与Block也完成同级提升，供具体
single-block callables直接消费，不复制nested decoder。canonical Block zero/one-instruction形状现已
组合真实双array headers、exact element call count及public Terminator；Callable九字段也已提升为
public decoder composition；canonical empty-params/singleton-Block/empty-loop shape现已闭合，kind/name/
result/steps仍由真实production decoder决定。root count=4 callables array现可按source order组合四次真实
Callable decoder；具体kind约束仍不由transport theorem伪造。九字段root body现已提升到public tagged
decoder；后续full transport composition又闭合exact size→magic→tagged root→finish success及finish
error（含trailingBytes）原样传播。carrier identity composition现又接通真实transport→structure-gated
encoder→ByteArray BEq true/false，mismatch exact为nonCanonical。当前`InvariantABI` selected fixture已从
IO内联构造提升为纯`CanonicalInvariantFixtureV1.data`，并固定独立显式1235-byte golden；工程suite
检查sole production structure-gated encoder逐字节命中该golden且carrier仍走encode→decode。transport/
structure-gated encode/carrier的closed kernel theorem仍pending，不把该工程golden冒充formal evidence。
该golden现按15-byte magic/26-byte root header/1194-byte fields作proof-only transparent segmentation，
kernel已由production `consumeMagic`与`expectTag`真实路径闭合size=1235、magic offset 0→15及
`SemanticProgram.Data`/9-fields root header offset 15→41；九字段payload及finish尚未concrete闭合。
QualifiedName framing又已kernel闭合count=2（41→45）及两个exact sized payload（45→54、54→76）；
通用ASCII NFC fixed-point seam随后接入sole Unicode normalizer，最终production UTF-8/NFC、two-element
array与Common `parseQualifiedName`已闭合完整QualifiedName value cursor 41→76；下一字段为types。
types现又经production三元素array与真实nested TypeDecl/TypeShape decoder闭合Bool、Principal、Unit
source order cursor 76→187；紧随的empty constants array闭合187→191。下一字段为logicalState。

## D3：Registry、resolver、materializer与OutputSet

| Task | Formal | 新设计要求 | 实际代码与产品接线 | 现有测试事实 | Engineering | 迁移与旧代码处理 |
|---|---|---|---|---|---|---|
| `TASK-D3-01` | pending | TargetId、CodegenProfileId、NetworkProfileId三个独立typed parser | **工程子集已交付**：`ProofForgeV2/Core/TargetIdentityV1.lean` opaque `TargetId`（1..32 `[a-z][a-z0-9-]{0,31}`）/`CodegenProfileId`/`NetworkProfileId`（SPEC-COMMON profile grammar）+ internal `TargetKind`；descriptor/manifest 使用 typed IDs；NetworkProfileId 不参与 build selection；CLI `--profile` 解析 CodegenProfileId | `Tests/Materialization/BuildSelectionV1.lean` 固定三 ID grammar 边界；旧 enum dispatch 已移除 | **新轨子集**（三 ID typed parser 已接线；formal pending） | formal TaskQualification/full grammar corpus 仍 pending；不得把本切片写成 formal D3-01 done |
| `TASK-D3-02` | pending | static registry canonical digest、duplicate rejection、exact target/profile lookup | **工程 registry kernel（非 formal registry root）**：`Targets/TargetRegistryV1.lean` private-ctor sole 10-target membership/default/profile seed（4 implemented + 6 design-only），six axes 为 closed enums；`BuildSelectionV1` 直接绑定该 seed 并 sole mint private `ResolvedBuildSelectionV1`；旧 `StaticBuildSelectionIndexV1`/第二 seed 已删除；exact/case-sensitive target/profile 与 `PF-TARGET-*`/`PF-PROFILE-UNKNOWN`/`PF-REGISTRY-*` 保持；**M3a 工程 root**：`Targets/RegistryRootV1.lean` length-framed encode-only + `engineeringRegistryRootDigestV1`（domain `pf.registry-root.engineering.v1`；**不是** formal JCS root / formal root-digest product API）；formal-layout `BuildIdentityV1` 仅 five-field private layout/JCS、无 mint；**M3b** 另交付工程 `EngineeringBuildIdentityV1` mint（见 D3-03） | `Tests/Materialization/TargetRegistryV1.lean` 覆盖 sole membership、closed axes、validation、no-formal-digest/no-formal-BuildIdentity-mint deletion gate；`Tests/Materialization/RegistryRootV1.lean` 固定 engineering root 字节/digest golden、determinism、10×6 axis + profile/default tamper；`BuildSelectionV1.lean` 覆盖 registry seed resolve/aggregate/CLI/Counter wire；consumers 已迁到同一 registry seed | **新轨子集 + 功能型 alpha**（sole registry membership/default/profile + selection authority 已接线；**工程** root digest 已接线；formal registry root digest/full payload 与 formal BuildIdentity mint仍缺） | 在现有 `TargetRegistryV1` 原位扩展 formal root codec/digest/full descriptor/profile payload，再开放 formal BuildIdentity mint；不得新增并行 registry 或命名为 formal D3-02 完成 |
| `TASK-D3-03` | pending | ProgramRequirements → exact SupportClaim/claimDigest + BuildIdentity/ProfileSupportIndex | **工程子集（非 formal SupportClaim）**：`Targets/RequirementResolverV1.lean` 静态 S2 support index + `EngineeringBuildV1` sole mint `resolveEngineeringRequirementsV1 (selection, compiled)` → private `ResolvedEngineeringBuildV1`（exact retained SemanticProgramV1 requirements + **M3b** bound `EngineeringSupportClaimV1`，无 caller request override）；exact empty-predicates only；`PF-REQ-UNSUPPORTED`/`PF-REQ-PRECONDITION`/`PF-REGISTRY-*`；已删 product `checkSupport`；**S6** 已删 public residual Common resolve/validateResolved/makePlan、supportedRequirements membership acceptance、public Residual planFromAlpha/lowerPlan/filesFromIR 与 dead public ResolvedProgram；**M3b 工程 SupportClaim**：`Targets/SupportClaimV1.lean` private-ctor `EngineeringSupportClaimV1` + sole `mintEngineeringSupportClaimsV1`（domain `pf.support-claim.engineering.v1`；preimage=targetId/profile/supported-ids/engineeringRegistryRootDigest）；**M3b 工程 BuildIdentity**：`Targets/EngineeringBuildIdentityV1.lean` private-ctor `EngineeringBuildIdentityV1` + sole `mintEngineeringBuildIdentityV1`（domain `pf.build-identity.engineering.v1`；bound target/profile/artifactName/source/semantic/root/claim digests）；`mintMaterializedArtifactsV1` 绑定 identity 于 `MaterializedArtifactsV1`（内存 only，**未**进 on-disk manifest）；无 formal SupportClaim/ProfileSupportIndex/predicate implication | `Tests/Materialization/RequirementResolverV1.lean` + residual type-chain reflection + `s6-plan-cutover-deletion-gate`；**`Tests/Materialization/IdentityChainV1.lean`** 固定 claim bulk mint 序/determinism/support-row+root tamper、product-path identity bind/determinism/profile+source sensitivity、sole-mint/forbidden-name surface；Counter 四 target capability materialize | **新轨子集**（engineering exact resolver + S6 Plan cutover + **M3b engineering claim/identity carriers**；formal SupportClaim/BuildIdentity 仍缺） | 继续 formal SupportClaim/claimDigest/predicate implication/formal BuildIdentity/OutputSet wiring；禁止命名为 formal D3-03 完成 |
| `TASK-D3-04` | pending | associated Plan/TargetIR protocol含schema、CodegenProfile、validation、EmitContext | **S6 工程**：`Materializer` 仅保留 associated `Plan`/`TargetIR` 类型见证；四 target 公开 capability-gated `planFromCapability`/`irFromCapability`/`buildFromCapability` + public validatePlan/validateIR inspection；四 target private Plan body均已改为 retained `SemanticProgramV1` S1 lowering，target module 内 `alphaResidualOf`/`makePlanFromAlpha`/alpha requirement re-derive归零；四 target lower/emitFromIR仍仅 capability 内部；无 public Semantic→Plan/Plan→IR/IR→files；仍缺完整 lowering与 formal schema/validateTargetIR/IO EmitContext | target modules 证明 Plan/TargetIR 类型未擦除；capability 路径覆盖四 target；host models 用 production irFromCapability；PrivateSum4 fixture 仅 isolated | **可复用地基 + S6 capability 入口** | 升级 formal protocol；不得用 Unit/string/JSON 或兼容 adapter 保留旧 public makePlan/Residual emission API |
| `TASK-D3-05` | pending | OutputSetV1、support-decisions、manifest schema/digest、exact file closure与atomic publish | **S7a+S7b+S7c 工程（非 formal OutputSetV1）**：private-ctor `MaterializedArtifactsV1` sole mint（pure base files）+ private-ctor `FinalizedArtifactsV1` sole mint（capability/artifacts bind + deployable + ordered extras + exact note）+ sole `validateEngineeringDiskClosureV1`（derived base+extras+sidecars；no-follow exact closure；manifest-last）；Registry sole `finalizeMaterializedArtifactsV1`；`LockedToolchainV1` cycle-free tool runner；四 target FinalizeV1 adapters；已删 public `OutputSet`/`OutputManifest`/`makeOutput`/`manifestJson`/`validateOutputSet` 与 `CLI/Toolchain`/`finalizeEvm`/`finalizeNear`；CLI publisher-only private v2alpha1 + staging/rename/no-clobber 保持 on-disk 字节兼容（S7c 仅写序 evidence→manifest）；M3c 工程已切 `proof-forge.output.v1` output set（registryRoot→SupportClaim→BuildIdentity→OutputSet digest 链已绑；仍无 candidate/plan/IR/tool digest 与 formal OutputSetV1） | `Tests/Materialization/OutputEnvelopeV1.lean` + `Tests/Materialization/EngineeringFinalizationV1.lean` + `Tests/Materialization/EngineeringDiskClosureV1.lean` + `s7-output-envelope-deletion-gate` + `s7b-finalize-authority-deletion-gate` + `s7c-disk-closure-gate`；`Tests/CLI/Emit.lean`/`ToolchainPolicy` 与 output shell negatives 覆盖原子性/tool-fail；artifact validator 统一 no-follow exact closure + 固定 v2alpha1 | **新轨子集 + 功能型 alpha**（engineering carrier + finalization + exact disk closure 已切；formal OutputSetV1/digest/decision-set 仍缺） | 实现完整 formal OutputSetV1/decision-set/closure/rollback；迁移后删除 private v2alpha1 sidecar contract；禁止命名 formal D3-05 done |
| `TASK-D3-06` | pending | `check/build/inspect/list-targets`、profile/evidence/resource flags、stable human/JSON | CLI只有list-targets、describe-target、build、build-counter；缺check/inspect/profile/minimum evidence/resource override/JSON | CLI emit与shell product-negative覆盖当前窄参数面 | **功能型 alpha 子集** | 实现规范command surface；删除ad-hoc `describe-target`/`build-counter`产品入口 |
| `TASK-D3-07` | pending | contained compiler-core/tool/output supervisor、effective profile digest/receipt和whole cleanup | Toolchain有exact locked execution/isolated env，Common有resource profile；compiler-core/output仍进程内，无统一receipt/whole-containment | `ToolchainPolicy`、toolchain negatives覆盖selected closure；不能关闭core/output process containment | **可复用地基 + 缺失** | 实现三stage supervisor/receipt/signal cleanup；保持与TaskQualification ceremony完全分离 |

## D4：EVM

| Task | Formal | 新设计要求 | 实际代码与产品接线 | 现有测试事实 | Engineering | 迁移与旧代码处理 |
|---|---|---|---|---|---|---|
| `TASK-D4-01` | pending | versioned EvmPlan schema、canonical invariants/hash和resolved identity binding | `Targets/Evm.lean`有target-owned Plan、storage/selector/ABI/resource/shape validation；无正式schema/canonical serializer/BuildIdentity binding | `Tests/Materialization/EvmSmoke.lean`有Plan mutation/shape测试 | **功能型 alpha，可大量复用** | 保留算法，增加schema/hash/decision binding；旧alpha Plan public contract随后删除 |
| `TASK-D4-02` | pending | validated SemanticProgramV1 → EvmPlan | **Wave 2 S1 迁移先导事实**：public `planFromCapability (capability : ResolvedEngineeringBuildV1)` 只读取 `CompiledSemanticV1.semanticV1Of`，经 `validateSemanticProgramV1` 后进入 private V1 lowering；EVM module 内 `alphaResidualOf`/`makePlanFromAlpha`/alpha requirement envelope归零。当前只接受 anonymous UInt64/Unit/body-local Bool、public state/params/results、forward-only 多块 init/entry/view（if/match region、无 back edge/block param）、literal/stateLoad/checked add-sub/UInt64 六比较/bare assert/stateStore/return（含带值早退 trailing join；臂内裸 return marker fail closed），并以 dense ValueId、expanded-tree cost和effect-segment consumption fail closed；single-semantic carrier与canonical Digest identity已接线，但 formal Plan/IR/Build/Output identity仍不存在。formal D4 pending | `CounterV1Evm`、`EvmSmoke` rich `Ledger`（双 state/多 param/nested add-sub/双 store/post-store return/view）与 durable `s1-evm-semantic-plan-deletion-gate` 固定 source authority、Plan shape及无 EVM residual route；既有 Plan depth/node mutation继续覆盖边界 | **SemanticProgramV1-native public-UInt64 add/sub pilot + 可复用 alpha downstream** | 保持 capability-only/private lowering，随 sole Normalize扩张逐步扩大 fragment；补 formal EvmPlan schema/hash/identity与 adversarial carrier corpus，不得重新引入 residual bypass或把本先导命名为 TASK-D4-02 done |
| `TASK-D4-03` | pending | EvmPlan → validated TargetIR/Yul + ABI | 有typed IR、dynamic Keccak selector、Yul/ABI renderer；`lower`先validate Plan，但没有TargetIR schema/hash/trace validation | EVM smoke、artifact validator固定selectors/ABI/Yul | **功能型 alpha，可大量复用** | 加TargetIR schema/validator/hash/trace，接D3 Materializer/EmitContext；移除alpha emit signature |
| `TASK-D4-04` | pending | locked solc bytecode packaging到OutputSetV1 | 真实source已通过digest-pinned solc 0.8.34生成bytecode；selected-tool closure已修复。**S7b**：solc 由 `Targets/Evm/FinalizeV1` + Registry `finalizeMaterializedArtifactsV1` 执行（不再 CLI inline）；on-disk 仍 v2alpha1 manifest/evidence | `ToolchainPolicy`、`EngineeringFinalizationV1`、target-smoke、solc-only root positive/full release-root negative通过 | **功能型 alpha** | 将ToolchainIdentity、bytecode validation和artifact roles移入 formal OutputSetV1；禁止命名 formal D4-04 done |
| `TASK-D4-05` | pending | Anvil Counter与reference interpreter differential | `scripts/smoke_evm.sh`已验证Counter和Accumulator init/mutate/read/UInt64 overflow rollback | 当前开发机真实Anvil smoke通过；不是新Semantic/Output contract上的formal TST closure | **功能型 alpha** | 新D2/D3/D4唯一路径完成后重跑reference-vs-Anvil；旧结果只作回归，过期hash/golden删除 |

## 共享迁移依赖

删除 D2/D3 alpha core 不只是 EVM 问题。四个 target 的 S1 Plan body、compiler、resolver与artifact identity现已统一消费 retained `SemanticProgramV1`/canonical Digests；剩余共享删除面是扩大 sole Normalize覆盖、隔离 legacy alpha compatibility，并接通下游 formal identity/output contract：

| Consumer | 当前 maturity | 迁移要求 | 不得顺带声称 |
|---|---|---|---|
| EVM | runtime-validated alpha downstream + retained-SemanticProgramV1 public-UInt64 add/sub + 比较/assert + Bool result + if/match region + emit/revert + fn/localCall + arith/unary + let/for + shift/bitwise Plan pilot（call/schedule 拒绝：无 address-bearing 类型） | 扩完整 V1 fragment并迁入 formal EvmPlan/TargetIR/BuildIdentity/OutputSetV1 contract | D4 formal done，除非全部TST/formal条件真实闭合 |
| Solana | retained-SemanticProgramV1 public-UInt64 add/sub + 比较/assert + Bool result + if/match region + emit/revert + fn/localCall + arith/unary + let/for + shift/bitwise Plan + plan-only downstream（call/schedule 拒绝：无 program-id 类型） | 迁入 formal identity/OutputSet，保留target-owned Plan/IR | sBPF ELF或runtime完成 |
| NEAR | retained-SemanticProgramV1 public-UInt64 add/sub + 比较/assert + Bool result + if/match region + emit/revert + fn/localCall + arith/unary + let/for + shift/bitwise Plan + schedule→原生 promise + Wasm-validated alpha downstream | 迁入 formal identity/OutputSet，保留NEAR Plan/Wasm算法 | sandbox receipt或D6完成 |
| Noir | retained-SemanticProgramV1 public-UInt64 add/sub + 比较/assert + Bool result + if/match region + emit/revert + fn/localCall + arith/unary + let/for + shift/bitwise Plan（路径枚举+叶结点断言/事件槽/select 内联）+ call/schedule status/arg slot + source-only downstream | 迁入 formal identity/OutputSet，继续non-deployable | ACIR/witness/proof/VK/verify或D7完成 |

如果任一产品 consumer仍读取旧 carrier或identity，相关删除行保持 `blocked by consumer`，不得增加公开兼容层来伪造归零。

## 旧代码退役清单

| 层 | 旧实现候选 | 当前删除状态 | 删除门槛 |
|---|---|---|---|
| D1 frontend | `Syntax.lean` legacy decoder/quote/elab；Loader legacy APIs；`ProgramExport` v1；quoted `ProgramPayload`；string-call语法；legacy-only fixtures | **D1-06 source-reading surface 已删除**：`Syntax.lean` legacy Source decoder 家族与 `Loader.lean` legacy `parsePrograms`/`selectProgram` API 已移除，`parseProgramsV1`/`selectProgramV1` 成为唯一入口；**B4 string-call 语法已删除**：`syntax "call " str` 与 decoder-rejection 臂归零，sole structured `call QualifiedId(args)`，legacy string call 仅 parser-boundary 拒绝；span/NodeId inventory 与 DiagnosticV1 product cutover 已完成；B11b2/B12 监督切面已按 2026-08-01 产品决策移除（CLI 源读取为进程内 `Loader.selectProgramV1Product`；原 durable gates 随层删除）。D1 formal executable/import identity 与 containment/host-race qualification 不再适用；迁移剩余主项转为 Wave 2 alpha product residual，故整体仍 **未满足** | 完整V1 declaration/stmt parser、span/NodeId、v2 export、selection、DiagnosticV1、worker都成为唯一入口 |
| D2 core | `Core/Source.lean`、`Core/Typed.lean`、`Core/TypedV1.lean`、`Core/SemanticIR.lean`、`Core/Semantics.lean`、`Compiler.compile(Source.Program)` | **未满足** | 完整Typed/Semantic/provenance/interpreter完成，四target consumer全部直接迁移，imports归零 |
| D3 framework | alpha TargetDescriptor/contains resolver、旧 Materializer signature、v2alpha1 OutputSet sidecar、engineering finalization carrier（非 formal OutputSetV1） | **未满足** | typed registry/resolver/BuildIdentity/OutputSetV1/supervisor完成，四target直接实现新protocol |
| D4 EVM | alpha EVM descriptor/materialize入口、target-owned solc FinalizeV1（S7b）、v2alpha1 manifest assertions和过期 plan-hash goldens | **未满足** | 新EvmPlan/TargetIR/OutputSetV1上Counter+Accumulator deterministic build及Anvil differential全绿 |

## 每次删除的机械门禁

1. 新实现已经是 CLI、Lean command、Loader/compiler和相关target的唯一产品入口；没有flag、fallback或第二reader。
2. `ProofForgeV2/**`、`Tests/**`、Lake入口和当前scripts对旧symbol/type/schema引用为零；历史文档只能以historical文字保留。
3. 旧正例、负例、边界和diagnostic priority已迁到新carrier，不能靠删测试获得绿色。
4. 所有shared consumers已直接迁移；不保留长期adapter。
5. 聚焦测试、`just dev-check`、`just ci`、`just target-negative`、`git diff --check`通过；EVM删除再要求deterministic target-smoke和Anvil differential。
6. `ProofForgeV2/**`变化已刷新并核对 `supply-chain/lean-package-files.v1.json`。
7. 本矩阵更新replacement、remaining consumers、删除文件和实际验证；formal task状态只按其独立规则变化。

## 下一实施顺序

1. **Wave 1 / D1**：B11b2/B12 前端监督层已按 2026-08-01 产品决策移除（CLI 源读取为进程内 `Loader.selectProgramV1Product`）；D1-04 shared IntegerLiteral lexical/range slice 已完成工程接线；contained/host-race qualification 随层删除不再适用。
2. **Wave 2 / D2（当前）**：初始 Counter-like S1 contract、EVM先导与 Solana/NEAR/Noir 非重叠 target leaf均已接线；四 target module 内 `alphaResidualOf`/`makePlanFromAlpha` 已归零。`RequirementContributionV1` → `ProgramRequirementsV1` sole authority、`CompiledSemanticV1` 单 carrier及 compiler/resolver/materialize/finalize/CLI alpha产品consumer删除也已接线；首个 sole Normalize扩面已把 public UInt64 checked-sub（含 provenance）贯穿四 target-owned Plan/IR/emitter，后续仍只在该 sole Semantic路径上继续扩完整 Normalize。隔离的 `AlphaCompatibility` 只服务 legacy hand-built测试，不是产品 fallback。
3. **Wave 3 / D3**：在现有 authority 原位接线 registry digest、SupportClaim/decision、可达 BuildIdentity、Plan/IR identity、`OutputSetV1` 与 stage supervisors；不建第二 registry/resolver/output contract。
4. **Wave 4 / D4–D7**：D3 contract 冻结后先闭合 **D4/EVM** 的完整 Plan→validated IR/Yul+ABI→locked solc/OutputSetV1→Reference↔Anvil differential；随后才推进 Solana/NEAR/Noir target completion。不得把 Wave 2 的 EVM consumer迁移或 plan-only/Wasm-static/source-only产物升格为里程碑完成。
5. **Wave 5 / D8**：D8-01/02/03 并行最终重跑，汇合后串行 clean-room full gate与 review/release/rollback。

`main` 是 sole integration authority；临时隔离 worktree 只承载接口已冻结、文件 allowlist 完全不重叠的 leaf lane，合并并通过聚合门禁后立即删除。formal task状态仍只按独立 qualification 规则变化。
