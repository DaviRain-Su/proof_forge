# Public only — never put APrivateKey here.
# Program deployed via `pf deploy --network testnet --broadcast ...`
VITE_ALEO_PROGRAM_ID=pfdemo336641.aleo

# testnet | mainnet | canary  (template defaults to testnet)
VITE_ALEO_NETWORK=testnet

# Explorer REST base (no trailing slash)
VITE_ALEO_API=https://api.explorer.provable.com/v1

# Optional fee in microcredits for executeTransaction
VITE_ALEO_FEE_MICROCREDITS=100000
