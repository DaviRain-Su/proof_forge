[package]
name = "pf-core"
version = "0.1.0"
edition = "2021"
publish = false
description = "Read-only core.v0 + capability-plan.v0 package loader (Seam A / D-057; zero chain SDKs)"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"

[dev-dependencies]
