[package]
name = "pf-core-inspect"
version = "0.1.0"
edition = "2021"
publish = false
description = "CLI for pf-core Seam A packages (zero chain SDKs)"

[dependencies]
anyhow = "1.0"
pf-core = { path = "../pf-core" }
sha2 = "0.10"
