# AGENTS.md

这是 ProofForge V2 独立工程的 agent 控制面。它只负责导航、当前产品检查点和工作协议，
不替代 PRD、架构或技术规格。恢复边界见 [`RECOVERY.md`](RECOVERY.md)，D1–D4 的
设计要求/代码事实/删除门槛见 [`MIGRATION_MATRIX.md`](MIGRATION_MATRIX.md)。

## Project Mission

构建 Lean 4 编译器 `proof-forge-next`：统一的 `program ... where` 源码经类型化、
目标中立语义和需求求解，进入 target-owned Plan/IR，最终生成平台制品。作者不写
顶层 `kind`；目标决定物化形态，但不得改变业务语义。

## Current Checkpoint

| Field | Current value |
|---|---|
| Program | V2 产品恢复桥：`ProgramV1` narrow public-UInt64 add/sub/mul/div/mod + unary（`~`/`!`，一元 `-` desugar 为 `0 - x`）+ 六比较 + bare assert + Bool entry/view 返回值 + anonymous Array/Map/Option/Bytes entry/view/fn 返回值（shared Semantic/Reference；八个非 Quint materializer 已开受限 Array/Option entry/view ABI；Psy 另开 Bytes 1..8 return 与 dense Map UInt64 state；Map 返回与多数 target Bytes/pureFn 聚合返回仍 FC，Quint 容器全 FC）+ if/match 多块 CFG + revert/emit（shared public UInt/Int/String event/error 表；九个 materializer String interface ABI 仍 fail closed）+ fn/localCall（pureFn 无 state/effect）+ 不可变 let + bounded for（同宽 legal UInt/Int；单 block-param header + latch 回边 + loopBounds，exact back-edge 边界强制；九个 materializer Int induction Plan 仍 fail closed）+ shift/bitwise/logical 二元 + call/schedule（S2 七键 + 非均匀 capability 矩阵：EVM static-QN 双键、Solana 仅 exact CPI profile sync（legacy profiles 与 schedule FC）、NEAR promise 仅 schedule、Noir status/slot 双键、Aleo 双键 fail closed、Psy 仅 sync、Quint 仅 rollback/state/Bool/checked-arithmetic 四键且 effect 全拒、CosmWasm sync 拒/async SubMsg 子集、TON async createMessage 子集已开（hash destination/value=0/fixed send-mode，语义 PARTIAL）；完整平台语义 caveat 见下）子集已接通九个 target-owned retained-`SemanticProgramV1` Plan/IR/emitter materializer；Normalize 已开 multi-width UInt/Int{8..256} + Field(bn254) 算术 + Principal identity（**T8 已把 ABI 与 body 多宽铺到四 target**：EVM/Solana/NEAR/Noir state/param UInt8/16/32 ABI + body 多宽 UInt8/16/32 全接线，Noir 另开 Field state/param；EVM 另开 mod-p Field 通道（ADDMOD/MULMOD+Fermat 逆元）与定长 Array state + IndexGet/IndexSet（运行时 bounds revert）；Psy Felt=Goldilocks(2^64-2^32+1)≠bn254 已证伪保持 fail-closed；Noir 私有 state/params 走 private-witness 输入；ContextRead/Commit 已贯通 Normalize+Reference（**ContextRead Plan 非均匀**：EVM `unixTimeSeconds`+`blockHeight`+`caller` 已开；Solana legacy plan/elf 开 `blockHeight`→Clock.slot、exact CPI profile 开 signer-role `caller`（交叉 profile/key 仍 FC）；NEAR 开 `unixTimeSeconds`+init/entry `caller`（view FC）；CW 开 `unixTimeSeconds`+instantiate/execute `caller`（query/view FC）；TON 仅 `unixTimeSeconds`；Noir/Psy/Aleo/Quint 保持 FC；Commit 身份透传 EVM/Solana/NEAR/Aleo））；**T9 宽度系列已 merged**：EVM 原生 UInt128/256（full-word）、Solana/NEAR 软件多字 UInt128/256（2×/4× limb，add/sub/cmp/bit + schoolbook mul + restoring binary div/mod；Solana WideDiv 有真实 ELF+Mollusk）、Noir UInt128 原生 + UInt256 multi-limb（T13）、四 target 窄结果与窄 Int（T9a/T9c）；Psy 另以显式 `psy-dargo-0.1.0-vm-v1` 开 4×UInt32-limb UInt128 state/param/literal/constant/entry-view result、checked add/sub/mul/div/mod 与六比较（mul=8×UInt16 schoolbook；div/mod=四段 32-step restoring，每函数 1 次 binding；默认 profile 仍 FC；bitwise/shift 与 UInt256 已在显式 VM profile 开放）；**T10/T12 Principal 存储 identity**（EVM/Solana/NEAR/Noir 9×leaf layout，wire identity 原样存储，**非** 20B address/32B pubkey/account-id/Field；B-3 PrincipalAddr pin 不变，不解锁 CALL/CPI）；**AddressBearing/CPI**：EVM 保持 static-callee call/schedule；Solana legacy profiles 对 call/schedule FC，仅 exact CPI profile 开 frozen sync-call API（显式 accounts/PDA；callee 为 catalog-owned static QualifiedName，非 dynamic pubkey）；**Field catalog 已扩为三 spec（T14 真做，2026-08-02）**：Wire FieldSpec catalog 从 sole bn254 扩为 bn254 Fr（EVM/Noir）+ BLS12-377 Fr（Aleo→Leo field）+ Goldilocks（Psy→Felt），exact (id,modulusBE) membership 无任意 modulus；Normalize/TypeCheck/Source/RequirementsInfer 已接线；EVM/Noir 仍仅 bn254，Solana/NEAR Field 仍 fail-closed（注：commit 30df771f2 原 message 假称已做 T14 实际未落地，本次真做并关闭 backlog DOC-CODE-1）；**N-A1 EVM String match switch + N-A2 多臂同构造器 match 细化已闭合**；**N-INVARIANT-IR 已闭合**（source invariant→零参 public-Bool Semantic callable + dense InvariantDecl；Wire sole exact closure/fuel；Provenance 完整覆盖；`.proof` 不进入业务 IR；Quint 对 Q0 read-only Bool invariant 已 lowering，其余八个 target 对 nonempty invariants 仍 fail closed）；**N-CONST/N-CONST-REF 已闭合**（`evalConstDeclValueV1` 字面量求值→constants 表；body bare const→`Op.Constant`，支持 forward ref、exact TypeId/ValueId 与 Provenance；声明表达式仍仅 literal/negative Int；四主 target 对表 FC；Aleo 对 literal-backed `Op.Constant` 已开（ALEO-CONST 内联 lowerLiteral）；Psy 对精确可表示标量及显式 VM-profile UInt128 constant 已开）；**B-SOL-WIDE Solana/NEAR UInt128/256 mul/div/mod 已切真多字**（mul 为 schoolbook；div/mod 为 restoring binary long division；Solana WideMul/WideDiv 已有真实 ELF+Mollusk 成功/溢出或零除回滚差分，四宽 handler 长距 dispatch 另有 runtime pin）；**B-OPT-STATE 八 materializer 已开**（EVM/Solana/NEAR/Noir/Aleo/Psy/CosmWasm/TON：`Option UInt64` tag+payload 双叶 state，none/reset 清零 payload并原子 store；**Quint FC**；Option params/非 UInt64/nested 仍 FC）；**NS-1 Token dense Map cap-8 四 target demo**（EVM locked-solc finalization 已通但 258460B creation bytecode超 EIP-3860、无链上部署声明；NEAR deployable、Solana plan 默认、Noir multi-leaf PI）；**CosmWasm/TON** 已有 capability Plan/IR/materialize/finalize（CW：UInt8/16/32、named state、bounded aggregate/Array/Option return、Binary SubMsg 与 mock+wasmd engineering rungs；TON：UInt8/16/32、named state、bounded view aggregate/Array/Option return、schedule createMessage 与 sandbox 10/10；两者 sync call及完整平台语义/formal 仍 FC）；其余表面继续 fail closed ；**#125** `solana-sbpf-cpi-elf-v1` advertise sync+extension + `cpi-product-ir` digests `b0f3f5bc…`/`e2c2ebac…` |
| Product milestone | 当前工程纵切面为 CLI 进程内 **单次** `IO.FS.readFile` → `Loader.selectProgramV1ProductWithTheoremInventory`（validated root；前端监督层已于 2026-08-01 移除）→ `normalizeProgramLocatedV1` structure gate → `compileProgramProductV1` → **private-ctor `CompiledSemanticV1` single-semantic carrier**（retained `SemanticProgramV1` + artifact name + canonical source/semantic Digest；无 alpha residual）→ **sole `certifyInlineProofV1` gate**（held raw source；`noProof`/`certified`/`failed`；失败 `PF-SRC-INVALID`/exit 3；无 `--proof-bundle*`）→ frozen `TargetRegistryV1` membership/default/profile/descriptor-axis resolve → **`resolveEngineeringRequirementsV1` → private `ResolvedEngineeringBuildV1` capability** → capability-only target Plan/IR/files → private-ctor **`MaterializedArtifactsV1`**（S7a）→ **`FinalizedArtifactsV1`**（S7b）→ **`ArtifactContentV1` sole physical scan**（base/extra role + path + size + exact-byte SHA-256）→ pure **`EngineeringOutputSetV1`** mint（含 `evidenceSha256`）→ evidence→manifest-last + post-write inventory/exact-disk-closure recheck → CLI publisher-only `proof-forge.output.v1` on-disk；`inspect <output-dir>` stable-read sidecars/listed artifacts并重走 exact closure。该完整链当前覆盖九个 materializer（EVM/Solana/NEAR/Noir/Aleo/Psy/Quint/CosmWasm/TON；`Registry.materializeResult` 含 `.cosmwasm`/`.quint`/`.ton` dispatch）；resolver 十三行静态 requirement support index（9 implemented target 中 EVM×2、Aleo×2、Noir×2、Psy×2、Solana×1，其余单 profile）；`check` 输出 proofStatus/count/digest，`build` 只门禁不输出 proof 字段。**Narrow inline-proof engineering closed**：legal-only production simple-closure encode/decode + exact ordinal-0 `InvariantTheoremV1` + literal-true/public-Bool-view same-file ordinary theorem 的真实 product `check` certified；theorem body 改写保持 source/semantic identity并改变 certification digest；in-process elab ≠ sandbox；package-owned session 不信任 ambient `LEAN_PATH`/用户 `.olean`；allowed axioms 仅 `Classical.choice`/`Quot.sound`/`propext`；proof fail 早于 target resolve/materialize 且零 staging；Quint Q0 对 read-only Bool invariant 已开放，其余八个 materializer 对 nonempty invariants 仍 fail closed；不声称 formal TASK/TST、reachability、target refinement、hermetic/release |
| Product status | D1–D4 formal 仍为 0/27 done（D2 7/7 pending，含 TASK-D2-07）；**D3/S4–Formal Spine V3 工程 registry kernel 已接线**（`TargetRegistryV1` 为 12-target membership/default/profile 与 descriptor semantics-axis sole authority（9 implemented + 3 design-only），six axes 为 closed enums；**D3-E9** 后 `TargetDescriptor` 直接复用 registry 轴，九个 registry-implemented descriptors 从 frozen seed 派生并在 capability resolve/CLI inspect 前 exact join；CosmWasm/Quint/TON 已 capability materialize/finalize，Protocol 重复六轴已删除；旧 `StaticBuildSelectionIndexV1` 已删除；无 formal root `registryDigest`；`BuildIdentityV1` 仅 private five-field layout、无 mint）；**D3/S5 single-semantic carrier + engineering exact requirement resolver 已接线**（`RequirementContributionV1` 为 target-neutral sole AST contribution engine，`Semantic.RequirementsV1` 独占 catalog/version/digest/sort 与 `ProgramRequirementsV1` mint；`TargetDescriptor` 无 requirement list；产品 umbrella/compiler/resolver/materializer/CLI import closure不含 legacy Source/Typed/SemanticIR/Semantics或 AlphaCompatibility（这些模块与 pure-alpha tests 已物理删除（含 `Core/TypedV1.lean` orphan）））；**D3/S6 capability-only Plan cutover 已接线并收紧 residual emission bypass**（cycle-free `EngineeringBuildV1` leaf sole mint private-ctor `ResolvedEngineeringBuildV1`；`DescriptorDataV1` 共享九个 registry-implemented descriptor（含 CosmWasm/Quint/TON S6 Plan leaf）；九个 materializer 仅 capability-gated `planFromCapability`/`irFromCapability`/`buildFromCapability` 公开入口（输入 `ResolvedEngineeringBuildV1`）+ descriptor/validatePlan/validateIR inspection；已删 public `Common.resolve`/`validateResolved`/`makePlan`、`TargetDescriptor.supportedRequirements` 字段与 membership acceptance；已删 public `namespace Residual` 与 `planFromAlpha`/`lowerPlan`/`filesFromIR` 完整 Semantic→Plan→IR→files bypass；已删 dead public `ResolvedProgram`；Registry `materializeResult` 直接 capability dispatch；**九个 materializer Plan body均已切到 retained `SemanticProgramV1`**（`planFromCapability` → `semanticV1Of` → `validateSemanticProgramV1` → target-private V1 lowering；EVM/Solana/NEAR/Noir/Aleo/Psy/Quint/CosmWasm/TON module内 `alphaResidualOf`/`makePlanFromAlpha`/alpha requirement re-derive归零）；九 materializer 的 private `lower`/`emitFromIR` 仍仅 capability 内部；`s6-plan-cutover-deletion-gate` + Lean residual type-chain reflection（defn/opaque/ctor + renamed/alias/capability controls）已接入 dev/ci）；**D3/S7a 工程 materialized-artifact carrier cutover 已接线**（private-ctor `MaterializedArtifactsV1` sole mint `mintMaterializedArtifactsV1`；`materializeResult` 返回 capability-bound carrier（exact target/profile/kind、semantic-derived artifact name、canonical ProgramV1 source Digest、retained `semanticHashV1` Digest、ordered `OutputFile`）；已删 public `OutputSet`/`OutputManifest`/`makeOutput`/`manifestJson`/`validateOutputSet`；CLI publisher 已切到 engineering OutputSetV1（proof-forge.output.v1 manifest+evidence，manifest-last 精确闭包；`inspect <output-dir>`/`check`/`--json` 产品 CLI 表面已闭合）；`s7-output-envelope-deletion-gate` 已接入 dev/ci；**不是** formal OutputSetV1/proof-forge.output.v1/BuildIdentity）；**D3/S7b 工程 finalization-authority cutover 已接线**（cycle-free `Materialization/LockedToolchainV1`；已删 `CLI/Toolchain`；private-ctor `FinalizedArtifactsV1` sole mint；Registry sole `finalizeMaterializedArtifactsV1` → 九 materializer FinalizeV1；CLI/Emit publisher-only；`s7b-finalize-authority-deletion-gate` 已接入 dev/ci；**不是** formal ToolchainIdentity）；**D3/S7c + D3-E7 工程 content-bound exact closure 已接线**（`ArtifactContentV1` 为 sole physical walker/stable-read/hash authority，private inventory 记录 base/extra `role/path/size/contentSha256`；pure `mintEngineeringOutputSetV1` 将 descriptors 与 exact `evidence.json` UTF-8 `evidenceSha256` 绑入 digest；publisher 在 evidence→manifest-last 前后比较 artifact inventory并于 rename 前重验 exact closure；`inspect <output-dir>` stable-read sidecars/listed artifacts、重走 no-follow exact closure并逐 descriptor 比对；legacy path-only `proof-forge.output.v1` fail closed；Python validator/hardlink规则同构；`s7c-disk-closure-gate` 已接入 dev/ci；仅 stable observation，**不是** race-free/hermetic/formal OutputSetV1）；仍非 formal registry root codec/`registryDigest`/SupportClaim/reachable BuildIdentity/`OutputSetV1`；当前四 target public-UInt64 add/sub Plan body虽已直连 retained `SemanticProgramV1`，但 bytecode结果仍不代表完整/formal D4 lowering、formal SupportClaim resolver、formal reference `step`/TST-SEM-002/003 或 formal D3 完成；**D1 frontend 监督层已按 2026-08-01 产品决策移除**（`Frontend/SafeOpen*`、`DarwinSupervisor*`、native C、safe-open worker exe 与 `b12-cli-source-authority-deletion-gate` 一并删除；B10 frontend worker 模块与 standalone optional executable 保留为非产品面；原 B11a/B11a2/B11b1/B11b2/B12 监督式 source-authority 工程切片整体 superseded）；产品 CLI `build`/`check` 现经进程内单次 `IO.FS.readFile` → `Loader.selectProgramV1ProductWithTheoremInventory` → `normalizeProgramLocatedV1` → `compileProgramProductV1` → `certifyInlineProofV1`（proof fail 早于 target resolve/materialize；无 `--proof-bundle*`）；`Frontend/ProtocolV1`/`WorkerV1` 协议与 worker 面保留；contained/host-race formal 资格随该层删除不再适用；formal TASK-D1-07/08 仍 pending |
| Engineering matrix | [`MIGRATION_MATRIX.md`](MIGRATION_MATRIX.md)：D1 wire/hash + located diagnostics + frontend protocol/worker 工程已接线；**前端监督层（B11a/B11a2/B11b1/B11b2/B12）已于 2026-08-01 按产品决策移除**（SafeOpen/DarwinSupervisor/native C/safe-open worker exe/B12 gate 删除；B10 frontend worker 模块与 standalone optional executable 保留为非产品面；CLI 源读取为进程内单次 read + `Loader.selectProgramV1ProductWithTheoremInventory`；contained/formal 资格不再适用）；D2 有 CheckV1 + multi-width Normalize + source invariant→Semantic callable/InvariantDecl/exact fuel/Provenance 工程闭环 + const declaration/body read→constants/Op.Constant/Provenance 工程闭环 + Semantic declaration/field/parameter/invariant name NFC/common identifier gate + admitted ReferenceV1/InvariantABI 工程子集（含公开 `evalInvariantV1`/`InvariantTheoremV1` ABI 定义与 Reference integer/field 语义执行）但 Quint materialization 对 Q0 read-only Bool invariant 已开放，其余八个 target 对 nonempty invariants 仍 fail closed、formal contract 仍 pending；inline same-file theorem narrow engineering gate 已闭合（exact ordinal-0 `InvariantTheoremV1`、package-owned in-process audit、proof-first；非 sandbox/formal/reachability/target refinement）；D3 有工程 `TargetRegistryV1` sole membership/default/profile/descriptor-axis authority + D3-E9 resolve/mint/inspect exact join + single-semantic `CompiledSemanticV1` + exact requirement resolver + S6 capability-only Plan cutover（九个 materializer Plan body均 retained V1，含 Quint/CosmWasm/TON）+ S7a MaterializedArtifactsV1 + S7b FinalizedArtifactsV1 + **S7c/D3-E7 content-bound exact disk closure/manifest-last + inspect revalidation**（无 formal registry root codec/digest、SupportClaim 或 OutputSetV1）；D4 EVM 已有 retained-`SemanticProgramV1` public-UInt64 add/sub Plan 先导并复用既有 IR/Yul/solc/Anvil alpha，但完整 contract 与 5/5 formal task仍 pending |
| Engineering slice | ProgramV1 整数字面量使用 sole expression/pattern decoder（仅 unsigned decimal 或 lowercase-prefix `0x` hexadecimal；hex digits 可大小写；拒绝 `0X`/`0b`/`0o`/underscore；范围 `0..2^256-1`；等值 spelling 的 AST/canonical bytes/sourceHash 相同）、structured `call QualifiedId(args)` / `schedule QualifiedId(args)`、direct `.if_` / bounded `.for_`、local function call / qualified constructor expression / bare-base index access、direct ProgramV1 unary expressions（prefix `-`/`~`/`!`）、direct ProgramV1 arithmetic binary expressions（`+`/binary `-`/`*`/`/`/`%`，含 precedence/left-assoc/grouping/hash binding/source-only div-zero/mod-zero）、direct ProgramV1 shift expressions（`<<`/`>>`，含 same-tier left-assoc、Add/Mul/Unary precedence、grouping/hash binding与source-only shift count 0/64）、direct ProgramV1 equality expressions（`==`/`!=`，含 Shift/Add/Mul/Unary precedence、non-associative chain rejection、grouping/hash binding与raw integer/Bool/place identity）、direct ProgramV1 ordering comparison expressions（`<`/`<=`/`>`/`>=`，含 exact op/source-order/raw escaped identity、Shift/Add/Mul/Unary precedence、与 equality 共享比较层的 non-associative chain rejection、parenthesized nested comparisons 与 grouping/hash identity）、direct ProgramV1 bitwise binary expressions（`&`/`^`/`|`，含 exact op/source-order/raw escaped identity、Compare > bit-and > bit-xor > bit-or precedence、higher unary/arithmetic/shift preservation、left-assoc/right-nested grouping、enum variant pipe coexistence 与 grouping/hash identity）、direct ProgramV1 logical binary expressions（`&&`/`||`，含 `.logicalAnd`/`.logicalOr` exact op、lhs/rhs source-order、Bool/raw escaped dotted place identity、same-tier left-assoc/right-nested grouping、Compare > `&` > `^` > `|` > `&&` > `||` precedence、higher unary/arithmetic/shift preservation、canonical grouping/hash identity、hash non-aliasing、malformed digraph boundaries 与 left-before-right portable-name errors）、ProgramV1 core statements（let/assign/return/assert）、direct ProgramV1 match statements（`match Expr with` + nonempty `| Pattern => do Block` arms，限 wildcard/bind/bool/integer/string literal/constructor patterns，constructor pattern 接受 `QualifiedId(PatternList?)` 并解码为 `.constructor qualified args`，支持空参数、嵌套与混合参数 patterns，保留 source order/raw identity/canonical hash binding；MatchExpr 继续 fail closed）、direct ProgramV1 `revert`/`emit` statement shape tests、direct ProgramV1 string literal expression tests（return/let value、empty/ASCII/escaped quote/backslash/tab/newline/Unicode scalar decoded values、alternate escape canonical identity、grouping identity/hash non-aliasing与 parser-boundary negatives）以及 direct ProgramV1 dot-only field-place chains（bare rvalue/assignment target、multi-field source order、escaped raw component canonical identity、unary/binary operands、ProgramV1 bytes/sourceHash equality与 field-vs-whole-escaped/depth/order non-aliasing，固定 uppercase/lowercase root case-sensitive identity 与 constructor/local-call parentheses classification）和完整 PlaceSuffix chaining（bare/qualified root 后任意混合 `.` field 与 `[Expr]` index suffix 的 `.place (.field/.index ...)` rvalue 与 assignment target，含 escaped component canonical identity、whole-escaped dotted base/index value/suffix order/depth hash binding、nested index expression、bare/field-only/terminal-index identity不变、target→base→earlier suffix→later suffix/index→rhs 错误优先级，继续拒绝 `(x)[0]`/`f()[0]`；legacy Source reader 对 indexed assignment 与 qualified index base 保持 fail closed）以及 direct ProgramV1 expression-level `match Expr with`（`ExprMatchArm` 为 `| Pattern => Expr`，值位置而非语句位置，支持嵌套 match expression、全部现有 pattern kinds、source order/raw identity/canonical hash binding；statement match 与 expression match 通过不同 arm category 严格分离，`do` block 进入 expression arm 与 bare expression 进入 statement arm 均在 parser boundary 失败；legacy Source reader 保持 fail closed）以及 direct ProgramV1 完整 recursive prefix-atom `pfType` 表面（sole `portableType` + `prefixType`：`Option`/`Array`/`Map` same-line prefix 与 sole `collectTypeAtomSyntaxV1`→`decodeTypeV1At` 解码路径（现另 emit canonical-preorder syntax anchors）承载任意嵌套，含浅层 Array/Option/Field/Bytes 与 `Array Map`/`Option Map`/更深 Option·Array 链与 Map key/value 递归；**全部 fixed-depth `@[pfType_parser default+2]` 与 specialized `@[pfAggregateMember_parser default+1]` 已删除**（`pfType_parser==2`、`pfAggregateMember_parser==1`）；state/const/param/result/struct-field（sole `aggregateField` 嵌入 `pfType`）位置、canonical bytes/sourceHash 类型变更绑定、ordinary/escaped named-type equality、recursive-type hash non-aliasing 与 malformed Map/length/constructor 负例矩阵已固定）已进入 ProgramV1 Loader；`NodeTraversalV1` 公开 `typeConstructorTagV1`/`canonicalTypeVisitsV1`（Map key-before-value preorder）；`ProofForgeV2/Source/SpanJoinV1.lean` 已把 immutable parser snapshot 的每个 canonical-preorder ProgramV1 节点精确映射到原始 syntax byte span（Type 子树仅 zip decoder anchors × traversal type visits，无独立 type 分类/递归 walk；透明 syntax 结构跳过、共享 token 共享 span、count/tag/path 与 same-node-count tampered Type 不一致 fail closed），Loader 提供 additive `selectProgramV1WithSpans`（identity/canonical bytes/sourceHash 不变），`Tests/Language/ProgramV1SpanJoin.lean` 固定全 constructor 家族结构对应、命令 span 包含、preorder 单调、注释/布局变体 delta shift、same-length rename 的 identity/position 分离、synthetic/count-mismatch fail closed、recursive Array Map/Option Map type-node span join、Map key-before-value span 序与 Array↔Option/Map key-value swap same-count tamper 拒绝；**B5** 新增 `ProofForgeV2/Source/OriginJoinV1.lean` 为 sole Source-owned exact NodeId×span join（private-constructor opaque `OriginInventoryV1`、length-framed `pathLookupKeyV1`、NodeId-ordered origins、exact path lookup；invalid path/dup·missing·extra spans/count mismatch/invalid origin/dup NodeId fail closed；无 Semantic import）；`Semantic.ProvenanceV1.buildSourceNodeInventoryV1` 仅为 thin projection（删除 Semantic 侧 path-key/span-join 算法），Normalize source-bound rebuild 权威与 wire API 不变；`Tests/Language/ProgramV1OriginJoin.lean` 固定 Source ownership/determinism/tamper；unescaped qualified revert/emit name 在 D2 error/event lookup 迁移前 fail closed；`Tests/Language/ProgramV1DeclarationNegatives.lean` 现已覆盖 direct ProgramV1 declaration negatives（reserved/qualified 声明名、malformed syntax、empty aggregates、duplicates、reserved components、visibility misuse）；ADR-0019 step-4 原子切over 已完成：Lean `program ... where` command 与 persistent export 已切到 ProgramV1，`Syntax.lean` `elab_rules` 经 `decodeProgramCommandV1Checked` 产出 `ProgramExportPayloadV2{schema="proof-forge.program-export.v2", canonical bytes}`，`ProgramExport.lean` 只登记 v2 schema、attribute 要求 `ProgramExportPayloadV2`、reconstruction 仅经 `decodeCanonicalSourceAstBytesV1` 并验证 raw-component identity join，`ProgramPayload.lean` 已删除；Examples 不再暴露 `Source.Program`，product/materialization/core 消费者改用 V1 source-text 路径（`Tests.Fixtures.SourcePrograms` 已删除）；旧 statement/operator/declaration suites（`Tests/Language/*Statements.lean`、`Bitwise*.lean`、`Logical*.lean`、`Checked*.lean`、`Shift*.lean`、`Equal.lean`、`NotEqual.lean`、`Less*.lean`、`Greater*.lean`、`StringLiterals.lean`、`Grouping.lean`、`ConstructorExprs.lean`、`LocalFnCalls.lean`、`IndexAccesses.lean`、`RevertStatements.lean`、`ValueLessReturns.lean`、`BoolLiterals.lean`、`PrimitiveDeclarations.lean` 等）已按 coverage mapping 删除，对应覆盖由现有 `ProgramV1*` direct suites 承载；export/command surface（`ProgramExports.lean`、`ProgramExportAcceptance.lean`、`ProgramExportAcceptanceEmpty.lean`、`ProgramCommandAcceptance.lean`、`ProgramSyntax.lean`）已迁移到 v2 payload positive/negative/boundary/diagnostic-priority 覆盖；**legacy source-reading surface 已移除**：`Syntax.lean` 的 legacy Source decoder 家族（`decodeType`/`Param`/`Expr`/`Statement`/`Item`/`Program` 与 `decodeProgramCommandChecked`）与 `Loader.lean` 的 legacy `parsePrograms`/`selectProgram` API 已全部删除，`parseProgramsV1`/`selectProgramV1` 是唯一 source-reading 入口；`Tests/Language/SourceBoundsAcceptance.lean` 已删除，其 PF-BOUND-001 覆盖迁移到 `ProgramSyntax`/`Loader` 与 ProgramV1 suites；**B3 source-driven full-tag ProgramV1 golden 已冻结**：`testdata/golden/source-program-v1/source-full-tag-v1/{source.lean,canonical.bin,manifest.json}` 为 sole legal `program FullTag` source authority；Lean `Tests.Language.ProgramV1SourceFullTagGolden` 独立对照 `programPayloadV2` command/export 与 `selectProgramV1WithSpans` Loader，固定 identity/canonical bytes/sourceHash/NodeId preorder/span count 与 representative exact spans；独立 Python `scripts/reference_source_program_v1_source_golden.py` 建模 source-derived value/offsets/encoder（无 Lean import）；闭合当前 84 wire / 57 node / 63 edge；constructed `full-tag-v1` 保持不变（负例 mutation base）；**B4 obsolete `call StringLiteral` 已删除**：sole `call`/`schedule` 表面为 structured `QualifiedId(args)`；`syntax "call " str` 与 `pfStmt| call $_callee:str` decoder-rejection 臂归零；legacy `call "…"` 在 Lean parser boundary 以 `failed to parse file` 拒绝（`ProgramV1ExternalStatements`/`ProgramV1ControlFlow`/`ProgramV1Declarations`；无 decoder 识别或 fallback）；alpha Core `synchronousCall` carriers 与 `Core/Source`/`Core/Typed`/`Core/SemanticIR`/`Core/Semantics`/`AlphaCompatibility`/`Tests.Fixtures.SourcePrograms` 已物理删除（sole product 表面为 structured `call`/`schedule` + S2 `effect.synchronous-call`）；`Core/TypedV1.lean` orphan 已一并删除；**B2 ProgramV1 source-bound gate 已恢复**：`Tests/Language/ProgramV1Bounds.lean` resident unit 固定 root-inclusive syntax depth 256/257、nodes 100000/100001、identifier/Root-identity 256/257（Fast/`dev-check` 可跑；不包含 300-term/20k-state/16 MiB 重向量）；`decodeProgramV1Unchecked` 在 programIdentity 组装处对 size>maxSyntaxNesting 直接 `CompileError.resourceBound`/`PF-BOUND-001`（非 shared qnCountError 字符串 remap）；`scripts/program_v1_source_bounds` + `just source-bounds` 在 Darwin 以真实 CLI `--module Root` 子进程校验 300-term/20k-state/exact 16 MiB accept/16 MiB+1 `PF-SRC-INVALID`/combined overflow 精确诊断与零 partial output；Linux ordinary `just ci` 保留 portable bound unit并只断言 Darwin-only product supervisor以 protocol diagnostic/零制品 fail closed（独立于 quarantined `dsl-negative`）；**alpha residual 物理删除**：`Core/Source.lean`/`Core/Typed.lean`/`Core/SemanticIR.lean`/`Core/Semantics.lean`/`Compiler/AlphaCompatibility.lean`/`Tests/Fixtures/SourcePrograms.lean` 及 pure-alpha suites（`Tests/Core/Semantics`/`Tests/Compiler/Pipeline`/`Tests/Compiler/TypedNameIndex`）已删除；test consumers 已迁 V1 或剥 alpha；`Core/TypedV1.lean` 仍 orphan 待后续删除；**B6** `ProofForgeV2/Core/DiagnosticV1.lean` 已原子替换为 ADR-0022/SPEC-DIAG-001 结构化 v1 载体：59-code closed `DiagnosticCodeV1` 全表（exact wire+rank+default severity/phase；无 alpha `PF-SEM-*`/`PF-CLI-USAGE`/`PF-OUTPUT-MANIFEST`）、`DiagnosticOriginV1` nullable `nodeId`（primary Option + related 全序归一）、severity/phase enums、program/target/requirement/extension/expected/actual/context/stableContext/suggestion 全字段、sole PF-JCS encoder+decoder（unknown/trailing/noncanonical fail closed）、message-independent order/dedupe 键 `(path,start,code,stableContext none→"")`、结构 redaction 校验、`normalizeDiagnosticBundleV1` 100/101 cap+`PF-DIAG-LIMIT`（B8a 已由 `DiagnosticBundleV1.mkFailureBundleV1` 复用；B8b product CLI/compiler 接线已完成；formal 仍 pending）；`renderHuman` 仍 `CODE: message`；`errorSentinelNodeId` 已在 **B7a** 删除（pre-node `nodeId=none`；`DiagnosticLocateV1`+`selectProgramV1WithOrigins`+`childPathV1` 已接线；B7b1 NameResolution/CallGraph path drafts 已接线；**B7b2** TypeCheckV1 path drafts 已接线（`typeCheckProgramDraftsV1` additive；public `typeCheckProgramV1`/`typeCheckExpr`/`typeCheckItem` exact erase；primary=offending AST + related=decl contracts via ModelV1 path helpers；sole `childPathV1`/`directChildPathV1`/`indexChildPathV1`；path fail→`pathInternalDraft`）；**B7b3a** EffectCheckV1 path drafts 已接线（sole `checkEffectsDraftsV1`/`checkProgramEffectsDraftsV1` authority；public `checkEffectsV1`/`checkProgramEffectsV1`/`checkProgramEffectsResultV1` exact erase；primary=offending FnDecl/ViewDecl；related=finite causal evidence：direct occurrence sites + effect-propagating LocalCall edges，visit-bounded worklist；6-bit fixed-point 仍为 closed-effect 权威；PF-EFFECT-001 code/message/phase/declaration+EffectKind order/shadowing/duplicate-fn incomplete 不变）；**B7b3b** BoundCheckV1 path drafts 已接线（sole `checkBoundsDraftsV1`/`checkProgramBoundsDraftsV1` authority；public `checkBoundsV1`/`checkProgramBoundsV1`/`checkProgramBoundsResultV1` 与 `collectCycleDiagnostics`/`collectLoopProductDiagnostics`/`checkLoopBoundsInBody` exact erase；cycle drafts 复用 sole `collectFnCallEdgesV1`：primary=min-ordinal FnDecl、related=remaining SCC FnDecls + in-SCC Expr.LocalCall sites；loop-product drafts 单 path-threaded walk：primary=overflowing Stmt.For、related=enclosing callable item + ancestor Stmt.For；PF-BOUND-001 code/message/phase/cycle-before-loop/UInt32 product/post-overflow traversal/ok/analysisComplete/shadowing/duplicate-fn incomplete 不变）；**B7b3c** DisclosureCheckV1 path drafts 已接线（sole `checkDisclosureDraftsV1`/`checkProgramDisclosureDraftsV1`/`checkBodyDrafts`/`checkConstValueDrafts` authority；public `checkDisclosureV1`/`checkProgramDisclosureV1`/`checkProgramDisclosureResultV1`/`checkBody`/`checkConstValue` exact erase；VisibilityEvidence label+finite causes；join 更密 label 保留 causes、等 label stable-union；primary=sink input expr/control；related=value causes∪PC causes∪sink contract/site primary-excluding；PF-VIS-001 code/message/phase/lattice/PC/sink set/source order/ok/analysisComplete/duplicate-fn incomplete 不变；**B7b3cR** if/match PC 与 Expr.Match result taint 经 sole `withControlRootEvidence` 附加 condition/scrutinee Expr root（decl causes 在前；等 label nested 保留双 control roots；更密 outer PC 丢弃 lower-secrecy inner control；primary/erase/code/message/count/order/ok/analysisComplete 不变））；**B7b3d** CheckV1 draft composition + additive located API 已接线（sole `TypedCheckDraftResultV1`/`checkProgramTypedDraftResultV1` authority；legacy exact erase；`checkProgramTypedLocated*` sourceHash 门禁 + all-or-nothing `locateArray`；无 sort/dedupe/cap）；**B7b engineering complete**；**B8a** inert `DiagnosticBundleV1`/`DiagnosticResultV1` 地基已落地（private constructor、sole total `mkFailureBundleV1` 复用 normalize+validate/encode、fail-closed 固定 PF-INTERNAL、read-only diagnostics、human/PF-JCS array render、exit priority；无 Loader/Normalize/Compiler/CLI product caller）；**B8b** product multi-error cutover 已完成；formal TASK-D1-07 仍 pending）。Loader pre-node primary 使用显式 `nodeId=none`（zero-sentinel 已退役）；NameResolution/CallGraph 经 B7b1、TypeCheck 经 B7b2、EffectCheck 经 B7b3a、BoundCheck 经 B7b3b、Disclosure 经 B7b3c `DiagnosticDraftV1` path drafts（public erase）；CheckV1 locate composition **B7b3d 已完成**；**B8a** inert DiagnosticBundle foundation 与 **B8b** product cutover 已完成（formal 仍 pending）；**B9** 新增 inert `ProofForgeV2/Frontend/ProtocolV1.lean`：closed one-frame `Frontend.Req/Ok/Err.v1` binary wire（exact SemVer parser selection、validated ProjectRelativePath、raw UTF-8 module/program selectors 无 NFC gate、raw sourceBytes；success=requestDigest+canonical ValidatedSourceV1 bytes+NodeAssignment preorder spans-only；failure=requestDigest+canonical DiagnosticBundleV1 PF-JCS；maxProtocolBytes 64MiB/source 16MiB/span 100000；full-consume+re-encode identity；reconstruct 仅 decodeCanonicalSourceAstBytesV1+assignNodeIdsV1 zip+joinOriginsV1；`Tests/Frontend/ProtocolV1`；该 B9 slice 当时 inert）；**B9R** 去掉任意 4096-byte selector 语义上限（sole 分配/帧 guard=`maxProtocolBytes`，`maxSelectorBytes` 仅为相等兼容别名；精确 Lean `1..256×1..240` 与源诊断分类推迟到 Loader），并在 `parsePfJcs` 前对 PF-JCS 诊断数组做 O(n)/O(1) top-level 条目预扫描（0 或 >101 拒绝；嵌套/字符串逗号不计；canonical 权威仍为 `mkFailureBundleV1` re-encode identity）；**B10** 新增 standalone `proof-forge-frontend-worker-v1`（one request/process、bounded stdin→exact 1.0.0→UTF-8→single Loader snapshot、Ok/Err exit0、usage/protocol/internal 64/65/70 stable stderr；真实 subprocess tests），Loader worker spans payload 与 product OriginJoin 共享 private parse/select/SpanJoin snapshot；**B11a** 新增 package-owned native safe-open foundation（trusted absolute root 下 component `openat` no-follow/nonblocking/cloexec、directory parents、regular single-link leaf、16MiB pre-read gate、exact-size+one-byte probe、fd/path metadata recheck、closed redacted faults；`Tests/Frontend/SafeOpenV1` 含 permission/symlink/hardlink/FIFO 与 full-only 16MiB±边界；`.lean/.c/.h` package source pin）；**B11a2** 新增 pure private-ctor `DarwinFrontendSupervisorReceiptV1`（sole `darwin-development-observed`、hard/effective profile+digest、optional request digest、bounded observations、closed event/result/cleanup、canonical 11-field PF-JCS/4KiB cap/digest/replay/privacy gates；不 spawn/measure/kill/reap）；**B11b1** 已新增 Darwin development-only worker supervisor execution（仅监督已编码 canonical request；monotonic budget 从 native allocation/pipe/spawn 前开始；selective `LEAN_SYSROOT`/`LEAN_PATH` + fixed env；process/memory/output/deadline observation、process-group kill/cleanup 与实际 B11a2 receipt；malformed/cross-request/incomplete-cleanup fail closed）；**B11b2** 已新增 request-bound closed SafeOpen helper + private native absolute monotonic budget capability，把 open→request construction→frontend worker 纳入同一 wall 并合并 aggregate peaks；仅 exact SafeOpen request binding + canonical fault + complete cleanup mint `sourceOpenFailed`，private supervised carrier保留 closed fault供 `.tooLarge` 精确诊断；**B12 前端监督层已于 2026-08-01 产品决策移除**（进程内 open/Loader 恢复为 CLI 产品路径，原 supervised authority 切面 superseded；contained/formal 资格不再适用），formal TASK-D1-08 仍 pending：sole 产品链为 CLI 进程内单次 read → `Loader.selectProgramV1ProductWithTheoremInventory` → `normalizeProgramLocatedV1` → `compileProgramProductV1` → `certifyInlineProofV1` → CLI full-bundle `renderHuman`/`selectExitCode`；`selectProgramV1*`/`normalizeProgramV1`/`compileValidatedSourceV1` 仅非产品兼容面，raw `*WithDiagnostics` 已删除；`Tests/Core/DiagnosticV1.lean` 与 `ProgramV1Diagnostics` 已迁 primary/related；新增 `ProofForgeV2/Typed/ModelV1.lean` 与 `NameResolutionV1.lean` 已直接消费 `ValidatedSourceV1` 构建 13 类声明 source-order 序数表与显式作用域名解析（body 内 params/let/for/match binders、state、fn-only local calls、struct/enum-only named types），`Tests/Typed/NameResolutionV1.lean` 固定声明表/遮蔽/arm 与 loop 泄漏/错误类别诊断；不做 body type checking，不改动现有 alpha Typed 路径；新增 `ProofForgeV2/Typed/TypeCheckV1.lean` 已实现 D2-01 核心表达式类型检查切片（literal/place/unary/binary/constructor/localCall，整数字面量必须携带预期整数类型并按宽度做精确范围检查，不默认宽度；struct constructor 按 declaration order 检查 field count/types 并产生 `.named structName`，enum variant constructor 检查 payload count/types 并产生 `.named enumName`，local fn call 检查 callee 解析、arg count 与 param types 并传播 fn result 类型；全部 expected-vs-actual 诊断按 source order fail closed，match expression 继续 fail closed），`Tests/Typed/TypeCheckExpressionsV1.lean` 与 `Tests/Typed/TypeCheckCallsV1.lean` 已分别固定 operator/place 与 constructor/local-call 覆盖；新增 statement/body 类型检查切片：let/assign/return/assert/revert/emit/if/for/call/schedule 在 declared result context 下完成精确 TypeV1 检查（annotation/target/result/param 一致、revert-emit error-event lookup 与 count/type、if-assert Bool 条件、for 同宽整数端点与作用域不外泄），init/entry/view/fn/const/invariant bodies 已接线，match typing 继续 fail closed，`Tests/Typed/TypeCheckStatementsV1.lean` 固定正向/负向/作用域/源顺序优先级覆盖；**新增 `Tests/Typed/TypeCheckMatchV1.lean` 已实现 D2-01 match typing 切片：pattern 对 scrutinee 类型的精确检查（wildcard/bind/Bool/integer-width/constructor 子模式、string fail closed）、expression match 全 arm 结果统一、statement match arm body 在 result context 检查、enum exhaustiveness（`_`/bind 或全 variant 覆盖，缺失 variant 按 declaration order 报告）与非 enum scrutinee catch-all 要求，诊断按 source order；**新增 `ProofForgeV2/Typed/CallGraphV1.lean` 已实现 D2-01 call graph + acyclicity 切片：从各 callable body 收集经 scope 规则解析的 `.localCall` 边，构建 fn 序数有向图，按 SCC 检测并按 declaration ordinal 确定性报告全部 cycle（self-recursion 为 1-cycle；entry/view/init 不可能在 cycle 上），`checkProgramStructureV1` 单入口一次通过报告 tables→resolution→acyclicity 全部结构错误；新增 `Tests/Typed/CallGraphV1.lean` 覆盖 acyclic chain、self-recursion、2-/3-fn cycles、entry/view/init→fn、shadowed callee、unreachable cycle 与 cycle+resolution-error phase order**；**新增 `ProofForgeV2/Typed/EffectCheckV1.lean` 已实现 D2-02 bounded effect 切片：AST direct effects（state.read/write、event.emit、external.call.sync、workflow.schedule、failure.revert）、fn/localCall fixed-point transitive closure（含 cycle 多跳上界；duplicate fn 时 analysisComplete=false fail closed）、fn 仅 `failure.revert` / view 仅 `state.read`+`failure.revert` allowlist、entry/init 允许当前可表达 effects、稳定 `PF-EFFECT-001`（`DiagnosticCodeV1.effectDisallowed`），shadowing 正确、诊断 declaration/effect-kind 确定序；`Tests/Typed/EffectCheckV1.lean` 固定正向/负向/transitive/shadow；**不接线 alpha 产品路径、不删旧 alpha view 规则、不做 context/disclosure/extension/bounds；**新增 `ProofForgeV2/Typed/BoundCheckV1.lean` 已实现 D2-03 终止/bound 工程子集：复用 CallGraphV1 边/SCC 拒绝 pure-fn local-call 递归环并稳定报告 `DiagnosticCodeV1.resourceBound`（`PF-BOUND-001`，message 含 declaration-ordinal 排序成员列表；不改动 CallGraph 既有 `.sourceInvalid` cycle 诊断）；检查 init/entry/view/fn 体内嵌套 `for ... bounded N` 的 UInt32 迭代乘积（checked multiply，overflow ⇒ `PF-BOUND-001`；`N=0` 仍有限；不做 `end-start ≤ N` 范围证明）；duplicate fn key 时 analysisComplete=false fail closed 并暴露 structural 诊断；`Tests/Typed/BoundCheckV1.lean` 固定 acyclic/self/2-/3-cycle、entry-view-init 非 cycle、single/double nest ok、三层 4096 乘积溢出、if/match 内 nest、shadowing、human wire 与 duplicate fail-closed；**不接线** alpha `Typed.check`/`Typed.checkV1`/`compileValidatedSourceV1`/CLI，不做 effect-occurrence Semantic、ResourceProfile limits、disclosure/authority；**新增 `ProofForgeV2/Typed/CheckV1.lean` 独立多遍入口**：单次 `resolveProgramV1` 后按 structure（resolution++callGraph）→ type（仅 resolution.ok 时 body）→ effect → bound → disclosure 相位拼接诊断，暴露 `TypedCheckResultV1{diagnostics,ok,analysisComplete}` 与 `checkProgramTypedV1`/`checkProgramTypedResultV1`；duplicate fn 时 analysisComplete=false/ok=false；`Tests/Typed/CheckV1.lean` 固定 Counter 正向、type/effect/bound/disclosure 错误、structure→type→effect→bound→disclosure 相位序与 incomplete；**新增 `ProofForgeV2/Typed/DisclosureCheckV1.lean` 已实现 D2-04a 显式 flow + D2-04b PC-label / implicit disclosure 切片：VisibilityV1 lattice `public_ < commitment < private_`（join=max secrecy，mayFlow 当 secrecy(src)≤secrecy(sink)），bottom-up 表达式可见性（literal public、place/param/state/local/const-as-public、field 继承、index join+public sink、unary/binary/constructor、localCall 结果 public_、**match 表达式结果=join(scrutVis, arm values)**）；语句 walk 线程 PC（entry=`public_`）：`if_`/`match_` 将 `pc'=join(pc,cond/scrutVis)` 传入分支；`assert_` condition public sink（assert⇒failure.revert）且不抬高后续 PC；`for_` 端点 public sink、body 保持当前 PC；每个既有显式 sink 有效源=join(valueVis,pc)；显式 sinks：assign→target、return/emit/revert/call/schedule args→public、for 端点→public、localCall arg→对应 fn `ParamV1.visibility`（tables.fn + NameResolution/TypeCheck shadowing；arity mismatch 仅 walk nested sinks）、const 定义表达式→public；let 记录 RHS 可见性、for binder public_、match binder 继承 scrutinee（value flow）；稳定 `DiagnosticCodeV1.visibilityViolation` → wire `PF-VIS-001`；duplicate fn 时 analysisComplete=false fail closed；**已由 `Typed/CheckV1.lean` 作为 disclosure 最终相位消费**（structure→type→effect→bound→disclosure；产品 `checkV1`/compile 门禁已接线）；不做 authority/custody、commit 算子；新增 `Tests/Typed/DisclosureCheckV1.lean` 固定 Counter 正向、private→private 正向、private/commitment 负向 assign/return/emit/revert/call/schedule/index、localCall public-param 负向与 private→private param 正向、const private-state/return-launder 负向、**match stmt/expr private scrutinee+public arms 负向（PC/result taint）** 与 binder leak 负向、**if private/commitment 条件 public sink 负向**、**private if 下 private→private 正向**、assert condition public sink 负向且不抬高 PC、for 端点 private/commitment 负向与 public 正向、lattice、shadowing、default=public、multi-error 源序、human wire 与 duplicate fail-closed；`Tests/Typed/CheckV1.lean` 另固定 disclosure-only 负向、effect/bound 前于 PF-VIS-001 相位序与 duplicate-fn 不发明 VIS；`CheckV1ProductGate` 固定显式 private return 与 implicit private-if return 产品路径 PF-VIS-001；****located `NormalizeV1` 已 fail-closed 消费 `CheckV1.checkProgramTypedLocatedResultV1`**（structure→type→effect→bound→disclosure；PF-EFFECT-001/PF-VIS-001/PF-BOUND-001/PF-SRC-INVALID 保持结构化 DiagnosticV1）；产品编译成功后直接 sole mint `CompiledSemanticV1`，不再执行 alpha shape/IR lowering；**TypedV1 alpha view write/synchronous-call 重复防御已退役**，产品 fn/view effect allowlist 与 `PF-EFFECT-001` 的 sole 权威为 `EffectCheckV1`（经 located CheckV1→Normalize；legacy `Typed.checkV1`/`Typed.check`/alpha Semantic 路径已物理删除）；**新增 `ProofForgeV2/Typed/RequirementsInferV1.lean` 已实现 D2-05 独立 ProgramV1 requirements inference 工程子集：消费 ValidatedSourceV1/ProgramV1，source-order first-seen stableUnique 产出 private-ctor `RequirementContributionV1` canonical id序列（覆盖 persistent/private/commitment state、witness/disclosure、Bool/Field、checked arithmetic+rollback、sync/async call、event、assert/revert；不发明 callerContext/authority/custody/commit）；由 `Semantic.RequirementsV1.freezeProgramRequirementsV1` sole consume，后者独占 S2 catalog/version/digest/sort与 `ProgramRequirementsV1` mint；不直接接线 CheckV1/CLI，不形成第二 authority；alpha derive 已删除、无产品consumer；`Tests/Typed/RequirementsInferV1.lean` 固定 Counter/state-visibility/param/emit/call/schedule/Bool/idempotent/parity/wire；formal RequirementRef/predicate merge/provenance 仍 pending**；**新增 `ProofForgeV2/Semantic/WireV1.lean` D2-06 wire skeleton + type-shape/FieldSpec + **canonical valueBytes** structure 子集：closed SemanticProgramV1/SemanticProvenanceV1 model + root `pf.semantic.v1`/`pf.semantic-provenance.v1` codec、empty/minimal round-trip、`semanticHashV1`、nested tag tables；**tagged 共享 `withTaggedNesting` fuel=`maxNesting`256**（非 tagged scalar 不耗 fuel；valueBytes 递归形状同 cap）；structure：`decodeSemanticProgramDataV1` 仅 transport（garbage valueBytes 仍可 transport），`encode`/`validateSemanticProgramV1`/`semanticHashV1` 强制 structure gate，`decodeSemanticProgramV1`=transport→structure-gated re-encode→byte identity（非法 structure 在 re-encode 路径失败）；structure 顺序=table id==index → shallow TypeId/CallableId → **SPEC §5 type-shape/FieldSpec/Map-key**（`name=some` iff struct|enum、struct/enum nonempty、integer widths {8,16,32,64,128,256}、Bytes/Array ≤4096、sole catalog `bn254FrFieldSpecV1`/`proof-forge.field.bn254-fr.v1`+exact 32-byte modulusBE → `.badType`；intra-type field/variant name clash → `.duplicate`；Map key 仅 Bool|UInt|Int|Principal|Bytes|Struct-of-legal-keys，fuel=`types.size` → `.badType`）→ **SPEC §5 canonical valueBytes**（Constant/`Op.Literal`/`SwitchCase` 共享 type-driven decode/encode/validate；full-consume + encode(decode)==bytes，否则 `.nonCanonical`；Bool/UInt/Int/Unit/Bytes/Array/Map/Option/Field/Struct/Enum；Map keys unsigned-lex unique ascending；Field value `<` modulus LE width）→ requirements domain/order/predicates；provenance：encode/decode 为 envelope transport；Wire `*JoinV1` 与 Provenance `build*` 为 caller-trusted low-level helpers；权威仅 `NormalizeV1.validateSemanticProvenanceV1`/`semanticProvenanceDigestV1`/`normalizeProgramWithProvenanceV1`（输入 ValidatedSourceV1+ProjectRelativePath+spans，内部 rebuild inventory，不接受 caller inventory；re-normalize carrier bind + exact originMap；coordinated path/span mutation 不能 self-certify）；`semanticProvenanceDigestV1` 先 join 再 SHA-256(envelope)；formal inventory/source-AST recompute 与 CAP registry 仍 pending；`Tests/Semantic/WireV1.lean` 固定 magic/hash/transport 负例、id/index 与 shallow ref structure 负例、**type-shape 正/负**、**valueBytes 正/负（Bool/UInt/Int/Unit/Bytes/Option/Field 0·p-1/Struct/Enum/Map/Array + Literal/SwitchCase；Bool 02/wrong-len/Option 02/Field≥p/Map unsorted·dup·keyLen/Enum OOB/trailing/truncated → `.nonCanonical`；transport garbage valueBytes 仍接受）**、requirements domain/order/predicate/enumContains、nesting>256（含 zero-modulus Field shape wire 仍合法）、provenance incomplete/foreign → `.badProvenance`（完整正向仅 Normalize suite）；Wire suite 仍独立；**S3/B8b** product compile 已由 `compileProgramProductV1` 经 located Normalize structure gate并 sole mint `CompiledSemanticV1`（无 post-Normalize alpha path）；alpha SemanticIR 已物理删除；formal TASK-D2-06/TST-SEM-001 仍 pending；CFG shape/reachability foundation 已加入 `validateSemanticProgramStructureV1`（per-callable entryBlock==0、block id==index、terminator target range、从 entry 的 total reachability，全部 `.badCfg`；**block-param arg arity count portion 已加入 step c.5**：jump/branch/switch target `args.size == target block params.size`，仅 in-range target，OOR 仍归 range check，全部 `.badCfg`；**loopBounds back-edge coverage 已加入 step e）**：exact coverage of every CFG back edge（`(header,backEdgeFrom)` 唯一升序、`maxIterations ≤ 4096`、range 归此处），全部 `.badCfg`；**SPEC §6 per-callable canonical ValueId assignment + use-existence 已加入 step f**：`collectValueDefSites` 严格按三个全局 pass 收集 callable parameters → 全部 block parameters（BlockId order）→ 全部 instruction results（BlockId/instruction order），`checkValueIdCanonicalAssignment` 要求 encountered IDs 精确为 `0..n-1` 并以 O(definitions) time/O(1) extra space 同时拒绝 wrong-start/gap/duplicate/reorder（`.badCfg`）；`collectValueTypeDefs` 使用同一三-pass 顺序，随后才做 use-existence/dominance/def-site typing；`testCfgValueIdCanonicalAssignment` 固定 global-category order、multi-block、per-callable reset、旧 per-block interleaving 拒绝、顺序/gap/sparse 负例、可观察 phase boundaries 与 raw transport accept + structure/encoder/carrier reject，既有 `testCfgValueIdSsa` 继续固定 duplicate/use-existence 回归；**dominance-of-use 已加入 step g**：`cfgPredecessors`（升序唯一 predecessor 列表）、`computeDominators`（迭代 dataflow `dom[b]={b}∪(∩ reachable preds dom[p])`，bounded、非递归）、`checkDominanceOfUse`/`validateCallableDominanceOfUse`（每 use 必须在 dominated-by-def-block，全 `.badCfg`）；`validateCallableCfgShape` 把 step d reachability hoist 供 d/f/g 共享、step f 复用 defSites、step g 加 dominance；`testCfgDominanceOfUse` 固定 DP1–DP5 正向（single-block/callable param/block param self-dom/only-path dominator/branch dominator of both arms）与 DN1–DN3 负向（arm def join use/later-block def earlier use/arm def sibling use，仅 dominance 失败、reachability/arity/loopBounds/SSA def-table 全通过），全 `.badCfg` structure+encode 双路径；既有 `testCfgValueIdSsa` P3 因 dominance 升级为 invalid 已改为 callable param inflow + distinct block param valueId；block-param TYPE/terminator typing 仍 'Not yet'）；block-param TYPE/terminator typing 已加入 step h/i（`collectValueTypeDefs`/`checkDefSiteTypeIdsInRange`：block params + instruction result ValueDef TypeIds in `[0,types.size)`、callable param/result 已由 step 2 覆盖不重复、失败 `.badReference`；`boolTypeId`/`checkTerminatorTyping`：branch cond==Bool TypeId、switch case.typeId==typeOf(scrut)、jump/branch/switch target args positional type==target block params[i].typeId（min-length guard total、arity 归 step c.5）、return (some v) type==callable.result.typeId、return none/revert/trap 不检查、全 `.badCfg`；`testCfgBlockParamTypeAndTerminatorTyping` P1–P5 正/N1–N8 负，N1·N2 `.badReference`、N3–N8 `.badCfg`，structure+encode 双路径），**per-op §5.1 type/result contract (step j) 已加入 value-producing ops（literal/constant/stateLoad/construct/fieldGet/indexGet/unary/binary/pureCall）的 result.typeId 与 operand 类型检查，全部 `.badCfg`；`checkOpTyping` 复用 step h 的 `defTypes`，`uint32TypeId`/`uint8TypeId`/`optionTypeId` shape-resolving helper 与 `serializableType` predicate（eq/ne operand 合法性）平行 `boolTypeId`；construct 解析 Struct/Enum/Array/Option/Unit/empty-Map shape（primitives/Bytes/Principal/Field/uint/int/bool reject）、indexGet 解析 Array/Bytes/Map（UInt32 index、UInt8/Option<value> result）、unary 解析 neg/not/bitNot、binary 解析 arithmetic/eq-ne/lt-le-gt-ge/and-or/bitwise/shl-shr、pureCall 检查 callee.kind==.pureFn 与 param 类型；`testCfgOpTyping` P1–P9 正/N1–N12 负 structure+encode 双路径；**void-op result-presence (step j extension) 已加入：StateStore/Assert/Emit/ExternalCall/Schedule 的 `Instruction.result` 必须为 `none`，spurious `result := some _` → `.badCfg`（structure+encode 双路径，`testCfgVoidOpResultPresence` P1–P5 正/N1–N5 负）；不查 event/error/effectId range 或 argument typing**；**value-producing result-presence (step j extension) 已加入：每个 value-producing op（Literal/Constant/StateLoad/Construct/FieldGet/IndexGet/Unary/Binary/PureCall/FieldSet/VariantTag/VariantPayload/IndexSet/CheckedCast/ContextRead/Commit）必须 `Instruction.result := some _`，缺失 → `.badCfg`（此前 typed 9 family 的 missing result 被静默接受，现 fail closed；7 deferred family 仅 presence-only、exact input/result typing 仍跳过）；`testCfgValueOpResultPresence` P1–P16 正/N1–N16 负 structure+encode 双路径 + 5 个 void result-none 回归**；**`Op.FieldSet` exact §5.1 contract (step j extension) 已加入 `checkOpTyping`：base ValueId type 必须解析为 Struct、fieldIndex 必须在 range、type(value) 必须精确等于 fields[fieldIndex].typeId、result 必须存在且 result.typeId 必须精确等于 type(base)，全 `.badCfg`；FieldSet 从 7 deferred family 独立，余 6 family（VariantTag/VariantPayload/IndexSet/CheckedCast/ContextRead/Commit）仍 presence-only；`testCfgFieldSetTyping` P1（first field）/P2（later field）正与 N1（non-Struct base）/N2（OOR fieldIndex）/N3（wrong value type）/N4（wrong result type）负 structure+encode 双路径，各 negative 隔离 FieldSet typing；`PresP10FieldSet`/`PresN10FieldSet` 已改为合法 Struct base 隔离 presence**；**`Op.VariantTag` exact §5.1 contract (step j extension) 已加入 `checkOpTyping`：base ValueId type 必须解析为 Enum 或 Option、`uint32TypeId` 必须解析到唯一 UInt32 TypeId、result 必须存在且 result.typeId 必须精确等于该 UInt32 TypeId，全 `.badCfg`；VariantTag 从 6 deferred family 独立，余 5 family（VariantPayload/IndexSet/CheckedCast/ContextRead/Commit）仍 presence-only；`uint32TypeId` 改为 uniqueness-gated（仅当恰好一个 `.uint 32` 声明时返回 `some`，重复匿名 UInt32 声明返回 `none` 被 VariantTag 拒绝，作为 TypeKey 结构化 interning 仍 pending 下的保守 fail-closed 替代）；`testCfgVariantTagTyping` P1（Enum base）/P2（Option-some base）/P3（Option-none base）正与 N1（primitive base）/N2（no UInt32 closure type）/N3（wrong result type）/N4（duplicate UInt32 closure type）负 structure+encode 双路径，各 negative 隔离 VariantTag typing；`PresP11VariantTag`/`PresN11VariantTag` 已改为合法 Enum base 隔离 presence**；**`Op.VariantPayload` static exact §5.1 contract (step j extension) 已加入 `checkOpTyping`：Enum base 要求 variantIndex/payloadIndex in range 并返回 selected payload TypeId，Option base 只允许 `(1,0)` 并返回 element TypeId；runtime Enum tag agreement 本切片不检查，D2-07 interpreter 尚未实现/接线且届时必须 trap；`testCfgVariantPayloadTyping` P1（Enum payload）/P2（Option-some）正与 N1（primitive base）/N2（variant OOR）/N3（payload OOR）/N4（Option-none）/N5（Option payload OOR）/N6（wrong result）/N7（empty variant payload）负 structure+encode 双路径；`PresP12VariantPayload`/`PresN12VariantPayload` 已改为合法 Option-some base 隔离 presence**；**`Op.IndexSet` exact static §5.1 contract 已加入：Array/Bytes index 必须为 UInt32、Array value=element、Bytes value=UInt8、Map key/value exact，result.typeId 必须等于 type(base)；runtime Array/Bytes bounds 留 D2-07 且尚未实现；`testCfgIndexSetTyping` P1 Array/P2 Bytes/P3 Map 正与 N1 primitive base/N2 Array index/N3 Array value/N4 Bytes index/N5 Bytes value/N6 Map key/N7 Map value/N8 result type/N9 duplicate UInt8 closure 负 structure+encode 双路径；`uint8TypeId` 改为 uniqueness-gated（重复 `.uint 8` fail closed，同时收紧 Bytes IndexGet/IndexSet）；`PresP13IndexSet`/`PresN13IndexSet` 改为合法 Map base 隔离 presence**；**`Op.CheckedCast` exact static §5.1 contract 已加入：source 与 `toType` 均必须解析为 UInt/Int，result.typeId 必须精确等于 `toType`；运行值 representability 留给尚未实现的 D2-07 checked-revert；`testCfgCheckedCastTyping` P1 UInt→UInt/P2 UInt→Int/P3 Int→UInt/P4 Int→Int 正与 N1 Bool source/N2 Bytes source/N3 Bool destination/N4 Bytes destination/N5 wrong result/N6 missing destination 负 structure+encode 双路径**；**`Op.StateStore` exact static §5.1 contract 已加入：stateId 必须存在、type(value) 必须精确等于 selected state.typeId、result 必须为 none；`testCfgStateStoreTyping` P1 UInt8/P2 Bool/P3 second-state selection 正与 N1 missing state/N2 wrong value/N3 wrong selected-state type 负 structure+encode 双路径，既有 void presence fixture 同步改为类型合法**；**`Op.Assert` exact static §5.1/§6 contract 已加入：condition 必须 Bool，`errorId=none` 要求 args empty，`some errorId` 必须存在且 args 按 ErrorDecl fields positional exact，result 必须为 none；`testCfgAssertTyping` P1 standard/P2 declared two-arg/P3 second-error selection 正与 N1 condition/N2 none+args/N3 missing error/N4 arity/N5 arg type/N6 selected error type 负 structure+encode 双路径**；**`Term.Revert` exact §6 ErrorDecl join 已加入：errorId 必须存在，args 按 selected ErrorDecl fields positional exact；`checkTerminatorTyping` 显式消费 errors table；`testCfgRevertTyping` P1 empty/P2 two-arg/P3 second-error selection 正与 N1 missing error/N2 arity/N3 arg type/N4 selected error type/N5 reversed two-arg positional order 负 structure+encode 双路径**；**`Op.Emit` exact §5.1 EventDecl join 已加入：eventId 必须存在，args 按 selected EventDecl fields positional exact，result 必须 none；`testCfgEmitTyping` P1 empty/P2 two-arg/P3 second-event selection 正与 N1 missing event/N2 arity/N3 arg type/N4 selected event type/N5 reversed two-arg positional order 负 structure+encode 双路径；既有 void presence Emit fixtures 改为合法 empty EventDecl**；**§6 per-callable EffectId canonical assignment 已加入：Emit/ExternalCall/Schedule 按 BlockId/instruction order 从 0 连续编号、每 callable 重置，gap/duplicate/wrong start/reorder 均 `.badCfg`；`testCfgEffectIdOrder` P1 三 effect family/P2 跨 block/P3 per-callable reset 正与 N1 starts-at-one/N2 duplicate/N3 gap/N4 block-order restart 负 structure+encode 双路径**；**ExternalCall/Schedule callee qualified-shape gate 已加入：callee 必须至少两个 components，保留 common QualifiedName 的单 component carrier 但在 Semantic structure gate fail closed；`testCfgExternalCalleeShape` P1/P2 双 component、P3 三 component 正与 N1/N2 单 component 负 structure+encode 双路径；args serializability 因 type domain 尚未逐项规范化而继续 deferred，不复用 Eq/Ne 专用 `serializableType`**；**program root qualifiedName shape gate 已加入：SemanticProgram root 必须至少两个 components，复用 helper 在 encoder table-size 前与 structure step 0 检查，使单 component 对 table-size/table-id 等 mixed-invalid 保持 `.badScalar` 优先；transport decoder 仍只做 scalar transport，carrier re-encode fail closed；`testProgramQualifiedNameShape` 固定双/三 component 正向、单 component structure/encode/carrier 负向及 table-id/table-size precedence**；**Term.Switch canonical case-shape gate 已加入 CFG step b.5：cases 必须 nonempty，零 case（有/无 default）均 `.badCfg`，必须由 normalizer 物化为 Jump；`testCfgSwitchCasesNonempty` 固定单 case 有/无 default 正向与零 case 负向，SSA undefined-switch fixture 已迁为 nonempty case 以避免 test theater**；**Term.Switch typed case-value uniqueness 已加入 CFG step b.6：每个 Switch 内 `(typeId,valueBytes)` 必须唯一，target/args 不参与 key；实现先生成 typed raw keys、private qsort 后相邻比较，避免 quadratic scan且不改 public source order；`testCfgSwitchCaseValueUniqueness` 固定 Bool/UInt8 distinct、跨两个 reachable Switch 同 key 重用正向，以及同/异 target、异 target args duplicate 负向与 malformed duplicate `.nonCanonical` phase precedence**；**callable kind/name presence 已加入 signature step 4.25：initializer 仅 `name=none`，entry/view/pureFn/invariant 仅 `name=some`，在 canonical values 后、CFG 前失败 `.badCfg`；`testCallableKindNamePresence` 固定五 kind 正向、逐 kind wrong Option shape 负向及 canonical-value-before-signature/ signature-before-CFG mixed-invalid phase precedence；name grammar 由后述 shared structure gate 统一检查**；**named callable exact-string uniqueness 已加入 signature step 4.25：所有 `some name` 在 unified callable table 内必须唯一，initializer `none` 不参与；private UTF-8 qsort + adjacent exact equality 不改 source order，重复 `.badCfg`；`testCallableNameUniqueness` 固定五 kind distinct、case-sensitive 正向、same-kind/cross-kind duplicate 负向及 shallow-ref→canonical-value→uniqueness→CFG precedence；identifier grammar 由后述 shared structure gate 统一检查**；**per-callable parameter-name exact-string uniqueness 已加入 signature step 4.25：每个 callable 独立 private sort，重复 parameter name `.badCfg`，不同 callables 可复用同名；`testCallableParameterNameUniqueness` 固定 distinct/per-callable reset/case-sensitive 正向、pureFn/entry duplicate 负向及 shallow-ref→canonical-value→uniqueness→CFG precedence；parameter grammar 由后述 shared structure gate 统一检查**；**named TypeDecl exact-string uniqueness 已加入 type-shape step 3：收集 Struct/Enum 的 `some name` 并复用 shared private UTF-8 qsort + adjacent exact equality checker，重复 `.duplicate` 且不改 types source order；`testNamedTypeNameUniqueness` 固定 anonymous-only、Struct/Enum distinct/case-sensitive、exact wire source-order round-trip正向、cross-shape duplicate 负向及 shallow-ref→type-shape→named-type-name→canonical Constant/Op.Literal/SwitchCase valueBytes→callable-signature→CFG precedence；identifier grammar 由后述 shared structure gate 统一检查；anonymous canonical rank/order、usage closure与完整 TypeKey closure仍 deferred**；**Constant-name exact-string uniqueness 已加入 name step 4.2：constants table 内将 names 复制到 private array，以 UTF-8 qsort + adjacent exact equality 去重，重复 `.duplicate` 且不改 source order；`testConstantNameUniqueness` 固定 empty/distinct/case-sensitive 正向、duplicate 负向及 shallow-ref→canonical Constant/callable valueBytes→Constant-name→callable-signature→CFG precedence；Constant identifier grammar 由后述 shared structure gate 统一检查**；**logicalState StateDecl-name exact-string uniqueness 已加入 name step 4.2：与 Constant-name 同属不暴露组内 rank 的 same-error `.duplicate` phase；logicalState table 内 names 复用 shared private UTF-8 qsort + adjacent exact equality checker，重复 `.duplicate` 且不改 source order；`testLogicalStateNameUniqueness` 固定 empty/distinct/case-sensitive 正向、duplicate 负向及 shallow-ref→canonical Constant/callable valueBytes→logicalState-name→callable-signature→CFG precedence；StateDecl identifier grammar 由后述 shared structure gate 统一检查**；**events EventDecl-name exact-string uniqueness 已加入 name step 4.2：复用 shared private UTF-8 qsort + adjacent exact equality checker，并与 Constant/StateDecl/per-declaration interface-field names 同属不暴露组内 rank 的 same-error `.duplicate` phase；`testEventNameUniqueness` 固定 empty/distinct/case-sensitive、exact wire source-order round-trip正向、duplicate 负向及 shallow-ref→canonical Constant/Op.Literal/SwitchCase valueBytes→duplicate-name phase→callable-signature→CFG precedence；EventDecl identifier grammar 由后述 shared structure gate 统一检查**；**errors ErrorDecl-name exact-string uniqueness 已加入 name step 4.2：复用 shared private UTF-8 qsort + adjacent exact equality checker，并与 Constant/StateDecl/EventDecl/per-declaration interface-field names 同属不暴露组内 rank 的 same-error `.duplicate` phase；`testErrorNameUniqueness` 固定 empty/distinct/case-sensitive、exact wire source-order round-trip正向、duplicate 负向及 shallow-ref→canonical Constant/Op.Literal/SwitchCase valueBytes→duplicate-name phase→callable-signature→CFG precedence；ErrorDecl identifier grammar 由后述 shared structure gate 统一检查**；**event/error interface-field exact-string uniqueness 已加入 name step 4.25：每个 EventDecl/ErrorDecl 将 field names 复制到 private array，以 UTF-8 qsort + adjacent exact equality 去重，重复 field name `.duplicate`，跨 declaration 与跨 event/error kind 可复用同名且不改 source order；`testInterfaceFieldNameUniqueness` 固定 distinct/per-declaration reset/case-sensitive 正向、event/error duplicate 负向及 shallow-ref→canonical-value→field-uniqueness→callable-signature→CFG precedence；interface identifier grammar 由后述 shared structure gate 统一检查**；**initializer signature step 4.3 已闭合 cardinality + result shape：source-order 允许零或一个 initializer，第二个 initializer `.badCfg`；每个 initializer result TypeId 必须解析为 Unit 且 visibility 必须为 public，否则 `.badCfg`；两项均在 CFG 前检查；`testInitializerCardinality` 固定 0/1、initializer+entry、double initializer 与 later-CFG mixed-invalid，`testInitializerResultShape` 固定无 initializer/Unit-public 正向、Bool/private/commitment 负向及 shallow-ref→canonical-value→signature→CFG phase precedence**；**invariant callable result shape 已加入同一 signature phase：每个 `.invariant` result TypeId 必须解析为 Bool 且 visibility 必须为 public，否则 `.badCfg`，非 invariant callable 不受该 gate 影响；`testInvariantResultShape` 固定 Bool-public 与其他 kind 正向、Unit/private/commitment 负向，以及 shallow-ref→canonical-value→signature→CFG phase precedence；closure/op/fuel 与 `invariantSteps` 仍 deferred**；**invariant zero-parameter signature 已加入同一 phase：仅 `.invariant` callable 要求 `params.isEmpty`，非 invariant callable 不受影响，违规 `.badCfg`；`testInvariantParameterShape` 固定 initializer/entry/view/pureFn 单参数与 invariant 零参数正向、invariant 非空参数负向，以及 shallow param-ref→canonical-value→signature→CFG mixed-invalid precedence；closure/op/fuel 与 `invariantSteps` 仍 deferred**；**InvariantDecl exact join 已加入 signature phase：`.invariant` callables 按过滤后的 callable source order 与 `invariants` rows 一一对应，row `callableId`、referenced kind 与 name 必须 exact；missing/extra/wrong-kind/name/reordered/duplicate binding 均 `.badCfg`；`testInvariantDeclarationJoin` 固定 empty/one/mixed-callable source-order 正向及 range→canonical-value→join→CFG phase precedence；closure/fuel 与 `invariantSteps` 仍 deferred**；**invariant root `loopBounds=[]` gate 已加入 signature phase：`.invariant` 禁止任何 static loop bound，其他四种 callable 仍按 generic exact bounded-loop CFG contract；`testInvariantLoopBoundsShape` 固定 initializer/entry/view/pureFn 合法 self-back-edge 正向、invariant empty 正向、invariant exact bounded back-edge 负向及 canonical-value→signature→CFG precedence；完整 closure DAG/op/fuel 与 exact `invariantSteps` 仍 deferred**；**non-closure-by-kind `invariantSteps` absence 已加入 signature phase：initializer/entry/view 不可能成为 invariant root 或 `Op.PureCall` callee，必须携带 `none`，任意 `some` 均 `.badCfg`；`testNonClosureCallableInvariantSteps` 固定三 kind `none` 正向、实际 pureFn→invariant closure exact steps 3/6 的 `some` scope 正向、`some 1`/`some 0`/`some UInt64.max` 负向，以及 canonical-value→fuel-metadata→CFG def-site 的可区分 precedence；pureFn closure membership、invariant root exact step computation、DAG/op closure 与 10M ceiling 仍 deferred**；**rootless pureFn `invariantSteps` absence 已加入同一 signature phase：若 callables 中没有 `.invariant` root，则每个 `.pureFn` 都可证明不属于任何 invariant closure，必须携带 `none`，任意 `some` 均 `.badCfg`；`testNonClosureCallableInvariantSteps` 新增 rootless pureFn `none` 正向、`some` 负向及 canonical-value→rootless-fuel→CFG def-site 可区分 precedence，并保留实际 pureFn→invariant closure steps 3/6 的 `some` 正向防止 scope broadening；有 root 时未被调用 pureFn 的 membership、exact computation、DAG/op closure 与 10M ceiling 仍 deferred**；**invariant root `invariantSteps` presence 已加入 signature phase：每个 `.invariant` callable 必须携带 `some`，`none` 以 `.badCfg` fail closed；本切片只验证 presence，不验证数值。`testInvariantRootStepsPresence` 固定单 literal root 的 exact `some 3` 正向、`none` 负向及 canonical-value→root-fuel→CFG def-site 可区分 precedence；通用 `cfgCallableKindName` 对 simple invariant root 物化 exact `some 2` 以迁移既有 signature suites；pureFn membership、exact step computation、DAG/op closure 与 checked accumulation 仍 deferred**；**`invariantSteps` intrinsic 10M ceiling 已加入 post-CFG/pre-requirements phase：新增 public `maxInvariantStepsV1=10000000`，对所有 present metadata（invariant root 与 pureFn closure member）要求 `≤` ceiling，超界 `.badCfg`；`testInvariantStepsIntrinsicCeiling` 固定 exact simple root 正向、ceiling equality 穿透至 later `.badRequirement`、root/pureFn 10000001 负向，以及 canonical-value→CFG→ceiling→requirements 可区分 precedence；exact computation、membership、DAG/op closure 与 checked accumulation 仍 deferred**；**invariant root direct forbidden-op subset 已加入 post-CFG closure phase：generic `checkOpTyping` 先运行，随后 `validateInvariantRootDirectOpsV1` 仅扫描 `.invariant` body 并拒绝 direct `Op.StateStore`/`Op.ContextRead`/`Op.Commit`/`Op.Emit`/`Op.ExternalCall`/`Op.Schedule` `.badCfg`；StateLoad 与对应 entry op 保持正向。`testInvariantRootStateStoreProhibited`/`testInvariantRootContextReadProhibited`/`testInvariantRootCommitProhibited`/`testInvariantRootEmitProhibited`/`testInvariantRootExternalCallProhibited`/`testInvariantRootScheduleProhibited` 固定双路径 scope、CFG/op typing→closure restriction→intrinsic fuel→requirements precedence；non-wire `validateCfgInvariantPhasesV1` 由真实 structure gate 消费并保留 phase observation，使共享 `.badCfg` 的相邻 phase 顺序可测试；**exact transitive pureFn closure membership metadata gate 已加入同一 post-CFG phase**：从全部 invariant roots 按 `Op.PureCall` source order 做 bounded worklist reachability，每个 callable 最多入队一次；pureFn 当且仅当属于任一 invariant closure 时必须 `invariantSteps=some`，否则必须 `none`，generic CFG/op typing 先于 membership，membership 先于 intrinsic fuel/requirements；`testInvariantPureFnClosureMembership` 固定多跳、双 root disjoint closure、unused-none 正向、unused-some/transitive-member-none 负向与 phase precedence；**reachable invariant-closure `Op.PureCall` call-graph DAG gate 已加入**：复用 exact membership，以 bounded Kahn indegree traversal 拒绝 reachable self/2-node cycle，duplicate static edges 独立计数，unreachable pureFn cycle 不受本 gate 影响；`testInvariantPureFnClosureDag` 固定 acyclic/duplicate-static-edge/unreachable-cycle 正向、self/two-cycle 负向及 CFG→DAG→fuel precedence；**invariant-closure callable CFG no-back-edge gate 已加入**：复用 exact membership 与 canonical `cfgBackEdges`，在 generic loopBounds exact coverage 后拒绝任何 reachable closure-member back edge，unreachable pureFn bounded loop 保留 generic 正向；`testInvariantClosureCfgBackEdges` 固定 acyclic/unreachable-loop 正向、direct/transitive reachable loop 负向及 CFG→closure-CFG→fuel precedence；**invariant-closure pureFn forbidden-op allowlist 已闭合**：exact reachable closure pureFn 内 `.stateLoad`/`.stateStore`/`.contextRead`/`.commit`/`.emit`/`.externalCall`/`.schedule` 均拒绝，invariant root direct StateLoad 与 unreachable pureFn 对应 ops 保持相应正向，generic op typing 先于 closure restriction；`testInvariantClosurePureFnStateLoadProhibited`/`testInvariantClosurePureFnStateStoreProhibited`/`testInvariantClosurePureFnContextReadProhibited`/`testInvariantClosurePureFnCommitProhibited`/`testInvariantClosurePureFnEmitProhibited`/`testInvariantClosurePureFnExternalCallProhibited`/`testInvariantClosurePureFnScheduleProhibited` 固定 direct/transitive reachable 负向、unreachable 正向及 CFG→closure→fuel→requirements precedence；**exact checked `computedInvariantSteps` 已加入**：在 closure DAG/CFG/op restrictions 后以 reverse Kahn leaf→caller 线性传播，精确计算 `1 + Σ(block.instructions.size + 1) + Σ(each static PureCall occurrence calleeSteps)`，duplicate call occurrence 独立计数，所有加法在 schema 10M ceiling 下 checked fail-closed，carried metadata 必须逐 callable exact 相等；`testInvariantStepsExactComputation` 固定 leaf/direct/duplicate/multi-block 正向、callee/root/duplicate undercount 负向、computed>10M 与 CFG→closure→fuel→requirements phase；interpreter 仍 deferred**；**named TypeDecl contiguous-prefix rank 已加入 structure step 3**：全部 `name=some` Struct/Enum 必须占据 `types[0..namedCount)` 连续前缀，anonymous 后再出现 named 以 `.nonCanonical` fail closed；`testNamedTypePrefixRank` 固定 anonymous-only/all-named/named-prefix 正向、interleaving 负向、transport-only 接受与 carrier 拒绝、table/ref/shape/FieldSpec/Map-key→namedPrefix→primitive/recursive→named-name/value/signature/CFG/requirements 相位；既有 type-shape/valueBytes/CFG-op fixtures 已机械迁移到 named-first TypeId；**primitive anonymous TypeKey leaf interning 已加入**：Bool/同宽 UInt/同宽 Int/Principal/Unit/同长度 Bytes/exact FieldSpec 同 shape 只能一个匿名 TypeId，private canonical TypeShape-byte sort 检测 duplicate 并在 shape legality 后以 `.nonCanonical` fail closed；**recursive anonymous Array/Map/Option structural-class uniqueness 已加入**：以 iterative gray/black DFS + fixed-size signature/class ID HashMap interning（lookup/insert only、无 map iteration、no nested-key expansion）解析 child structural identity，anonymous container duplicate 与不经过 named anchor 的 anonymous cycle 均 `.nonCanonical`；`TypeKeyValidationPhaseV1` 固定 namedPrefix→primitiveLeaf→recursiveAnonymous→namedBodyCycle 同错误相位，10k acyclic Option chain 固定线性空间 shipped structure/encoder path；**named-body Option-cycle legality 已加入**：从 TypeId graph移除 Option nodes/incident edges后以 explicit-stack gray/black DFS要求 induced graph acyclic，结合 earlier anonymous-cycle gate保证 accepted cycle同时含 reserved named key与Option，direct/Array/Map/mutual/enum/sibling-branch no-Option cycle均 `.nonCanonical`，5k named chain与1k-variant Enum固定 O(V+E) 资源边界；anonymous canonical rank/order、usage closure/missing/unreferenced 仍 deferred**；**ContextRead same-key result-TypeId consistency 已加入**：全program按 callable/block/instruction source order以exact SchemaId lookup/insert固定一key一result TypeId，mismatch `.badCfg`并处于generic CFG后、invariant closure/fuel/requirements前的`.cfg`相位；raw transport仍接受而structure/encoder/carrier拒绝，2k occurrence固定线性资源边界；**SPEC §6 declaration/field/parameter/invariant name gate 已加入 structure step 4.4**：`Core.Common.validateIdentifierComponent` 是 sole shared rule（Unicode 17 NFC、UTF-8 1..240、非 exact `_`、`Lean.isIdFirst`/`Lean.isIdRest`），覆盖 named Struct/Enum、field/variant、constant/state/event/error 及 interface field、named callable/parameter、InvariantDecl；initializer `name=none` 保留；既有 uniqueness/InvariantDecl join → name `.badScalar` → CFG/requirements 相位固定，transport 仍只做 bare-string NFC，structure/encode/carrier 三路 fail closed；`Tests.Core.CommonRemaining`/`Tests.Semantic.WireV1` 固定全类别、边界、mixed-invalid 与 transport 负例；**非** formal TASK-D2-06/TST-SEM-001；尚缺 full TypeKey rank/reachability/provenance join/normalizer/product wire、ExternalCall/Schedule argument serializability、ContextRead requirement-to-result binding/key capability support与Commit exact disclosure contract**；**SPEC §6 aggregate callable entry/view presence gate 已加入 signature step 4.25 后、initializer/invariant signature 前：`callables` 必须至少一个 `.entry`/`.view`，否则 `.badCfg`（O(callables)/O(1) source-order scan，`decodeSemanticProgramDataV1` transport 仍 permissive）；`testCallableEntryViewPresence` 固定 entry only/view only/initializer+entry/pureFn+invariant+later view 正向、zero callables/initializer only/pureFn only/invariant only/initializer+pureFn+invariant 负向（structure+encode 双路径）、kind-name/duplicate-name/duplicate-param before aggregate 与 absence-before-dup-initializer/bad-invariant-sig/generic-CFG/bad-requirements 稳定相位、hand-built zero-callable envelope transport accept + structure/encode/carrier reject；既有 valid/later-phase structure-gated fixture 已迁移（`programWithTypes`/`programWithState`/`programWithEvents`/`programWithErrors` 追加 minimal `entry_gate` callable，相关正向结构测试改用 `minimalValidProgram`）；`emptyProgram` 仍用于 raw transport fixture 与故意在新 aggregate gate 前由更早 structure phase 拒绝的负例**；**新增 `ProofForgeV2/Semantic/NormalizeV1.lean` narrow public-UInt64 Semantic normalizer 纵切面工程子集**：直接消费 `ValidatedSourceV1`，门禁 `Typed.CheckV1.checkProgramTypedResultV1`（`ok=true`∧`analysisComplete=true`，typed-not-ok 不产出 carrier），将 shipped narrow public-UInt64 ProgramV1 子集（public UInt64 state、init/entry/view、bare-place assign/return、**expected-type UInt64 integer literal**（exact 8-byte LE `Op.Literal`）、binary checked `+`/`-`、anonymous UInt64+Unit）降低为 `SemanticProgramDataV1` 并**仅经** `encodeSemanticProgramDataV1` structure-gated 路径返回 `SemanticProgramV1`；单块 CFG（entryBlock=0/block id=0）、empty loopBounds/invariantSteps、empty constants/events/errors/invariants；S2 exact requirements freeze；unsupported shape fail closed 于 normalizer 边界，**无** alpha Typed.Program/Semantic.Program bridge；S2 已闭合 requirements freeze + provenance join（非 CLI-facing provenance）；**S3** product `compileProgramProductV1` 已经由 `normalizeProgramLocatedV1` 完成 located Normalize structure gate；`compileValidatedSourceV1` 仅非产品 API；套件托管于 `Tests/Typed/CheckV1.lean` 内 absolute namespace `Tests.Semantic.NormalizeV1`（Loader→CheckV1→Normalize→validate/decode/semanticHash、CFG op 序列、state-after-init、param-shadow assign、determinism、typed-not-ok/CheckV1-ok unsupported fail closed；由 `Tests.Typed.CheckV1.run` 调用以覆盖 ordinary CI 与 `Tests.Fast`，无 lakefile/Tests.lean 注册编辑；`Tests/Semantic/NormalizeV1.lean` 为 contract 文档指针）；formal TASK-D2-06/TST-SEM-001 与 D1–D4 仍 pending；S2 exact ProgramRequirementsV1 freeze + SemanticProvenanceV1 complete join on NormalizeV1 Counter path（`RequirementsV1` 闭环 catalog 三键 SPEC wire 序、`ProvenanceV1` production inventory + fail-closed `validateSemanticProvenanceV1`；S2 requirements/provenance join 仍非 CLI-facing；formal TASK-D2-05/D2-06/TST-SEM-001 与 D1–D4 仍 pending）；**S3/B8b：`Compiler.compileProgramProductV1` 是唯一产品编译入口，消费 Loader 单次 parse 产出的 `ValidatedSourceV1`+`OriginInventoryV1`，先经 `NormalizeV1.normalizeProgramLocatedV1` structure gate（located CheckV1 ok∧analysisComplete → current public-UInt64 SemanticProgramV1 encode），成功后只经 `finishCompiledSemanticV1` 绑定 artifact name、canonical ProgramV1 source Digest与 `semanticHashV1` Digest；无 alpha-only fallback/parity/flag/dual reader/adapter；当前 public-UInt64 envelope 外（private/commitment/call/fn/aggregates/Bool·String·非UInt64 literal 等）产品 compile 以稳定 `invalidProgram`/`PF-SRC-INVALID` 失败；CheckV1 type/effect/bound/disclosure 线码与相位优先仍经 Normalize `.typedNotOk` 保持；resolver、四 target consumers与artifact identity全部只读该单 carrier；formal D2/D3 与 maturity 仍 pending**； **D3/S4 工程切片：sole static build-selection authority 已交付**（`ProofForgeV2/Core/TargetIdentityV1.lean` opaque `TargetId`/`CodegenProfileId`/`NetworkProfileId` + internal `TargetKind`；`ProofForgeV2/Targets/BuildSelectionV1.lean` `StaticBuildSelectionIndexV1`/`StaticBuildRegistrationV1`/`resolveBuildSelectionV1`→private-ctor `ResolvedBuildSelectionV1`；八 implemented + 三 design-only；CLI build 接受 optional `--profile`，`--network` 仍 usage error；list-targets/`inspect <target>` 同源 index；ad-hoc `build-counter`/`describe-target` 已删除；无 raw-TargetId product materialize overload、无 public target-local `materialize(program)`；`Tests/Materialization/BuildSelectionV1.lean` 覆盖 grammar/index/resolve/aggregate/CLI/Counter identity；**不是** formal `TargetRegistryV1`/`registryDigest`）；**D3/S5 工程切片：`CompiledSemanticV1` single-semantic carrier + engineering exact requirement resolver cutover 已交付**（`CompiledSemanticV1` private-ctor sole mint `finishCompiledSemanticV1`，由产品 `compileProgramProductV1` 与非产品 `compileValidatedSourceV1` 共享；carrier仅绑定 retained semantic、artifact name与canonical source/semantic Digests，无 S2↔alpha bridge；`RequirementResolverV1` 十一行静态 requirement support index；private-ctor `ResolvedEngineeringBuildV1` sole mint `resolveEngineeringRequirementsV1`；`materializeResult`/`emitProgram`/CLI build 仅 capability，已删 `checkSupport` 与 selection+compiled overload；describe-target 同源 S2 request identities；S5 历史 residual `Common.resolve` alpha contains 已于 **S6 删除**（不再作为 post-capability backend defense 或 product acceptance）；`Tests/Materialization/RequirementResolverV1.lean`；**不是** SupportClaim/formal resolver/BuildIdentity/OutputSetV1/SemanticProgramV1 直连 Plan lowering/formal D3）；SupportClaim、SemanticProgramV1 直连 Plan lowering、full TargetSemantics/CodegenProfile payloads/digests、BuildIdentity、network registry、OutputSetV1 与 formal D3 仍 pending；**D2-07 工程子集（非 formal）**：新增 `ProofForgeV2/Semantic/InvariantABI.lean`（`LogicalStateV1`/default/initial/`stateConformsBoolV1`；slot encode/decode 经 `validateValueBytesV1`；**工程**公开 `evalInvariantV1`/`InvariantTheoremV1` 已存在且有 kernel/suite，**formal** TASK-D2-07/TST-SEM-002/003 仍 pending）与 `ProofForgeV2/Semantic/ReferenceV1.lean` admitted primitive/effect 参考机（`admitReferenceProgramSliceV1` + `stepReferenceSliceV1`，**不**导出正式 `step`；直接消费 validated SemanticProgramV1，不经 alpha）；S1 Normalize Counter init/entry/view/overflow trace、ordered Emit/ExternalCall/Schedule + per-EffectId occurrence、external response cursor/terminal exhaustion、overlay rollback、bounded-loop/standard revert 已由 `Tests/Semantic/InvariantABI.lean` 与 `Tests/Semantic/ReferenceV1.lean` 固定；umbrella `ProofForgeV2.lean` 在 WireV1 后 import 两模块；alpha `Core/Semantics.lean` 已物理删除；**无** product CLI/materializer 接线；缺口：general loop/resource profile、target differential/full formal corpus（context/commit、Int/Field/Principal/aggregates/index 已由 N2a–N6/EvmIndex 系列补入）；formal TASK-D2-07 与 TST-SEM-002/003 **仍 pending（不声称 passed）**；正式 D2 仍 pending |
| Active task | **Noir ACIR / circuit IR lane**（Aleo Instructions + Psy DPN idle residual；其他 target 扩张仍暂停）：权威物化目标 = **ACIR / nargo compile 电路产物**，**非**长期 sole `.nr` 源包。规划：[`docs/targets/07-noir-acir-lowering.md`](docs/targets/07-noir-acir-lowering.md)。**NOIR-IR-0 规划已落**；**NOIR-IR-1 金样已冻结**（`testdata/golden/noir-acir-v1/` path-normalized ProgramArtifact multi-file inventory + `InventoryV1`/`NoirAcirV1`；bytecode=base64(gzip ACIR)，**未** opcode decode）；**NOIR-IR-2 Plan→ACIR MVP 已接线**（路径决策 = **nargo-assisted** `Acir/CaptureV1`；Counter product Plan source-join ≡ golden product + live capture circuit core ≡ golden；`.nr` 过渡；IR-6 前无产品 ACIR OutputFile；**现 IR-6 已开 opt-in dual-write**；**IR-7/G6 prove honesty PARTIAL+MISSING**（`just noir-runtime` → `PF-TOOLCHAIN-MISSING`；Lane idle）；**NOIR-IR-3 / G3 已接线**（`CaptureV1.admitSurfaceFixturesV1` circuit-hash pins：BranchCounter if、LoopSum for、OptionState、ArrayRet 全捕获；MapMini init pin + put/get nargo type residual honesty；package-stem 恒跑，nargo 缺席 live capture honest skip；Counter 金样回归）；**NOIR-IR-5 / G5 轻量诚实矩阵已接线**（§3.2 状态列 Y/P/F；call/schedule **P** witness-binding only 禁止 ACIR Y；String/Option non-UInt64 **F** plan-FC；prove/VK **F** Finalize deployable=false；无假 Y）；**NOIR-IR-6 / G4 已接线**：默认 `noir-source-u64-relations-v1` zero-tool Finalize；显式 `noir-nargo-1.0.0-beta.26-acir-v1` dual-write path-normalized ProgramArtifact `finalized-extra`（`nargo-compile/{stem}/*.json`；缺 nargo `PF-TOOLCHAIN-MISSING`；Counter ≡ 金样；`deployable=false`；`.nr` transitional/debug base）。**NOIR-IR-7 / G6 prove honesty PARTIAL+MISSING 已接线**（Tool Lock `barretenberg=null`；`scripts/noir_runtime_test.sh` + `just noir-runtime` → `PF-TOOLCHAIN-MISSING`；不发明 bb/CRS；prove/VK 仍 F；Lane **idle**）。现状产品 default 仍 relation Plan → Noir source；ACIR 产品 dual-write 经 opt-in profile + locked nargo 1.0.0-beta.26。既有 Solana **#110 engineering epic complete** / **#111–#125 engineering closed** checkpoint 保留但非当前 capability 扩张面：active `irSchema=proof-forge.solana.cpi-product-ir.v1`，profile digest `b0f3f5bc7f3973daf176c308cc4ca310f8ad5b51ea33a33c9d1bd3e4d3e91b04`，catalog digest `e2c2ebac5e690b99ad50fb7f8a5f6ecfdb8295bb43f3913229c2fd48d2820419`，SBOM 244；#125 baseline **13** test binaries / **304** active tests，current **21 integration test binaries / 405 active tests**。formal/UPS/network/deploy/hermetic 状态不变。 |
| Next task | **Noir ACIR lane idle**（optional residual only）：IR-4 multi-fixture inventory；未来真实 Barretenberg/backend Tool Lock pin 后的 Counter prove 扩展（仍 host-heavy / 非 ordinary ci / 非 product prove）。 |
| Known blocker | **产品决策债**：`B-CALL-SEM`（call/schedule capability 与真实平台语义对齐）、`DOC-ADR-SCOPE`（accepted PRD 四目标未自动扩大 vs engineering 9 implemented (including Quint/TON)；frontend assurance 取舍）仍 open；**E4 residual**：MiniAmmAssets 双链 runtime（Solana Mollusk + EVM Anvil dual ERC-20，strict deploy）已闭；其余 residual 为 formal TASK-D5 / hermetic / mainnet，非工程双链门；**release qualification**：`TASK-D1-01` 的 formal TaskQualification/eligible-host 前置仍未满足，三者互不代签。 |
| Recovery authority | [`RECOVERY.md`](RECOVERY.md) |
| Engineering backlog | [`docs/engineering-backlog.md`](docs/engineering-backlog.md) |
| Task authority | [`docs/04-task-breakdown.md`](docs/04-task-breakdown.md) |
| Document authority | [`docs/document-status.md`](docs/document-status.md) |
| Phase 1 registry targets | `evm`, `solana`, `near`, `noir`, `aleo`, `psy`, `quint`, `cosmwasm`, `ton`（9 implemented materializer；accepted PRD 仍以四目标为 Phase-1 文案，工程扩面不自动改写 accepted 范围——见 DOC-ADR-SCOPE） |
| Design-only targets | `soroban`, `icp`, `openvm` |

> **2026-08-02/03 correctness/audit update**：普通 CI 已补注册 Authority/Custody、
> Context/Extension、ProofSubject、InvariantTheorem、ResourceFlags 与 PsyAcceptance 六个关键 suite。
> EVM/Solana/NEAR/Noir cap-8 及 Aleo cap-2 的 dense Map 聚合 `StateStore` 已统一闭合
> store-then-read hazard：每个 target 以 target-owned atomic aggregate statement 先在 pre-store
> snapshot 上求值全部 leaves、再统一写；不同 `StateStore` 仍按序可见。Solana MapMini
> `put_into_empty` 已解除 ignore；WideMul 覆盖 UInt128/256 成功与 `0x1001` 溢出回滚，
> PrincipalStore 覆盖 wire identity 的 `len + 8×UInt64` state/param 叶、逐叶 equality 与高位清零。
> Mollusk 现为 Counter + 18 fixtures = 19 programs（CpiCaller 已随 #111 legacy call
> honesty 移除）；#113 已闭合 V1 单 state-account 安全负例矩阵（Custom(1)+完整 exact snapshot）；
> OptionState 6 项固定 `Option UInt64` 双叶 state。legacy call/schedule 在
> resolver/Plan/IR/SBPF 纵深 fail closed。该工程事实不改变 formal TASK 状态或各 target
> 成熟度上限。
>
> **2026-08-03 Solana #118 preflight update**：exact CPI profile/extension 的 private capability现可
> 从 retained `SemanticProgramV1` sole-derive handler-local role/site Plan，并经 private resolved IR生成
> 16-role direct-mapped ABIv1 concrete checks。1088-byte frame emitter只做 role count/walk/pointer/key/
> owner/data/header/privilege preflight，PDA/Token/ATA/provisioning继续 fail closed，且无 `sol_invoke*`。
> `AccountRoles` 经真实 authority/emitter生成 36,416-byte assembly，locked `sbpf 0.2.2` 产出
> 14,640-byte ELF；committed manifest绑定 source/profile/extension/boundary/text/ELF，Mollusk固定
> init/route/view、0/16/17 与 22 个 one-mutation full-snapshot negatives。该 ELF 只能称
> production-code-generated **test-preactivation ELF**，不是 `OutputFile`/产品 artifact；ordinary resolver
> 仍拒 sync；#119/#120 见下；#121+ 与 formal D5 仍 pending。
>
> **2026-08-04 Solana #119 unsigned companion CPI update**：独立 `CpiUnsignedIRV1` +
> `EmitCpiUnsignedSbpfV1` 在仍为 activationDenied/test-preactivation 的 lane 上，对 pinned
> companion-v1 发射真实 unsigned `sol_invoke_signed_c`（零 signer groups）与成功路径
> `sol_set_return_data(0,0)`；#118 no-invoke 链保持独立。exact Semantic-bound authority；
> `CompanionCpi` fixture 证明 state write → CPI → post-call write 的 source order，以及 fail 路径
> 完整 snapshot rollback + `fail:v1!` 保留。committed unsigned manifest 绑定 source/profile/extension
> 与 #115 companion ELF pin。准确称谓是 production-code-generated **test-preactivation unsigned-CPI
> ELF**，**不是** `OutputFile`/产品 artifact（当时 preactivation lane）；
> #120 见下；formal D5/Stage-0 hermetic/deployment 仍 pending。
>
> **2026-08-04 Solana #120 canonical PDA / signed CPI update**：独立 `CpiPdaIRV1` +
> `EmitCpiPdaSbpfV1` 在仍为 activationDenied/test-preactivation 的 opt-in
> `solana-sbpf-cpi-elf-v1` lane 上，对 pinned companion-v1 发射真实 canonical PDA/`invoke_signed`：
> frozen recipe `current-program-tagged-v1`（seed0 ASCII `proof-forge:pda:v1` + seedAuthority +
> seedTag LE + bump）、canonical bump 搜索 **255..1**、**bump 0 拒绝**（IR literal + runtime param）；
> 真实 `sol_try_find_program_address`（前 3 seeds）+ 返回 bump/key 与 authorityPda 全 32B 相等校验，
> 再 `sol_invoke_signed_c`（单 signer group / 4 seeds）。#118 no-invoke 与 #119 unsigned 链保持独立。
> Mollusk focused **21/21**（manifest identity、success/rollback、wrong-bump/noncanonical/bump0/role 负例）。
> 准确称谓是 production-code-generated **test-preactivation PDA-signed CPI ELF**，
> **不是** `OutputFile`/产品 artifact/activated sync；legacy profiles 仍 fail closed；
> ordinary resolver 在 #125 已激活 product sync（async 仍 FC） 产品 sync；
> 不把 Principal 全局等同 Solana pubkey；非 formal/hermetic/deploy maturity；#121 见下；#122+ 仍 pending。
>
> **2026-08-04 Solana #121 native System CPI update**：独立 `CpiSystemIRV1` +
> `EmitCpiSystemSbpfV1` 在仍为 activationDenied/test-preactivation 的 opt-in
> `solana-sbpf-cpi-elf-v1` lane 上，对 exact native System program 发射真实 System CPI：
> frozen identity `system-v1` **zero program id** + runtimeNative Agave commit pin；
> **transfer** 12B instruction（unsigned / 零 signer groups）与 **createPdaAccount** 52B
> （owner=current caller、canonical PDA/bump 复用、space≤4096）；**不重写、不加载 fake System ELF**
> （native runtime System only）。locked `sbpf 0.2.2` 产出 assembly **101762** bytes
> sha `eac15e4f65edf2af6c179c396185881ad1c72cd471e1a6ee1def31745e29aa89`、ELF **41576** bytes
> sha `e92259e5065ba4b181b8822afbce7c2194724e463a856d506c341271b5ca314e`。Mollusk focused
> **35/35**（native transfer/create 效果、inner failure、privilege/role/PDA/space 负例、
> 成功 transfer/create 后 caller overflow 全账户回滚）。#118 no-invoke / #119 unsigned / #120 PDA
> 链保持独立。准确称谓是 production-code-generated **test-preactivation native-System CPI ELF**，
> **不是** `OutputFile`/产品 artifact/activated sync；legacy profiles 与 ordinary resolver 仍 fail closed；
> #125 已激活 product sync（async 仍 FC）；不把 Principal 全局等同 Solana pubkey；非 formal/hermetic/deploy；
> **#121 工程切片已闭合**（已通过 `just docs-check`、SBOM 185、`just solana-runtime`（10 binaries/190 active，`cpi_system` 35/35；Mollusk **不属于** ordinary `just ci`，为单独运行）、`just test-targets`、`just dev-check`、ordinary `just ci` 全 exit 0；独立审计无 P0/P1）。
>
> **2026-08-04 Solana #122 classic Token CPI update**：独立 private `CpiTokenIRV1` +
> `EmitCpiTokenSbpfV1` 在仍为 activationDenied/test-preactivation 的 opt-in
> `solana-sbpf-cpi-elf-v1` lane 上，发射真实 classic Token CPI：frozen
> `transferChecked`/`transferCheckedPda`；真实 LoaderV3 `sol_invoke_signed_c`/canonical PDA；
> Token Account **165** / Mint **82** exact site-time predicates。vendored source-built
> official `solana-program/token program@v9.0.0` ELF **94960** sha
> `a19be3a2d4778533652da23b8fe31c4a341802f8e8c0c7b941b88581fc92d9d9`，annotated tag object
> `5c37ac99c248567bd7d50b965af8cbd45b6ced96`→peeled commit
> `dfb260231c761be7d9c8b63728e770a102b86495`，same-host clean **repeat2**，recipe digest
> `4af75b0a74ba14daa90a2d3913c71311609b3f3465728e733537dd0e34d8d063`；caller assembly **160129**
> sha `3cf744e36b5a91a441dbb06a33050613fcc366b57a03ad2b970f78cbe131e9fd`、caller ELF **67608**
> sha `4c7a10cc7dc5e411a9eec3109722e2080a48ac7a64868c34d4a60f7a813464c7`。Mollusk focused
> **31/31**。catalog digest corrected 为
> `0da1837ec10f7acc716c1151bee23a04e019174f99b1fedde635c7d75b4055f5`，但 **artifactBinding 仍
> absent / admitted=false**。#118–#121 链保持独立。准确称谓是 production-code-generated
> **test-preactivation classic-Token CPI ELF**，**不是** `OutputFile`/产品 artifact/activated sync；
> legacy profiles 与 ordinary resolver 仍 fail closed；#125 已激活 product sync（async 仍 FC）；**非** mainnet
> parity / tracked Tool Lock / cross-host / hermetic / formal / release / package-owner-published；
> Principal 不全局等同 pubkey；**#122 工程切片已闭合**（已通过 `just docs-check`、SBOM
> refresh/check **187**、`just solana-runtime`（**11 binaries/221 active**，`cpi_token` 31/31；
> Mollusk **不属于** ordinary `just ci`，为单独运行）、`just test-targets`（clean repo-local
> exact tool root）、focused；`just dev-check`/ordinary `just ci` 全 exit 0；独立审计无 P0/P1）。
> **#122 工程切片已闭合**；#123 见下；#125 已完成 product activation。
> 非 formal/Stage-0/release；Mollusk/`just solana-runtime` 属独立门禁，**不**并入 ordinary `just ci` 计次。
>
> **2026-08-04 Solana #123 classic ATA CPI update**：独立 private `CpiAtaIRV1` +
> `EmitCpiAtaSbpfV1` 在仍为 activationDenied/test-preactivation 的 opt-in
> `solana-sbpf-cpi-elf-v1` lane 上，发射真实 classic ATA CPI：frozen
> `createIdempotent`（data byte `01`、六 ordered metas：payer(w+s)/ATA(w)/wallet(ro)/mint(ro)/
> native System(ro)/classic Token(ro)；零 caller signer groups）；真实 LoaderV3
> `sol_invoke_signed_c`；canonical ATA address check（seeds wallet/classic-Token/mint under
> classic ATA program；`sol_try_find_program_address`；bump 非 caller signer group）；atomic
> **fresh** zero-lamport/zero-data System-owned **or existing** classic Token-owned 165B
> initialized ATA closed prestate。vendored source-built official
> `solana-program/associated-token-account program@v8.0.0` ELF **111136** sha
> `d3f6df6f95f8b81c482478cc8c44b67ac3de2ca03162eaaf6c587ee8db646519`，annotated tag object
> `de77f367fdc0341879b1b9f0224c6b86107e1769`→peeled commit
> `0b867b5340cd001e5980d8ca7928effc4e10015c`，same-host clean **repeat2**，recipe digest
> `f7ebe5236730d66ad730df6348b74332eb95e2abfda3377f389a13022e4528e2`；caller assembly **108322**
> sha `80ea42196a9a37a13012d4bcc720b50d97d6167e42dde88da501ef928d6364b9`、caller ELF **46872**
> sha `9902eb1e8a251b3352a08b4469e32003d7f82b980bc0f33b8557f0cf37d13e37`。catalog raw SHA-256
> `513c268853e59e5b274457ef95e7b4007f499897d4db50116d43a6be54da1ead` / domain digest
> `41ace268b3bea9837e4a1fc9e456dbfbd36c98a344e51dfd095ab4ffb2086351`（#122 历史 catalog domain
> `0da1837ec10f7acc716c1151bee23a04e019174f99b1fedde635c7d75b4055f5`；当前 requalification
> `41ace…`），但 ATA/Token **artifactBinding 仍 absent / admitted=false**。#118–#122 链保持独立。
> Mollusk focused **25/25**（fresh exact **2,039,280** rent+layout、full-result replay/payer
> unchanged、real Token TransferChecked usability、underfunded native System exact log+
> Custom(1)+empty return+full snapshot、post-CPI overflow ordered logs+full rollback、独立
> SHA256+curve oracle、完整 single-mutation matrix）。full `just solana-runtime` exit 0
> （**12** integration binaries / **246** active tests，`cpi_ata` 25/25；Mollusk **不属于**
> ordinary `just ci`，为单独运行）。strict ATA build manifest 现在逐值绑定 provenance 并有
> **4** mutation self-tests。准确称谓是 production-code-generated **test-preactivation
> classic-ATA CPI ELF**，**不是** `OutputFile`/产品 artifact/activated sync；legacy profiles
> 与 ordinary resolver 仍 fail closed；#125 已激活 product sync（async 仍 FC）；**非** mainnet parity /
> tracked Tool Lock / cross-host / hermetic / formal / release / package-owner-published；
> Principal 不全局等同 pubkey；**#123 工程切片已闭合**（已通过 `just docs-check`、SBOM
> refresh/check **189**、`just solana-runtime`、focused Lean/Rust、`just test-targets`
> （clean repo-local exact tool root）、`just dev-check`/ordinary `just ci` 全 exit 0；两轮
> 独立审计最终无 P0/P1）。**#111–#123 已闭合**（历史 Active 曾为 #124；见下 #124 闭合）。非 formal/Stage-0/release。
>
> **2026-08-04 Solana #124 composite escrow CPI update**：独立 private `CpiEscrowIRV1` +
> `EmitCpiEscrowSbpfV1` 在仍为 activationDenied/test-preactivation 的 opt-in
> `solana-sbpf-cpi-elf-v1` lane 上，闭合 composite escrow forcing golden：真实 **System → ATA →
> Token** CPI 序（native System createPdaAccount + classic ATA `createIdempotent` + classic
> Token `transferChecked`/`transferCheckedPda`；真实 `sol_try_find_program_address` /
> `sol_invoke_signed_c` / `sol_set_return_data`）。final fixture `EscrowCpi.lean` **5378** B
> SHA-256 `0424045e7cdc7e3c57b79d95c144e6047819db91b46c39607e42bf256b7c33bf`；assembly
> **366006** B SHA-256 `577f40646abb0a355bedebb76dd6b208ff39ae802bea1dab21ce4795ba5d102b`；
> caller ELF **158536** B SHA-256 `28744d799b9a58208a54066d730a97a45e4363ae4f407132cf49c0bc7782b5f9`；
> final ELF **37** calls 仅 `sol_try_find_program_address`/`sol_invoke_signed_c`/
> `sol_set_return_data`；frame **maxScratch793→reserve800→CPI_BASE2024→2824** bytes。
> Mollusk focused **36/36**；full `just solana-runtime` exit 0（**13** integration binaries /
> **282** active tests，`cpi_escrow` 36/36；Mollusk **不属于** ordinary `just ci`，为单独运行）。
> SBOM package-file pin 当前 **191** files。ATA/Token **artifactBinding 仍 absent/admitted=false**。
> **sequential world overlay** 语义：同 world 内 source-order CPI 叠加与 failure full snapshot
> rollback，**不是** 多顶层 transaction atomicity。#118–#123 链保持独立。准确称谓是
> production-code-generated **test-preactivation composite-escrow CPI ELF**，**不是**
> `OutputFile`/产品 artifact/activated sync；legacy profiles 与 ordinary resolver 仍 fail closed；
> #125 已激活 product sync（async 仍 FC）；**非** mainnet parity / tracked Tool Lock / cross-host / hermetic /
> formal / release / package-owner-published；Principal 不全局等同 pubkey；**#124 工程切片已闭合**
> （`just docs-check`、SBOM **191**、`just solana-runtime` 13/282、focused Lean/Rust、
> `just test-targets`、`just dev-check`/ordinary `just ci` 全 exit 0 为工程门，非 formal）。
> **#111–#124 已闭合**；随后 **#125** product activation 工程闭合（见下）；formal 状态与 GitHub issue 状态
> 不因本工程收口而改变。非 formal/Stage-0/release。

>
> **2026-08-02 invariant lowering update**：`NormalizeV1` 现将 source `invariant` 降为
> source-order Semantic `.invariant` callables 与 dense `InvariantDecl`，并复用 Wire sole closure membership/
> exact `invariantSteps` 公式；Provenance 对 multi-block predicate CFG 做 exact entity coverage。`.proof`
> 继续只属 certification metadata，不改变 semantic bytes/hash。Quint 对 Q0 read-only Bool invariant 已 lowering；其余八个 materializer 对 nonempty invariants 继续
> fail closed；formal TASK-D2-06/07 与 TST-SEM-001/002/003 状态不变。
>
> **2026-08-04 ADR-0027 + product CLI inline cutover（engineering）**：sole 产品 proof 链为
> 单次 read → `selectProgramV1ProductWithTheoremInventory` → compile →
> `certifyInlineProofV1` → target resolve/materialize；`--proof-bundle*` 已删（unknown）；
> ProofBundle 库模块非产品 alternate。check 报告 `proofStatus`/count/digest；build 仅门禁。
> **Narrow engineering closure 已完成**：(1) structure→encode→decode→`ProofedProof.safe`；
> (2) legal-only production simple-closure encode/decode（`SimpleClosureCertV1` /
> `LiteralTrueInvariantWitnessV1`）；(3) exact ordinal-0 `InvariantTheoremV1`；(4) literal-true /
> public-Bool-view **same-file ordinary Lean theorem** 经真实 inline product `check` 得
> `proofStatus=certified` + count=1/digest，theorem body 改写不改 `sourceHash`/`semanticHash`
> 但改变 certification digest；(5) false theorem/inventory/audit fail closed、proof gate早于
> target resolve且零 destination/staging；package-owned session 排除 ambient `LEAN_PATH` /
> 用户 `.olean`。in-process elab ≠ sandbox；allowed axioms 仅 `Classical.choice`/
> `Quot.sound`/`propext`。Quint Q0 对 read-only Bool invariant 已开放；其余八个
> materializer 对 nonempty invariants 仍 FC。**不**声称 formal
> TASK/TST-PROOF-001、reachability、target refinement、hermetic/release。详见
> [`docs/adr/0027-inline-same-file-theorem-certification.md`](docs/adr/0027-inline-same-file-theorem-certification.md)。
>
> **2026-08-02 const reference update**：`NormalizeV1` 现于 fn signatures 后、callable bodies 前
> 建完整 source-order constant lookup；body bare const 每次降为独立 `Op.Constant`，支持 forward ref、
> locals/params shadow、窄 UInt expected type 与 entry/view/pureFn/invariant。Provenance 精确绑定
> `.constant`、ConstDecl type、body place 和 Bool requirement origin。const 声明表达式仍只接受
> literal/negative Int；EVM/Solana/NEAR/Noir 对 nonempty constants 表 fail closed；**Aleo ALEO-CONST
> （2026-08-08）** 已开放 literal-backed `Op.Constant` 内联（同 `lowerLiteral` envelope → Instructions
> 字面量操作数）；Psy 已开放精确可表示的 UInt8/16/32、Bool、`UInt64 < Goldilocks p` 与非负 Int64 Constant lowering（Goldilocks ConstantV1 复用 decoder，但 source 无 Field literal；负 Int64 与 `UInt64 ≥ p` 保持 fail closed；无新 ABI/emitter primitive）；post-declared novel const shape 的 engineering semantic identity 会 cut over。
>
> **2026-08-03 String interface update**：`NormalizeV1` 的 public event/error 字段现允许
> anonymous legal UInt/Int/String；String 字面量复用 canonical wire valueBytes，located product compile
> 与 Reference Emit/Revert 已覆盖。该切片不定义 target ABI：EVM/Solana/NEAR/Noir/Psy/Quint 在 Plan
> fail closed，Aleo 在 `effect.event` requirement resolver fail closed；非 formal D2/D4 完成。
>
> **2026-08-03 Quint Q0 target update**：工程 registry 扩为 12 targets / 9 implemented +
> 3 design-only，resolver 为 13 rows（含 Noir ACIR dual-write profile）；历史新增 `quint-source-u64-model-v1` 的 target-owned
> Plan/IR/`.qnt` emitter 与 zero-tool finalize。Q0 只接受 public UInt64/Bool/Unit single-block
> 子集、zero-payload declared revert 与四个 S2 键（rollback/state/Bool/checked arithmetic）；失败调用
> 显式记录 identity-preserving outcome 且业务状态 stutter，输入域保持完整 UInt64；StateStore overlay
> last-write-wins，pureFn/fully-expanded expression 有独立资源上限。产品不运行 Quint/Apalache/TLC/Java；
> ITF/MBT/verify 属后续 profile，非 deployable target、非 formal D3/D4 完成，accepted PRD Phase 1 仍为四目标；见 ADR-0026。
>
> **2026-08-03 D3-E7 artifact closure update**：engineering `proof-forge.output.v1`
> 的 `files` 已从 path-only 数组原子迁为 closed descriptor（`materialized-base` /
> `finalized-extra` + path + size + exact-byte SHA-256），并新增 exact evidence sidecar digest。
> publisher 使用 sole scanner 生成 private inventory、pure mint OutputSet、manifest-last 后重扫比较；
> `inspect <output-dir>` 会重新打开 artifact 并验证 exact disk closure。该结果仅是工程 stable
> observation，不声称 retained-FD/race-free/contained/hermetic/formal `OutputSetV1`。
>
> **2026-08-03 NFR-REPEAT update**：ordinary `ci-target-smoke` 现运行
> `just nfr-repeat`，对同一 built binary 连续构建 Counter 的 Solana 默认 plan 与 Noir 默认
> source profile各两次；每树先重验 descriptor/evidence/exact closure，再要求两份 sidecar exact-byte
> 相等。该门仅是 same-host/zero-tool engineering subset，不是 hermetic/clean-room/multi-host/full-target/
> formal NFR-001 或 release evidence。
>
> **2026-08-03 RES-1B output-only update**：build 的
> `artifact-output.published-bytes` lower-only cap 现于 sidecar write/rename 前按 base+extra artifacts
> 与 exact evidence/manifest UTF-8 总量强制；equal 接受，over 以 `PF-RESOURCE-OUTPUT`/exit 6
> 清理 staging 且不发布 destination。memory/process/protocol/stderr、receipts/containment 与 formal
> NFR-008 仍 pending，禁止把该子切片写成完整 RES-1B。
>
> **2026-08-07 Aleo identity/query/compiled-finalization update（engineering）**：Aleo Plan
> 已有 canonical content digest domain `pf.aleo-plan.engineering.v1` 并进入 BuildIdentity；产品
> materialize 有序发出 `{programId}.aleo` 与 `{programId}.aleo-query-contract.json` 两个
> `materialized-base`，后者仅描述 public mapping、bare view 与 `resultDropped` 的
> **network-state descriptor**，不执行 `leo query`、不把 Final 当返回通道。默认
> `aleo-leo-4.0.2-u64-v1` 仍 zero-tool；显式
> `aleo-leo-4.0.2-u64-compile-v1` 与 default 共享 Plan/planDigest、独立 claim/BuildIdentity，
> product Finalize 在 staging 外临时 package + 隔离 HOME 中只编译 `.aleo` base，使用 Tool Lock
> Leo 4.0.2 运行 `build --offline --disable-update-check`，并发布 `.compiled.aleo`、`.abi.json`、
> `.leo-program.json` 三个 `finalized-extra`。missing/bad tool/partial outputs 均零发布；双次同机
> exact bytes 与 `inspect` disk closure 已钉。两 profile 均 `deployable=false`。RPT-024 已证
> execute/deploy/query 依赖网络、synthesize 需要 CRS 且无 pinned snarkOS/snarkVM，因此成熟度为
> **source emission + engineering locked compile finalization**，非 VM/proof/deploy/hermetic/formal。
>
> **Capability caveat（2026-08-04 B-CALL-SEM + #111 honesty）**：`call` / `schedule` 的 resolver
> support 不能等同于完整平台语义。EVM sync 为 static-QN→hashed-address 的真实 `CALL`，
> result-bearing UInt64 路径读取并校验 `RETURNDATA`（BL-28；更宽类型仍 FC），但 hashed callee
> 尚无 deployment-address binding；**EVM `schedule` 是同笔交易内同步执行的 fire-and-forget**
> （结果丢弃，匹配 Reference 无 response cursor 契约，与 CW-4 SubMsg 同一接纳纪律），**不是**
> 跨交易 deferred。**Solana 两个 legacy profile 已删除双键 support claim**，Plan/IR/SBPF 对旧
> call/schedule 节点纵深 fail closed；此前过渡 CPI / `sol_log_data` 观测桩不再可达，也不计作 CPI；
> 真实多账户/PDA/bump/CPI 仅由 opt-in versioned profile epic #110 打开。Noir 两键为
> witness-binding relation（电路不执行调用、证明不 attest 链上调用发生，result-bearing 待
> response-witness 契约仍 FC）；NEAR schedule 为原生 promise（无 sync）；TON 为真实异步
> out-message（无 sync）；CW 为 same-tx SubMsg（reply_on=never，非跨 tx async）；Psy sync 仅为
> `__invoke_sync#<Felt>` 源码面发射（无 VM/proof 验收门），schedule 已 decline + emitter FC；
> Aleo 双键 + event 全 decline；Quint 全 FC。`B-CALL-SEM` 继续 open：部署地址接线、Solana
> exact CPI 产品面与 Noir response-witness 契约闭合前，不得写成跨平台 call 已完成。

`docs/04-task-breakdown.md` 与既有 evidence ledger 继续保存 D0/D1 release-qualification
历史，但不再生成日常产品工作的完成条件。恢复期间禁止新增 `D1-PA-*`、资格协议、
custody 服务或 formal-evidence ceremony。

成熟度声明仍必须准确：EVM 已有 `solc` bytecode、**EvmSolc 真实 solc 编译验收门**
（M4 PlanSchema/ValidateIR + 产品 Yul 过 `solc --strict-assembly`）与 **G4 工程 Anvil 差分**
（Counter/Accumulator/ArithOps/EventFlow，含 overflow state-hold 与 emit 日志；formal
Reference↔Anvil closure 仍缺）；NEAR 已有 locked `wat2wasm` 结构编译 + host-optional
Wasm runtime 实例化门、deterministic HostModel，
以及 locked near-sandbox 2.13.0 的 Counter deploy/init/mutate/view receipt **工程 happy-path 门**
（非 Reference↔sandbox formal 差分或 Stage-0）；Solana 已有真实 SBPF 汇编发射
（`EmitSbpfAsmV1` 完整 Operation 表面）+ 锁定 `sbpf`（blueshift-gg/sbpf 0.2.2
pinned d835bc6e，cargo-git source-build 供给，Tool Lock v4）汇编成 Solana ELF `.so`
（`solana-sbpf-elf-v1` profile 产品链路，Counter 已端到端产出 ELF）+ **S3a+S3b Mollusk
运行时差分**（`runtime-tests/solana` + `scripts/solana_runtime_test.sh`：Counter +
18 fixtures = 19 programs 覆盖控制流/effect、窄 ABI/result、Array/Map、WideMul
UInt128/256 成功及 `0x1001` rollback、Principal `len + 8×UInt64` identity storage/param、
OptionState 双叶 state；legacy call/schedule 已 fail closed，真实 CPI 见 epic #110；
Principal 仍非 Solana pubkey；非 formal Stage-0/hermetic）；**T8/T9 已铺四 target**：
state/param UInt8/16/32 ABI + body 多宽 + 窄结果 + 窄 Int（EVM 另开 UInt128/256，Solana/NEAR 软件多字 128/256 add/sub/cmp/bit；Int128/256 仍 fail closed）；Noir 保持 source-only，
但有 locked nargo 1.0.0-beta.26 compile-only 工程门（无 ACIR/witness/prove/verify）；Aleo 有
opt-in locked Leo 4.0.2 product compile finalization（仍 `deployable=false`，compile-only 非 prove/deploy）；
Psy registry 保留历史默认 `psy-dargo-u64-v1` 并新增显式 `psy-dargo-0.1.0-vm-v1`（UInt128 4×UInt32 limb
的 state/param/literal/constant/entry-view return、checked add/sub/mul/div/mod、六比较；mul=8×UInt16
schoolbook，div/mod=四段 restoring；bitwise/shift=per-limb &\|^/~ + UInt32-count 128-step walk；UInt256 仍 FC），有 optional direct dargo compile
（`PsyAcceptance`，无 psyup）与**独立 host-heavy** `just psy-runtime` local-VM/base-proof 菜谱
（locked dargo v0.1.0 + std；仅 linux-x86_64/darwin-arm64；非 ordinary ci；产品 Finalize 仍 zero-tool；
两平台 Tool Lock pin 已落地；2026-08-07 Linux exact-member root 的 Counter + WideCounter 门实测通过，
Darwin 仅完成锁文件/成员 pin 验证）；Aleo/Psy 均无 network UPS/deploy/formal 闭环。CosmWasm 已有
target-owned Plan/IR/materialize/finalize（`wasm-validated-alpha`：WAT+wat2wasm+cosmwasm-check、
cosmwasm-vm 28-test mock 与 wasmd v0.70.3 Docker rung-1；sync 拒、async Binary SubMsg 子集；非主网/formal）；
TON 已有 Plan/IR/materialize/finalize（source-only + locked tolk/fift BoC、schedule createMessage PARTIAL
与 sandbox 10/10 工程门）。registry label 与工程门均不得冒充 formal/hermetic、主网部署或证明完成。

## Mandatory Reading Order

1. 完整阅读本文件并运行 `git status --short`，确认不会覆盖其他人的工作。
2. 阅读 [`RECOVERY.md`](RECOVERY.md)、[`MIGRATION_MATRIX.md`](MIGRATION_MATRIX.md)、
   [`docs/document-status.md`](docs/document-status.md) 与 [`docs/index.md`](docs/index.md)。
3. 阅读 [`docs/01-prd.md`](docs/01-prd.md)、[`docs/02-architecture.md`](docs/02-architecture.md)
   与 [`docs/03-technical-spec.md`](docs/03-technical-spec.md)。
4. 只阅读当前产品切片直接相关的模块规格、ADR、目标档案与测试规格；不要因为历史
   qualification 引用而扩张工作面。
5. 修改前核对真实代码和工具链；声称完成前运行聚焦测试与普通产品 CI。

## Non-Negotiable Product Boundaries

- 用户源码只有统一 `program Name where` 入口，不得加入用户可写顶层类别标记。
- frontend 和 `SemanticProgram` 不依据 `TargetId` 改写语义。
- `TargetId`、`CodegenProfileId`、`NetworkProfileId` 是三个独立身份。
- Wasm 只可共享 AST/编码/结构验证；NEAR、CosmWasm、Soroban、ICP 各自拥有 Plan。
- Noir、OpenVM、Aleo、Psy 分别属于电路、zkVM、ZK 应用链边界，不共用伪通用 Plan。
- 每项 capability/extension 必须精确版本并 fail closed；禁止 best effort 或隐式 fallback。
- 每个 materializer 保留关联 `Plan` 和 `TargetIR` 类型；不得擦除成 `Unit`、字符串或 JSON。
- V2 禁止依赖 `active/` 归档、`ProofForge.*` import、旧制品、旧二进制、symlink 或运行时回退。
- `ProgramV1` 产品路径必须直接从 Syntax 构造并在 Typed 边界消费；禁止新增
  legacy→ProgramV1 adapter、产品 dual reader、第二套 ProgramV1 decoder 或 fallback。

## Recovery Execution Protocol

1. 一次只推进一个 **shared-core cutover**，顺序为 D1 → D2 → D3 → D4（EVM-first）；shared API 未冻结前禁止并行发明 adapter。接口冻结后可在临时隔离 worktree 并行推进文件完全不重叠的 leaf lanes（含已具 target contract 的 implemented-target 机械迁移；registry-only target 必须先补齐 Plan/IR/materializer），但 `main` 始终是 sole integration authority，shared docs/registrations/SBOM/commit 由主代理串行维护；lane 合并后立即移除 worktree，不顺带提高 D5–D7 maturity。
2. 先写聚焦失败测试，再实现满足现有规格的最小代码；不为恢复工作新增 `TASK-*`、
   `TST-*`、`EV-*`、freeze package 或 qualification object。
3. 日常反馈使用 `just dev-check`；普通合并检查使用 `just ci`。
4. 当前 `justfile` **未注册** `governance-check` / `release-check`；不得声称执行过这两个
   recipe，也不得临时拼装命令冒充它们。若恢复历史治理审计或发布预检，必须先显式决策、
   补齐可验证 recipe 与文档；正式 Stage-0 仍只允许调用下述直接命令。
5. development completion 与 release qualification 分开记录。普通 CI 成功不得写成
   hermetic/formal evidence；release 主机不合格也不得反向否定已通过的产品测试。
6. 检查 unsupported 声明、`active/`/v1 泄漏、非确定制品、无关变更和生成垃圾。
7. 修改 `ProofForgeV2/**` 时运行相关 Lean 测试，并执行 `just sbom-package-files-refresh`
   保持 committed package-file pin 精确；这只是供应链闭包一致性，不代表 formal qualification。
8. 实现可交给 Grok Build worker；主代理必须独立审计 diff、产品边界和门禁结果。
   worker 自检不得冒充 release/formal 独立复审。
9. 交接时给出精确文件、命令、结果和限制；不提交、不推送，除非用户另行要求。

正式 Stage-0 若用于真正的 release 判断，仍只能由调用者直接执行：

`/usr/bin/env -i HOME=/var/empty PATH=/usr/bin:/bin LC_ALL=C TZ=UTC /bin/bash --noprofile --norc scripts/verify_host_stage0.sh --require-eligible`

当前不存在 `just release-check` recipe；未来若恢复，同类 wrapper 也只能算 release preflight，
不自动生成正式证据。

## Documentation Protocol

- 状态只使用 `draft`、`proposed`、`in_review`、`accepted`、`superseded`、
  `archived`、`not_started`。
- 产品行为变化同步更新相关规格或架构说明；不要批量重写历史 ledger/log/review。
- `06-implementation-log.md` 只保留已执行事实；`07-review-report.md` 不得预填通过。
- 文档变更运行 `just docs-check` 与 `git diff --check`。
- 不新增治理层来管理恢复工作；`RECOVERY.md` 是短期单一执行指针。

## Definition of Done

开发切片完成意味着：真实产品路径可运行、聚焦测试与 `just ci` 通过、文档与成熟度声明
准确、没有 fallback 或无关扩张。发布资格是独立的更高门槛；当前仓库没有
`just release-check`，因此只能由上述直接 Stage-0 命令与符合资格的外部正式流程判断，且不是
日常产品开发的完成条件。
