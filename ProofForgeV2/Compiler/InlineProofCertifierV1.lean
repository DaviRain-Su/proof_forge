import ProofForgeV2.Compiler.CertifiedInlineProofV1

/- Compatibility import. The sole public `certifyInlineProofV1` mint and its
   private audited core live together in `CertifiedInlineProofV1`, preventing
   callers from pairing an independently supplied Environment with raw bytes. -/
